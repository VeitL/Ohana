# Testing and Release Status

> Active validation dashboard only. The verbose pre-audit-sync ledger is
> archived at
> [`docs/archive/testing-progress-2026-07-10-pre-audit-sync.md`](archive/testing-progress-2026-07-10-pre-audit-sync.md).
> Older history is at
> [`docs/archive/testing-progress-full-2026-06-25.md`](archive/testing-progress-full-2026-06-25.md).
>
> Workflow: [`docs/ai-module-test-playbook.md`](ai-module-test-playbook.md).
>
> Status ownership: [`docs/status-ledger-map.md`](status-ledger-map.md).
>
> True-device plan: [`docs/release-true-device-test-plan.md`](release-true-device-test-plan.md).

## Current Release Read

- Last compacted: 2026-07-18.
- Release bar: **known Open P0 = 0; first-release-reachable implementation / proof P1 = 1**.
- Active phase: **finish Free / Personal Storefront acceptance, then resume
  final signed-device RC acceptance**. The Human-first/D28 repository and
  Simulator lane closed on 2026-07-18 after final targeted tests, a complete
  release-static pass, optimized Release compilation, and protected Dogfood
  existing-user acceptance.
- Open follow-ups: 11 total in `docs/task-follow-ups.md`.
- Open P1: 6 total: 1 current first-release implementation/proof gap, 1
  deferred CloudKit 1.x item, and 4 physical-device validation items.
- Current decision: the 2026-07-11 Pet-first evidence is historical for the
  superseded route. Do not submit to App Store or call the app RC-ready until
  TFU-20260715-003 and the physical-device P1 items are dispositioned.

## Evidence Reconciliation

Dated results describe different surfaces and must not be collapsed into a
single “all tests pass” claim:

| Evidence date | Surface | Result | Current meaning |
| --- | --- | --- | --- |
| 2026-07-18 Human-first/D28 repository and Simulator closure | Final Task Center refactor, starter-journey read path, Plant room-stack geometry, static governance, optimized Simulator Release, and the pinned existing-user Dogfood lane | Five focused suites executed 57/57 tests; after the final warning-only isolation annotation, `HouseholdStarterJourneyServiceTests` executed 15/15 with artifact `.build/DerivedData/tests/Logs/Test/Test-OhanaUnitTests-2026.07.18_01-05-51-+0200.xcresult`. The complete `scripts/release-hardening-check.sh --static-only` passed on the final source: complexity scanned 1,025 production files with 74 grandfathered declarations and no new/growing hotspot; V4 UI, accessibility, smoothness, route, runtime, data-safety, localization, resource, secret, and Git-size checks all passed. Resource integrity retained only the non-blocking `Assets.xcassets` 65.92 MiB review advisory. `scripts/build-release-fast.sh` produced the final 399 MiB unsigned `-O` app. The guarded Dogfood launcher then built and overlaid the exact WMO Release app, preserved its sealed store identity, and normal UI proved the Task Center card guide, real nested editor cancel, `Not sure yet` branch, 1/3 to 2/3 progression, relaunch resume at `petBodyProfile`, and unchanged 162-coconut balance. Final read-only status remained ready with 1 Human, 1 Pet, 17 ledger facts, and zero test artifacts; `overlay=pass` was recorded at `2026-07-17T23:23:41+00:00`. | Closes TFU-20260715-002 at the repository and Simulator level. It is risk-targeted current-source evidence, not a current full-Unit replay, signed Archive, physical-device, Storefront, notification, background, energy, or assistive-technology claim. Those external lanes remain separately owned. |
| 2026-07-18 local storage and conflict-copy cleanup | Authorized cleanup of verified conflict copies that had repeatedly filled the disk | Removed 244 invalid spaced Git-object copies and 12 byte-identical untracked/ignored LaunchMark conflict children, reclaiming 5,360,891,808 bytes. `.git` fell from about 6.0 GiB to 1.0 GiB; `git fsck --full --no-dangling` passed, the canonical static Git-size check reports zero garbage, and 48 GiB is free after the final Release/Dogfood builds. The three stable DerivedData roots and all user product data were preserved. | The recurring 5 GiB growth cause is removed and the build environment is healthy. This is storage integrity evidence, not product behavior evidence. |
| 2026-07-18 focused Human-first/D28 Unit/Integration | Current App + Unit source on guarded `iPhone 17 Tests` across onboarding coordinator, handoff responsiveness, starter gift, household journey, Task Center snapshot/guide, reset, growth unlock, and compatibility | Nine selected suites executed 97/97 tests with 0 failures or skips. Artifact: `.build/DerivedData/tests/Logs/Test/Test-OhanaUnitTests-2026.07.17_23-58-13-+0200.xcresult`; source hash `ce6b0d8f52f63d80bec1db3682c857d4e2e6a732ff4d91b6ba8c315afd516459`, contract hash `6665d5759e7e900bb0bc33460902840a52ef4f809bb9fcd86fd8931fbcb37b2a`. | Current focused domain evidence for TFU-20260715-002, not a full Unit-suite, full release-hardening, or signed-device claim. The build also surfaced existing Swift 6 concurrency warnings that remain debt rather than test failures. |
| 2026-07-18 pre-repair release-static attempt and tail diagnosis | `scripts/release-hardening-check.sh --static-only` plus direct read-only execution of every check hidden by its fail-fast stop | The canonical lane passed syntax, whitespace, fixture/self-tests, CI policy, the exactly-once 119-selector manifest, runtime guardrails, and architecture boundaries before production complexity failed on `TaskCenterRouteContainer.swift` `type_body_length` max 918 and `TaskCenterRouteDataActor.load` `function_body_length` max 124. Direct tail execution found 13 later checks green, including economy/member/derived/shared-care/data-safety/localization/governance/resource/smoothness/route/gitleaks/git-size. V4 UI remained red on 5 findings (3 true hits across 2 geometry-token causes; 2 intentional card-stack effects needed scoped allows), and accessibility remained mechanically red on 8 source-pattern findings classified as chained-modifier/parent-Button false positives. | Historical diagnostic attempt. The final closure row above supersedes every blocker with a complete current-source pass. |
| 2026-07-18 pre-repair optimized unsigned Release compile | `scripts/build-release-fast.sh`, Release `-O`, incremental dual-architecture generic iOS Simulator product | Build succeeded and verified that Settings DesignLab is excluded. The arm64 + x86_64 app was `.build/DerivedData/release/Build/Products/Release-iphonesimulator/Ohana.app` (399 MiB; binary SHA-256 `96e66106ad0923746cc623d1fab2cc88b2c64fe17eb4b7ca518a72d1998758f9`). The cold cache consumed 2.6 GiB and left 47 GiB free. It exposed three LaunchMark conflict-copy warnings plus Swift isolation and smaller existing compiler diagnostics. | Historical compiler checkpoint. The final closure row supersedes its binary and resolved the LaunchMark plus starter-journey/Task Center isolation diagnostics; the current product remains unsigned and does not prove physical-device or Storefront behavior. |
| 2026-07-17 post-checkpoint UI continuation | Unchanged App source across the frozen and UI-test-only revised `OhanaUITests` provenances on `iPhone 17 Tests` | Ninety-four additional distinct selectors now have trusted green evidence. Beyond the first identity/profile/preventive/care/Memorial continuation, later batches cover Human records/routes/gestures/avatar/wishlist/delete, store retry, local privacy, Pet edit/delete/hygiene/health/Memorial/walk safeguards, Calendar CRUD/filter/keyboard/title routing, feeding/Water/litter/scoop plans and records, Preventive Health replacement and single-claim readback, economy projection/spend/ledger, the expanded Plant module, Settings observability/report routes, first-release reachability with production overlays, launch smoke/performance, Personal gating, Back-navigation state, Reduce Motion, synthetic relaunch-persistent Cat and Dog long sessions on `iPhone 17 Tests`, and an existing-user journey without reset. The held Member Card relaunch selector passed after the one-shot seed helper was repaired and rebuilt. Cumulative trusted campaign coverage is 119/119 distinct formal selectors; none remains without green evidence. The latest rebuilt provenance has 3/3 distinct executed selectors green; its focused Walk rerun also reproduced a non-failing CoreLocation main-thread runtime warning at live route startup. | Continuation evidence for TFU-20260715-002. The 119-selector total spans compatible UI-test-only provenance identities against unchanged App source and is not presented as an exact-current-build 119/119 or single-run full-suite claim. The reproducible runtime warning remains a P2 attribution/performance risk under TFU-20260629-004. Diagnostic failures and final passing artifacts are recorded in [`UI_CONTINUOUS_QA_CHECKPOINT.md`](audits/2026-07-17/UI_CONTINUOUS_QA_CHECKPOINT.md). |
| 2026-07-17 continuous Human-first UI checkpoint | That exact `OhanaUITests` App + UI build on the guarded `iPhone 17 Tests` simulator | The source compiled under the UI scheme and 25 distinct selected selectors passed with 0 failures. They cover immediate and deferred first-Pet onboarding, explicit and interrupted starter-gift claim, Oasis unlock, skip/back/cancel/reopen paths, Human/Pet deletion and Memorial routing, member-card privacy, unknown/not-applicable answers, real-data supersession, relaunch persistence, reward idempotency, and the Plant room-stack handoff. The formal manifest remains 119 selectors, so 94 lacked green evidence from that exact build. Detailed selectors, artifacts, provenance, and later campaign coverage are recorded in [`UI_CONTINUOUS_QA_CHECKPOINT.md`](audits/2026-07-17/UI_CONTINUOUS_QA_CHECKPOINT.md). | Historical exact-provenance Simulator evidence that contributed to the now-closed TFU-20260715-002 lane; not a current 119/119 replay, signed-device acceptance, or a substitute for natural-day Dogfood accumulation. |
| 2026-07-16 Free / Personal implementation snapshot | Current local catalog, StoreKit adapter/config, entitlement service, Free quota readers/gates, feature gates, paywall, Settings, and focused test sources | `jq` accepted `Ohana.storekit`; commercial Swift parsing, scoped SwiftFormat and strict SwiftLint passed; a direct 1,040-file App-source `swiftc -typecheck` with the project MainActor/upcoming-feature flags completed with no Personal error. `git diff --check` passed. No App/test target or test executable ran because only about 14 GiB was free and 94 Swift File Provider conflict copies remain outside an approved disposition. | Local implementation/type-system evidence, not target, runtime, Sandbox, signed Storefront, nine-language, accessibility, or release proof. TFU-20260715-003 stays open. |
| 2026-07-16 Free / Personal commercial decision | Product foundation, entitlement contract, privacy/compliance, README, and active release ledgers | The product owner approved Free with 1 active Pet, 2 active Humans, 5 active Plants, and 3 ordinary active logical plans; basic care records, existing-history viewing/editing, manual export, critical-health plans, memorials, and over-quota grandfathering stay available. Personal uses monthly/yearly/Lifetime products, offers a target 14-day yearly trial, unlocks unlimited active capacity plus approved advanced local functions and Supporter cosmetics, and treats verified legacy Supporter purchases as Personal Lifetime. Family/Care+ remain future subscriptions and ads remain absent. This documentation update does not itself prove implementation, tests, Store configuration, or runtime behavior. | Current approved product contract. TFU-20260715-003 now owns implementation and validation of the new catalog, quota semantics, grandfathering, downgrade, paywall, StoreKit lifecycle, App Store Connect, Sandbox, second-device restore, and signed Storefront. |
| 2026-07-15 Supporter Pack commercial policy and implementation worktree | Historical product contract, StoreKit service, cosmetic gate, and targeted source/typecheck evidence | The then-approved structure was complete free Solo plus one cosmetic non-consumable. Swift/config checks and standalone warnings-as-errors typechecking passed, but full targets, runtime, and Storefront were not proven because the storage and File Provider conflict-copy blockers remained. | Superseded by the 2026-07-16 Free / Personal decision. The code and evidence remain migration input for legacy Supporter grandfathering and cosmetics, but cannot prove the new catalog or quotas. |
| 2026-07-15 Human-first onboarding and household starter journey worktree | Then-current uncommitted source, tests, and active specifications for D17/D28 | Source review confirmed a required local Human name, immediate/deferred first-Pet branches, Task Center create/claim system items, an explicit idempotent 50-coconut island claim, Oasis locking, and six independently claimed member-wallet rewards totaling 400. That documentation task did not execute the new tests or a clean-Simulator journey. | Historical implementation-intent checkpoint. The 2026-07-18 closure row supersedes its missing proof; older Pet-first/no-Human runs remain historical. |
| 2026-07-12 local-only account decision | Current source, project configuration, entitlements, privacy manifest, Settings, Reset, onboarding, and active product documents | Both unshipped identity experiments were removed. The current app has no Sign in with Apple capability, login UI, Auth SDK, Supabase code/configuration, remote session, account deletion path, or account data collection. Restricted iCloud Drive backup remains independent. Validation for this cleanup is recorded in the newest validation snapshot below. | Current product truth. Future account/backend boundaries are planning-only in `docs/planning/account-backend-extension.md`; they are not an active follow-up or release capability. |
| 2026-07-12 local-only signed Release | WMO Archive, strict signing/capability inspection, non-destructive overlay installation, and CLI launch | `scripts/archive-release-local.sh` succeeded and verified `/tmp/OhanaArchives/2026-07-12-180306/Ohana-c2aa2af859-dirty.xcarchive`. The arm64 iPhone-only/iOS 26.2+ App passed Xcode store validation, strict `codesign`, designated-requirement, and xattr checks. Its signed entitlements contain HealthKit + CloudDocuments only; no Sign in with Apple, CloudKit, APNs, App Group, remote-notification mode, extension, Supabase/crypto artifact, collected-data type, or tracking domain is present. It overlaid and launched on iPhone 17 Pro Max / iOS 26.5.2 without uninstalling or clearing data, and its process remained present. | This remains the newest signed local-only package evidence, but it predates the current Human-first/D28/Personal worktree and cannot prove the current build. Its capability inspection remains historical input for R0; a new signed package is still required. |
| 2026-07-12 superseded identity experiments | Device-local Apple identity, later provider-neutral/Supabase prototype, and a development-signed Archive containing Sign in with Apple | The local prototypes previously passed targeted simulator/build checks; the signed Archive installed without clearing data, and user-observed Apple authorization did not create or link a Human. No Supabase project was configured or deployed. | Historical evidence only. The prototypes and entitlement were removed before release and cannot prove or describe the current local-only build. HealthKit and other device acceptance still require a newly signed current build. |
| 2026-07-11 HealthKit physical partial and direct-source repair | Signed Human Workout path on the current physical device, followed by targeted simulator Unit/source-contract tests and incremental `-O` Release compilation | The user confirmed authorization and matching values except Exercise, Stand, and the activity rings. The former import action created a local copy but gave no visible result, prompting a correction of the source model instead of another feedback patch. The final local repair passed 10/10 targeted tests and Release compilation. | The app directly reads Exercise Time, Stand Hour, Activity Summary, and `HKWorkout`; each available goal ring remains visible, including Apple Move Time mode. HealthKit/PetWalk rows are read-only live facts, manual Ohana rows remain deletable, and legacy external copies are preserved only as fallback. A newly signed physical-device retest plus relaunch, denial, and revocation remain open under TFU-20260706-001; this is not a full HealthKit closure. |
| 2026-07-11 Plant watering-detail decomposition | Water mode/primary/trend/discovery rendering, watering schedule form, stale Plant source contracts | Plant detail experience tests passed 16/16 and long-language layout tests passed 10/10 on the iPhone 17 simulator; Debug compilation occurred in the test lane; incremental `-O` Release succeeded in 31.46 seconds and the unchanged warm rerun in 13.08 seconds; changed-file, full complexity, architecture, UI, accessibility, localization, runtime, and data-safety gates passed | `PlantCareFeatureDetailView.swift` fell from 2,065 to 1,403 lines. Pure value-driven watering render components and a binding-only schedule form now own presentation while persistence, reminder synchronization, and failure recovery remain in the parent command boundary. Five unreachable legacy render branches were removed, the 191-line function hotspot disappeared, and the production complexity baseline shrank from 97 to 96 declarations. These are compile/static/source-contract results, not a claim of manual VoiceOver or physical-device interaction proof. |
| 2026-07-11 TFU-20260711-002 closure and complexity reduction | Wallet bootstrap boundary, Home quick actions, Plant side effects, Calendar/Care/Member command payloads, Release compile surface | Wallet tests passed 20/20; Plant selections passed 9/9; the final calendar/backdate selection passed 4/4 after removing a global-registry test-order dependency; Debug and incremental `-O` Release simulator builds succeeded; the complete static release-hardening lane passed | `CoconutWalletService.swift` is 1,000 lines and back under its architecture ratchet. The production complexity baseline shrank from 120 to 97 declarations with no new/growing hotspot. Release excludes 9,747 lines of Settings DesignLab source and verifies that exclusion from the generated Swift compile list. TFU-20260711-002 is closed; CloudSync remains compiled but runtime-disabled in Solo because safely excluding it requires a later explicit module/stub boundary. |
| 2026-07-11 Oasis island-funding regression repair | D17 gift ownership, legacy gift migration, island-total injection, no-Human Pet-first UI | Targeted Unit/Integration passed 32/32; the Pet-first flow completed five injections from Lv0 to Lv1 in 1/1 UI test (41.138 seconds); a newly signed Release overlaid and launched on the current iPhone without clearing data; the user then completed the physical injection check on that overlaid Release | The island-reserve migration and aggregate injection transaction remain relevant and device-verified for that build. The no-Human Pet-first route and automatic eligibility timing are superseded; this row does not prove the current explicit claim item, Oasis lock, or Human-first journey. |
| 2026-07-11 TFU-20260710-007/008 closure | Expense command/restore invariants and Reduce Motion runtime semantics | Expense/policy/insurance/atomic-restore tests passed 20/20; the Reduce Motion Pet-first care/reward/Oasis UI path passed 1/1 on the iPhone 17 simulator | User-entered expenses now require a finite positive amount; only domain-generated, marked insurance reimbursements may persist as negative expenses. Restore rejects invalid expense facts before mutation. Reduce Motion now selects `.minimal` motion without throttling refresh/background work or disabling interaction, while Low Power and thermal constraints remain independent. Both follow-ups are closed locally; physical assistive-technology acceptance remains external. |
| 2026-07-11 TFU-20260710-006 closure | SwiftData primary-store identity, startup failure/retry UI, and risk-based test routing | Store recovery Unit/Integration tests passed 9/9; the fail-closed/retry UI path passed 1/1; the dedicated Unit and UI schemes each resolve to App plus one test target | Production no longer opens a second writable disk or memory store. Migration-plan fallback retries the same primary identity; total open failure mounts a non-writable recovery shell. TFU-20260710-006 is closed locally. |
| 2026-07-11 D17 Pet-first closure | Clean-install Pet-first journey, optional-Human compatibility, interruption/idempotency units, and relaunch-isolated timing | D17 stability passed 10/10; median 29.97 seconds, mean 30.61, range 29.03–32.93; later optional-Human UI flow and legacy Human-fixture smoke passed | Historical closure for the superseded Pet-first contract. It remains useful regression history but does not validate D17's current required-Human/optional-Pet state machine. |
| 2026-07-11 UI smoke closure | Fast release smoke, relaunch-isolated first-pet stability, and 47-failure root-cause disposition | Fast smoke passed 3/3; first-pet path passed 10/10 repetitions; all 47 historical failures map to 14 named clusters with representative reruns | Repository-local UI smoke gate is restored. The full 81-test suite was not rerun, so this is not an 81/81 claim; physical VoiceOver traversal remains true-device evidence. |
| 2026-07-10 pre-audit baseline | CI-equivalent unit scheme and static/local gates | Archived ledger records 1,509 unit tests passing, lint/audits passing, and a Release simulator build passing | Strong unit/static baseline for that commit; not proof of current uncommitted changes or device behavior |
| 2026-07-10 independent audit | Full UI suite | 8/80 passed; 68 failures shared the first-pet bootstrap/accessibility identity | Historical pre-repair evidence; it proves the old shared bootstrap failure, not that the visible first-pet entry was absent or that the current fast gate is red |
| 2026-07-10 UI gate recovery | First-pet stability plus a post-repair full UI run | First-pet smoke passed 10/10; the full run executed 81 tests, with 34 passed and 47 failed across roughly 13 root-cause clusters | Historical full-suite checkpoint, now triaged by the 2026-07-11 closure. It remains the newest full-suite run and is not a green 81/81 result. |
| 2026-07-10 privacy/document repair | OS backup exclusion, delete-all semantics, public identity, permission localization, and rules/status consolidation | Changed-file/static audits passed; 14 targeted tests passed with 0 failures; Debug simulator build succeeded | Local implementation evidence only; OS backup behavior, signed capability identity, and release-device paths still require TFU-20260709-001 |
| 2026-07-10 atomic restore repair | Strict manifest/media preflight and one SwiftData transaction without nested saves | 24 unique targeted unit regressions passed; final post-change atomic/plant run passed 7/7; release-data-safety audit and Debug simulator build passed | Repository P0 is closed locally; process termination, low-storage, and signed-device behavior remain separate device evidence |

## Validation Ladder

1. `git diff --check` and syntax/manifest checks.
2. `scripts/dev-check-changed.sh` for the changed-file recommendation.
3. Risk-specific audits and targeted unit tests.
4. `scripts/build-debug-fast.sh` for compiler/runtime/persistence/privacy changes.
5. Full module/release gates only at an appropriate handoff.
6. Physical-device, signed capability, backup/restore, energy, location, Health,
   notification, and accessibility checks remain separate evidence.

## Phase Overview

| Area | Status | Current note |
| --- | --- | --- |
| Product and documentation authority | 🟡 | D17/D26/D27/D28 define Human-first onboarding, one Task Center, multi-object care, explicit starter-gift claim, and the household starter plan; its repository/Simulator proof closed on 2026-07-18. D4/D6/D9/D22/G12 approve Free / Personal 1.0 with Family/Care+ deferred; quota/catalog implementation and release proof remain under TFU-20260715-003. |
| Privacy and delete-all contract | 🟡 | OS backup exclusion and local Reset fencing are locally verified. Personal purchase state and Free quota counts remain on-device with no developer purchase backend or care-data transfer; ads and analytics remain absent. Signed-device backup exclusion, iCloud Drive cleanup failure/retry, final StoreKit implementation proof, and App Store privacy answers remain external evidence. |
| StoreKit / Free / Personal | 🟡 | Current source implements three Personal products, StoreKit-derived yearly trial eligibility, legacy Supporter grandfathering, Free quotas, downgrade protection, paid capability gates, and the final paywall. The current App + UI target compiles, `.build/TestResults/PersonalFocused.xcresult` reports 67/67 focused Unit tests passing, free space is about 44 GiB, and no `* 2.swift` / `* 3.swift` conflict copies remain. Complete nine-language commerce copy plus signed Storefront/Sandbox/second-device subscription, Lifetime, restore, expiration, refund/revocation, and delivery evidence remain open under TFU-20260715-003. |
| Backup restore | 🟢 | Strict bounded preflight rejects malformed identities, dates, duplicates, required relations, and media; one SwiftData transaction rolls back every injected phase/save/cancellation failure before publishing defaults or notifications. |
| Unit/static baseline | 🟡 | Focused Human-first/D28 Unit/Integration passed 97/97; the final repair batch passed 57/57 focused tests and 15/15 after its last isolation annotation; the focused Personal artifact reports 67/67. Earlier 191/191 and 499/499 checkpoints predate the broad Human-first/D28/Personal worktree, so these selections cannot be promoted to a current full-Unit claim. The complete final-source release-static lane is green. |
| UI automation | 🟡 | The frozen exact App + UI checkpoint has 25 selected formal selectors green. UI-test-only assertion and route revisions followed; across compatible provenances, all 119/119 distinct formal selectors now have trusted green evidence against unchanged App source. Coverage includes immediate/deferred onboarding, skip/back/cancel/reopen, privacy/unknown/not-applicable, supersession, nested editors, care and economy projection, deletion/Memorial safeguards, Calendar CRUD/filter/keyboard/title routing, direct care and plan readback, the expanded Plant module, Personal gating, Settings responsiveness and observability, launch smoke/performance, relaunch, Reduce Motion, reward idempotency, synthetic relaunch-persistent Cat/Dog long sessions on `iPhone 17 Tests`, and an existing-user no-reset journey. The latest rebuilt provenance has 3/3 distinct executed selectors green. Status remains yellow because this is cumulative campaign evidence rather than a single-provenance 119-selector replay, the focused Unit lane is green but the full Unit, static, signed-device, and Storefront lanes remain separate, and two passing Walk runs reproducibly report a non-failing CoreLocation main-thread runtime warning at live route startup. |
| Domain data validation | 🟢 | User-entered expense commands reject zero, negative, NaN, and infinity; domain-generated marked insurance reimbursements remain valid negative facts; backup preflight and rehydrate enforce the same persisted-value contract before mutation. |
| Store recovery identity | 🟢 | All automatic production opens target one primary identity; migration/default-open failure, disk-full recovery, repeated operation, relaunch persistence, fail-closed startup, and retry are covered locally. |
| Accessibility/runtime policy | 🟡 | Reduce Motion is locally separated from Low Power/thermal work throttling and the core interactive path passes; VoiceOver, Voice Control, Switch Control, and signed-device motion/touch acceptance remain external. |
| Device/OS product scope | 🟢 | D24 approves iPhone-only / iOS 26.2+; all target configurations and Debug/Release simulator products report device family `[1]`, with native iPad/watchOS deferred. |
| Architecture/CI gate | 🟢 | The final canonical `scripts/release-hardening-check.sh --static-only` run passed from start to finish. Task Center complexity now has no new/growing hotspot; V4 UI and accessibility both pass across 1,044 files; resource integrity has one advisory-only `Assets.xcassets` review warning. |
| Simulator manual coverage | 🟡 | Broad historical coverage exists; remaining depth is non-blocking P2. |
| Physical-device acceptance | 🟡 | The newest development-signed Release Archive predates the Human-first/D28 and all current Personal work. Its capability inspection and preserved-data Oasis injection remain historical evidence, while a new signed build must re-prove onboarding/claim and real Storefront IAP after local validation. HealthKit, App Store distribution/storefront, Sandbox purchase/restore/refund, minimum-device smoke, notification, Memorial, backup, location, energy, and accessibility checks remain open. |
| CloudKit/Family | ⚪ | Future-only; the local-only capability profile remains closed. |

## Active Module Pointers

| Surface | Follow-up | Required evidence |
| --- | --- | --- |
| Free / Personal 1.0 | TFU-20260715-003 | Free quota/domain-command enforcement and logical-plan deduplication; Personal monthly/yearly/Lifetime plus legacy Supporter grandfather; trial, downgrade, over-quota data protection, paid capability mapping, StoreKit lifecycle, nine-language/accessibility, App Store Connect, Sandbox, second-device, and signed Storefront evidence |
| Future CloudKit live apply | TFU-20260614-014 | Two-device shared-zone conflict/delete proof after capability enablement |
| Memorial | TFU-20260612-017 | Real-device UI and notification acceptance |
| Notifications | TFU-20260612-016 | Real-device delivery/action/privacy acceptance |
| Human Workout | TFU-20260706-001 | Permission passed by user observation; real summary/per-ring/direct-workout-read/relaunch/denial/revocation evidence remains |
| Solo signed Release/public identity | TFU-20260709-001 | R0-R6, iPhone-only signed local-only Archive/capability inspection, minimum-hardware smoke, verified App Store Apple ID/storefront, OS backup exclusion, and energy/location paths |
| Pet GUI depth | TFU-20260629-004 | Narrow regression-driven negative/stale-route depth plus attribution of the reproducible live-Walk CoreLocation main-thread runtime warning |
| Long-language layouts | TFU-20260612-020 | German/max-Dynamic-Type dense-screen sweep |
| Concurrency/global lifecycle | TFU-20260710-010 | Incremental strict-concurrency, task cancellation, and DI proof |
| Maintenance/Home invalidation | TFU-20260710-011 | Bounded work and measured scoped-invalidation migration |

## Recent Validation Snapshots

These are dated passing snapshots. They remain useful for unchanged boundaries,
but none validates the complete current Human-first/D28 plus Free / Personal
worktree or the new Storefront catalog.

| Date | Scope | Result |
| --- | --- | --- |
| 2026-07-18 | Human-first/D28 final repository and Dogfood acceptance | The final repair batch passed 57/57 focused tests and then 15/15 on the warning-clean service source; the complete static lane passed; optimized fast Release and WMO Dogfood Release builds succeeded. The protected existing user retained its sealed store and normal UI resumed the guided pet-profile task at step 2 after choosing `Not sure yet` and relaunching, with balance 162 and no test artifacts. This closes TFU-20260715-002 locally; signed-device evidence remains separate. |
| 2026-07-18 | Focused current Human-first/D28 Unit/Integration | Nine selected suites executed 97/97 tests with no failures or skips on guarded `iPhone 17 Tests`; xcresult `.build/DerivedData/tests/Logs/Test/Test-OhanaUnitTests-2026.07.17_23-58-13-+0200.xcresult`. This is focused current-source evidence, not a full Unit-suite claim. |
| 2026-07-18 | Optimized unsigned Release compiler lane | `scripts/build-release-fast.sh` succeeded for arm64 + x86_64 at `-O`, verified the Release compile list excludes Settings DesignLab, and produced `.build/DerivedData/release/Build/Products/Release-iphonesimulator/Ohana.app`. This is compiler evidence only; the separate current static gate remains red and the product is unsigned. |
| 2026-07-14 | Shared-litter one-shot undo and V90 recovery closure | Added a six-second global undo window that persists one `SharedCareUndoReceipt` with the shared session and per-cat coverage facts before emitting reward, Reminder/Event completion, plan/settings, notifications, or Oasis feedback. Undo removes the whole pending operation once; expiry performs stable-key core settlement, freezes the original Reminder occurrence, retries failed external effects, restores every pending receipt after relaunch, and schedules the earliest outstanding deadline. V90 registers and migrates the receipt model and extends Reset, backup, and physical-deletion coverage. Plant and family reports now distinguish reality-operation count from object-coverage count. The complete App and test targets passed `build-for-testing`. In `.build/TestResults/2026-07-14/unified-task-v90-final.xcresult`, 190/191 tests passed and the only failure was a source-string assertion that still required a helper to live inside the main View type; after making that assertion extension-safe, `.build/TestResults/2026-07-14/plant-batch-layout-rerun.xcresult` passed the complete 7/7 layout suite. Thus all 191 selected behaviors are green after the final product-code refactor. The scoped changed-file lane passes formatting, status, UI V4, accessibility, complexity, localization, smoothness, route, runtime, shared-care metadata, economy, lifecycle, derived-state, and release-data-safety audits. No visual Simulator, notification-delivery, relaunch UI, or physical-device acceptance claim is made. This closes TFU-20260714-001 at the repository product-contract level; remaining release P1 work is external/manual or deferred CloudKit. |
| 2026-07-14 | Unified Task Center, typed care creation, multi-object care, and aggregate reporting | Implemented the fixed Home → Tasks → Oasis → Plants order; Event/Reminder/FamilyTask union and deduplication; one Task action executor across Tasks, Calendar and Today Focus; Human/Pet/Plant/household subjects; member-filtered handoff; Solo-noise suppression; optional rewards; typed Pet/Plant creation with one Event/Reminder/optional FamilyTask save boundary; V89 separation of notification delivery from task occurrence; Plant all-or-nothing batch/one undo/operation-versus-coverage metrics; one shared pet session with per-target coverage; and Household Insights routing. The complete App and Unit targets passed `build-for-testing`; the final plan-scoped result bundle `.build/TestResults/2026-07-14/unified-task-final-v89-rerun.xcresult` passed 499/499 tests with no failures or skips. The source passed syntax, SwiftFormat, `git diff --check`, doc status, UI V4, accessibility, complexity, localization, smoothness, runtime, economy, lifecycle, derived-state, shared-care metadata and release-data-safety checks through the plan-scoped changed-file lane. No visual Simulator or physical-device acceptance claim was made. At that checkpoint shared-litter UI undo was still hidden; the V90 closure row above supersedes that limitation. |
| 2026-07-12 | Local-only account decision and future boundary | Removed the unshipped Apple/Supabase account implementations, dependency/configuration, Sign in with Apple entitlement, Settings/Reset hooks, tests, backend prototype, and account data declarations. Restored the product/privacy/onboarding/release truth to local SwiftData plus restricted user-owned iCloud Drive backup. Added `docs/planning/account-backend-extension.md` as planning-only future guidance and fixed the future UX contract: optional Apple login, clear local continuation, explicit one-tap Human creation, and equal quest completion for optional-data disclosure or skip. `xcodebuild -list` resolved the current targets and schemes. `scripts/dev-check-changed.sh`, `git diff --check`, doc-status/governance/agent-skill audits, plist/entitlement/privacy-manifest syntax, and explicit no-account-reference checks passed. On the iPhone 17 simulator, `HumanHealthActivityResolverTests`, `HumanHealthKitManagerLoadingTests`, and `HumanWorkoutSourceMergePolicyTests` passed 10/10 in 3 suites while recompiling/linking the complete App and Unit targets; xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUnitTests-2026.07.12_17-56-22-+0200.xcresult`. The current signed Release evidence is recorded in the reconciliation row above. A real iCloud Drive failure/retry run and user-observed HealthKit acceptance remain pending. |
| 2026-07-12 | Superseded Apple identity and account prototype | Historical only: local identity tests, an account prototype build, and a development-signed Archive with Sign in with Apple existed briefly. No account backend was deployed, no production credentials were added, and those artifacts no longer match the target's current entitlements or UI. They cannot be used for RC acceptance; only the current local-only build may satisfy R0-R6. |
| 2026-07-11 | HealthKit physical partial and direct-source repair | The user confirmed successful authorization and matching displayed Health values except Exercise, Stand, and the concentric activity rings. The former import action persisted a copy—the control changed to delete—but supplied no visible result. Static tracing found both missing reads and the larger semantic error: a directly readable HealthKit workout was being presented as something the user had to import into Ohana. The final repair directly queries Exercise Time, Stand Hour, Activity Summary, and `HKWorkout`; supports active-energy and Apple Move Time goals; renders every available ring independently; labels unreadable/no-data state without claiming confirmed read access; and presents HealthKit/PetWalk rows as read-only live facts. Manual Ohana workout rows remain deletable. Legacy external-source copies are not deleted and appear only when the corresponding live source is unavailable. Resolver, per-ring, source-contract, fallback, and duplicate-suppression tests passed 10/10; xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUnitTests-2026.07.11_22-18-55-+0200.xcresult`. Strict targeted SwiftLint, formatting, UI, accessibility, changed-code complexity, status-ledger, `git diff --check`, and incremental `-O` Release compilation passed. A new signed overlay and real-data retest remain required; no simulator evidence is being substituted for HealthKit authorization, values, relaunch, denial, or revocation. |
| 2026-07-11 | Plant watering-detail decomposition and stale-test repair | Removed five unreachable legacy water render branches; extracted value-driven mode, primary-task, chart, discovery, and schedule-form components without moving persistence or reminder side effects into render code. `PlantCareFeatureDetailView.swift` fell from 2,065 to 1,403 lines; the 191-line schedule function hotspot was removed; full production complexity now passes across 947 files with 96 grandfathered declarations. Three pre-existing source-string assertions were updated to the already-refactored Plant request facade and current Plant feature-collection UI path instead of restoring deleted APIs. Plant detail experience passed 16/16 and long-language layout passed 10/10; changed-file, architecture, V4 UI, accessibility, and Release checks passed. Incremental Release took 31.46 seconds after the source change and 13.08 seconds unchanged/warm. |
| 2026-07-11 | TFU-20260711-002 architecture and complexity closure | Wallet bootstrap moved to `CoconutEconomyBootstrapService`; Home status/render work and Plant schedule side effects gained narrower boundaries; Calendar/Care/Member long-parameter write APIs now use compact request/value payloads. The complexity fixture self-tests passed, the all-source audit scanned 945 production files and passed with 97 grandfathered declarations, and full architecture passed. The complete 44.19-second static release-hardening lane passed all 964-file runtime, economy, lifecycle, data-safety, UI, accessibility, smoothness, and route audits. Wallet tests passed 20/20, Plant selections 9/9, and the final deterministic Calendar/backdate selection 4/4. Debug and incremental `-O` Release builds passed; the final unchanged warm Release took 14.08 seconds and compile-list verification confirms Settings DesignLab is excluded. TFU-20260711-002 is closed. |
| 2026-07-11 | Rule and validation-lane simplification | `AGENTS.md` was reduced from 520 to 271 lines while retaining authority, product/data invariants, authorization, and risk routing. `dev-check-changed.sh` is read-only by default and refuses unscoped `--fix-format`; module exit no longer reruns changed audits; release hardening now aligns its 17 static audit families with CI and adds full Unit/optional UI lanes. Validation routing tests, governance/skill checks, data-safety bad/good fixtures, and the complete 959-file audit fixture suite passed. At that checkpoint the canonical release static run stopped at the wallet oversized ratchet; the TFU-20260711-002 closure row supersedes that blocker. |
| 2026-07-11 | Oasis island reserve and aggregate injection repair | A current-device R0 smoke with one Pet, no Human, and 59🥥 reproduced an injection that only vibrated. Static tracing found that the old D17 gift was member-owned while the injection command required a current Human. New gifts now credit `system:island`; an idempotent legacy transfer preserves the total while moving the still-available old gift amount out of the member wallet; tree injection atomically spends reserve then active member wallets and excludes `system:legacy`. Targeted Unit/Integration passed 32/32 with xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUnitTests-2026.07.11_14-25-42-+0200.xcresult`. The no-Human Pet-first UI path performed five successful injections to Lv1, 1/1 in 41.138 seconds, xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUITests-2026.07.11_14-30-00-+0200.xcresult`. An incremental `-O` Release device build succeeded at `/tmp/OhanaDeviceReleaseFast-20260711`, passed strict codesign verification, overlaid without uninstalling, and launched on the current iPhone. A read-only store copy then showed `system:island=50`, Pet=9, `system:legacy=0` plus the paired migration transfer and marker. The user subsequently performed the physical injection on that overlaid Release and confirmed the expected coconut deduction and energy increase; this device regression is closed. |
| 2026-07-11 | Release build-time routing | The 959-file / 313,518-line App target made every fresh Release Archive run one full WMO frontend; the signed baseline took about 8 minutes 47 seconds, and reusing its DerivedData still started another complete WMO compile. `scripts/build-release-fast.sh` now provides an explicit `-O` incremental iPhone 17 simulator lane with an external stable cache, while `scripts/archive-release-local.sh` preserves `-O` WMO for signed RC/device gates, rejects File Provider-backed output, disables the non-product index store, and verifies signing/xattrs without export or upload. The simulator fast lane succeeded from a cold cache in 178.14 seconds and from the warm cache in 13.22 seconds; an additional warm run before making `-O` explicit succeeded in 18.77 seconds. The same incremental `-O` strategy was used for the signed current-device repair package; after its cold device cache was established, an unchanged signed rebuild succeeded in about 15.7 seconds and still passed strict codesign verification. The Archive tool's read-only `--verify` path passed against the signed R0 Archive, confirming code-sign validity, iPhone-only family `[1]`, and iOS 26.2 minimum without triggering another WMO build. Shell syntax, invalid-mode fail-fast, repository-output rejection, and `git diff --check` passed. The final distribution Archive remains WMO by design; this optimization reduces its frequency rather than claiming WMO itself became incremental. |
| 2026-07-11 | TFU-20260709-001 R0 signed Release partial | A Release 1.0 (1) Archive from commit `eece7d642` plus the current dirty worktree succeeded when DerivedData and the archive were placed outside the File Provider-managed project directory. Strict `codesign` verification and Xcode store validation passed. The arm64 product reports `UIDeviceFamily = [1]` and iOS 26.2 minimum, contains no app extension/watchOS content, and signs HealthKit plus CloudDocuments without CloudKit, APNs, or App Group. The development profile includes the connected iPhone 17 Pro Max; installation and launch succeeded on iOS 26.5.2 (23F84), and the process remained present after launch. The public privacy-policy URL is anonymously reachable. The repaired 59🥥 no-Human Oasis path passed its newly signed overlay retest by user observation. R0 remains open because this machine has only Apple Development signing and App Store Connect/storefront plus minimum-device, backup, energy/location, permission-dialog, and iCloud Drive failure proof are absent. Two earlier `.build` Archive attempts failed when File Provider reattached `com.apple.FinderInfo` after the xattr build phase; the external build location removed that signing blocker without changing app code. |
| 2026-07-11 | TFU-20260711-001 architecture-gate closure | Closed locally. AppState and plant-reminder preference backup mapping moved without behavior changes into `DataBackupManager+AppState.swift`; `DataBackupManager.swift` fell from 1,274 to 1,103 lines without adding an oversized-file baseline. Paid-consumable AppState, plant-reminder preference/rebuild, and repeated idempotent restore tests passed 3/3 under `OhanaUnitTests`; artifact `.build/TestResults/2026-07-11/tfu-20260711-001-backup-app-state.xcresult`. Strict full architecture, release-data-safety, audit fixtures, runtime, localization, governance, resources, full UI/accessibility/smoothness/route static gates, gitleaks, git-size, and the complete `scripts/release-hardening-check.sh --skip-build` passed. The test run compiled the changed App and Unit targets; the release-hardening command intentionally skipped a duplicate build. |
| 2026-07-11 | Post-device-matrix full release-hardening checkpoint | Historical checkpoint superseded by the TFU-20260711-001 closure row. Debug/Release builds, device-family product inspection, governance, status, changed-file and audit-fixture gates passed, but the first complete no-build release-hardening attempt stopped at the then-oversized `DataBackupManager.swift`; later sections were unexecuted in that attempt and were subsequently run successfully during closure. |
| 2026-07-11 | TFU-20260710-009 supported-device decision and repository configuration | Closed locally and remaining physical proof merged into TFU-20260709-001 R0. Product D24 now fixes first release at iPhone-only / iOS 26.2+, deferring native iPad and watchOS. All nine App/Unit/UI target configurations use device family `1`; Debug and Release `iPhone 17` simulator builds succeeded, and both built products report `UIDeviceFamily = [1]` plus `MinimumOSVersion = 26.2`. A machine-readable release-device manifest and governance audit reject family/deployment/watch-target drift. Signed Archive, App Store Connect/storefront, paired-Watch notification forwarding, and smallest physical iPhone smoke remain external R0/GAP-6 evidence rather than being falsely marked complete. |
| 2026-07-11 | TFU-20260710-007/008 expense and Reduce Motion hardening | Closed locally. User, shared, and Human expense commands reject non-positive or non-finite amounts before persistence and expose localized retryable errors; backup preflight and rehydrate apply the same rule. The sole negative exception is a finite insurance reimbursement produced by the insurance domain command with the audited category/marker. Reduce Motion now maps to `.minimal`: spatial/repeating motion stops, essential state feedback and interaction remain available, and refresh/background maintenance are not treated as Low Power work. AppWorkloadPolicy passed 6/6; expense plus insurance passed 7/7; atomic restore passed 7/7; the Reduce Motion Pet-first care/reward/Oasis path passed 1/1 in 34.623 seconds on iPhone 17. Debug simulator build succeeded. Artifacts: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.11_11-30-42-+0200.xcresult`, `Test-Ohana-2026.07.11_11-45-31-+0200.xcresult`, and `Test-Ohana-2026.07.11_11-33-46-+0200.xcresult`. The full Unit and 84-test UI suites were not rerun; physical assistive-technology and energy acceptance remain open. |
| 2026-07-11 | TFU-20260710-006 primary-store identity and test-lane repair | Closed locally. Every automatic production attempt now uses the same primary SwiftData store identity: first with the migration plan, then without it. If both opens fail, startup mounts a non-writable recovery shell with Retry and Support instead of silently creating a second disk store or an in-memory writable session. Fault-injected failure, recovery/retry, repeated disk-full behavior, and real cross-launch persistence tests passed 9/9 under `OhanaUnitTests`; the startup fail-closed/retry user path passed 1/1 under `OhanaUITests`. UI shard completeness passed with 83 tests assigned exactly once. Final artifacts: `.build/TestResults/2026-07-11/tfu-006-store-identity-unit-postformat.xcresult` and `.build/TestResults/2026-07-11/tfu-006-store-recovery-ui-final.xcresult`. The full Unit and 83-test UI suites were not rerun because this closure uses risk-targeted lanes. |
| 2026-07-11 | TFU-20260710-005 D17 Pet-first implementation | Closed locally. Onboarding requires the first Pet, then opens the Pet quick-weight entry for one persisted care fact, grants the deterministic one-time starter gift to the unowned `system:island` reserve, and exposes Oasis Lv0 without requiring or silently creating a Human. The formal island total combines that reserve with all active member wallets and can fund tree injection; member-specific balances remain distinct. Human name is required only when a Human is later added; gender and birthday default to absent. The original targeted onboarding, member-lifecycle, reset, route and 10/10 clean-install stability evidence remains valid; the 2026-07-11 ownership repair additionally passed 32 targeted Unit/Integration tests and a 1/1 five-injection no-Human UI path. The preserved-data injection retest also passed on the overlaid physical-device Release by user observation. Primary original artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.11_10-03-07-+0200.xcresult`; ownership-repair artifacts are recorded above. Broader signed-device touch latency, VoiceOver traversal, battery/thermal energy, and permission dialogs remain physical-device work. |
| 2026-07-11 | TFU-20260710-004 UI smoke closure | Closed for the repository-local gate. The current fast release smoke passed 3/3, covering first-pet creation without and with production overlays plus the first-pet accessibility action contract. The first-pet path then passed 10/10 repetitions with App relaunch enabled; the xcresult reports ten passed repetitions with an average duration of 39 seconds. The historical 47 failures are individually mapped into 14 root-cause clusters, with representative Human, Pet, Calendar, walk, litter, health, hygiene, feed, Plant, Settings, Daily Streak, profile, and quick-menu reruns recorded in `docs/audits/2026-07-11/UI_SMOKE_GATE_CLOSURE.md`. A confirmed Settings `LazyVStack` layout loop was removed; Plant reminder and bulk-edit routes passed afterward. Artifacts: `.build/TestResults/2026-07-11/ui-release-smoke-v3.xcresult`, `.build/TestResults/2026-07-11/first-pet-stability-10x.xcresult`, and the focused bundles listed in the closure report. The full 81-test suite was not rerun, so no 81/81 claim is made. Physical VoiceOver traversal remains true-device acceptance. |
| 2026-07-10 | TFU-20260710-004 UI smoke recovery | Historical checkpoint, superseded by the 2026-07-11 closure row. First-pet smoke passed 10 consecutive runs. The next full UI run executed 81 tests: 34 passed and 47 failed, clustered into roughly 13 root causes rather than one shared bootstrap failure. Recording inspection confirmed Daily Streak visibly opened, so that failure was a stale selector/assertion rather than a route failure; its revised selector had not yet been rerun at this checkpoint. The Calendar flow progressed past the former duplicate `Done` ambiguity, but its long CRUD rerun was interrupted after exceeding the short validation budget and was not counted as pass/fail. Three targeted route/rail policy unit tests passed; one selector was initially mistyped and executed zero tests, then was corrected and passed in a separate run. VoiceOver order was unverified at this checkpoint because the available runtime inspection exposed no usable accessibility tree. Full-suite artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_21-35-04-+0200.xcresult`; 10-run first-pet artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_18-54-32-+0200.xcresult`; policy artifacts: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_23-58-26-+0200.xcresult` and `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_23-59-40-+0200.xcresult`. |
| 2026-07-10 | TFU-20260710-003 strict and atomic backup restore | Closed locally. Required versions, identities, dates, duplicates, relations, media metadata/content, and bounded sizes are validated before live mutation. Restore now requires a clean context and uses one SwiftData transaction with autosave disabled and no nested saves; defaults, notifications, projections, and plant reminder side effects publish only after commit. Fault injection at all six phases, transaction-save failure, cancellation, malformed/oversized/tampered input, repeated idempotent restore, localization, and 17 existing backup compatibility regressions passed 24/24 unique targeted tests. The final post-overlay atomic/plant run passed 7/7. `git diff --check`, `scripts/dev-check-changed.sh`, release-data-safety, and `scripts/build-debug-fast.sh` passed. Artifacts: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_16-58-14-+0200.xcresult`, `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_17-03-22-+0200.xcresult`, and `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_17-08-37-+0200.xcresult`. Process termination, low-storage, and signed-device behavior are not simulator-proven and remain part of device/reliability acceptance rather than this closed repository P0. |
| 2026-07-10 | TFU-20260710-002 Human Note attachment lifecycle | Closed locally. Note deletion now removes only post-commit unreferenced files; Human deletion clears owned/orphan files while preserving surviving shared references; Reset removes the entire managed root only after the SwiftData commit. Injected-save rollback, repeated-delete, shared-reference, Note/Human/Reset, original command, and automatic-backup/Reset regressions passed 18/18 with 0 failures. `scripts/dev-check-changed.sh`, release-data-safety, UI V4, accessibility, localization, smoothness, route-first-frame, runtime, lifecycle, and status-ledger gates passed; `scripts/build-debug-fast.sh` succeeded. Artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_16-33-30-+0200.xcresult`. Filesystem behavior is simulator-local; signed-device backup inspection remains TFU-20260709-001. |
| 2026-07-10 | TFU-20260710-001 automatic backup + Delete-All Reset | Closed locally. One shared service now owns the run task, generation checkpoints and managed-file cleanup; Settings awaits the coordinated Reset. Deterministic non-cooperative-export, in-flight-write, cleanup-failure and new-generation tests passed. The combined targeted run passed 25 tests with 0 failures; `scripts/dev-check-changed.sh`, release-data-safety, architecture and runtime audits passed; `scripts/build-debug-fast.sh` succeeded. Artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_16-04-06-+0200.xcresult`. Real iCloud Drive timing remains part of TFU-20260709-001, not this repository P0. |
| 2026-07-10 | Current documentation/privacy repair | `git diff --check`, manifest/plist syntax, status-ledger, governance, agent/skill, release-data-safety, strict localization, audit-fixture, and `scripts/dev-check-changed.sh` gates passed. `LocalBackupExclusionPolicyTests`, `AppResetServiceTests`, and `SettingsRouteContainerTests` passed 14/14 with 0 failures. `scripts/build-debug-fast.sh` succeeded for the iPhone 17 simulator. Test artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_15-25-45-+0200.xcresult`. |
| 2026-07-10 | Independent Phase A-D audit | Confirmed 3 release-blocking code findings, one broken UI gate, and the D17 implementation conflict; generated the dated package under `docs/audits/2026-07-10/`. |
| 2026-07-10 | Pre-audit unit/static baseline | See the archived pre-audit ledger for exact commands and artifact paths; do not reuse it as proof for this worktree. |

## Update Rules

- This file answers only: current release gate, current phase, latest high-signal
  validation, and where the active work lives.
- Open work belongs in `docs/task-follow-ups.md`; this file points to it.
- Keep detailed command output and old snapshots in dated audit/archive files.
- Never turn a static/build result into a runtime, device, cloud, energy, or App
  Store claim.
- Update this dashboard only after durable status changes, then run
  `scripts/audit-doc-status-ledgers.sh`.
