# 测试推进总账（跨会话唯一进度源）

> 工作协议见 `docs/ai-module-test-playbook.md`。任何会话开工前先读本文件；收工前**必须**更新本文件对应行，否则视为未完成。
> 状态图例：⬜ 未开始 ｜ 🔵 分析完成待修 ｜ 🟡 修复中 ｜ 🟢 已过门禁并提交 ｜ 🟢\* 已过门禁但带人工验收债（见 track list） ｜ 🏁 对抗复审通过（成熟） ｜ ⛔ 阻塞（备注写明阻塞原因）
> **首发门槛（2026-06-15 立，取代"逐模块 🏁 才能发"）**：首发唯一停止条件是**全局「P0 且首发可达」清零**（崩溃/数据丢失/隐私/核心流程断/经济算错，且单机核心流程会触发）。其余一切（P1/P2、首发不可达、边角）登记进 `docs/task-follow-ups.md`（上线后修复 backlog），带着上线。详见手册「发布门槛（Release Bar）」。
> **🏁 已降级**为上线后长期成熟度目标，**不再是首发/Phase 8 阻塞**。原"核心五模块必须 🏁 才能进 Phase 8"作废——它把"复审零发现"这个无限过程当终点，是复审-修复怪圈的制度根源。审计（member-gate/economy/derived-state）继续跑以防新债反弹。

## 阶段总览

| Phase | 范围 | 状态 | 备注 |
|---|---|---|---|
| 0 | 基线收尾（提交未完成改动） | 🟢 | 已于 2026-06-12 提交（`ff7ac89f`）；后续模块工作可能使当前工作区非空 |
| 1 | `Ohana/Models` | 🟢 | 门禁通过并提交：`db44afe1`；P0 余留跨范围项 TFU-20260612-012 已由 Domain gate commit `304971af` 关闭，TFU-20260612-013 已由 `e16e6953` 关闭 |
| 2 | `Ohana/Domain` | 🟢 | 门禁通过并提交：`304971af`；2026-06-15 Feeding plan/stock P1 与 TFU-20260615-002 已本地修复，`scripts/module-exit-gate.sh` PASS；关闭仍等 push/CI 与全新纯复审 |
| 3 | `Ohana/Shared` | 🟢 | 门禁通过并提交：`fcf998088`；P1 余留见 TFU-20260612-015 |
| 4 | `Ohana/App` | 🟢 | 门禁通过并提交：`e48c13af7`；无 P1/P2 余留 |
| 5 | Home + TodayFocus + QuickCare | 🟢 | 门禁通过并提交：`b8e8710e`；TFU-20260612-015 已关闭，无 P1/P2 余留 |
| 6 | 大模块（Feeding/Members/Oasis/Settings/Health/Economy） | 🟢 | Feeding 门禁通过并提交：`b49134977`；Members 门禁通过并提交：`ead1e5fe4`；Oasis 门禁通过并提交：`87423afd8`；Settings/Health 门禁通过并提交：`5d4e71928`；Economy 原门禁：`662852a01`，复审修复轮门禁：`1679ddd66` |
| 6.5 | 宪法差距建设（联机门/删除模型/自动备份/植物门） | 🟢* | GAP-1、GAP-3~9 与 GAP-12 已过门禁并提交；GAP-2 用户可见回收站按 2026-06-14 产品决策退役，删除模型改为不可恢复物理删除 + 不可见 sync tombstone；*带验收债：人工/真机验收项见 `docs/planning/gap-acceptance-track-list.md`，必须在 🏁 复审与 Phase 9B 前清完 |
| 7 | 中小模块批量 | 🟢* | 2026-06-16 按双轨口径批量重走并更新模块明细：高风险模块以全仓 gate + targeted suites/audits 作为自动证据，低风险/功能门后模块以编译、路由/门禁、全仓 audits 作轻量证据；`scripts/module-exit-gate.sh --full` PASS（933 unit + 3 UI），latest CI run `27607807044` PASS。不是纯复审，不授 🏁；真实 UI/真机/专项人工验收仍并入 Phase 8/9 债。 |
| 8 | **首发门槛扫描 + 横向回归** | 🟢* | 2026-06-16 总览会话按 Release Bar 重走：`docs/task-follow-ups.md` 中 Open P0+首发可达=0；唯一旧 Open P0（TFU-20260612-006 CareLedger read-model migration）按当前门槛重判为 P1 backlog，不挡首发。自动横向回归证据：`scripts/module-exit-gate.sh --full` PASS（933 unit + 3 UI）、`scripts/release-hardening-check.sh --skip-build` PASS、latest CI run `27607807044` PASS。`*` 表示仍需 Phase 9 真机/RC 端到端人工验收，不代表 🏁 成熟态。 |
| 8.5 | 演进就绪审查（联网/订阅/账户地基） | ⏭️ 1.x | 首发不可达（CloudSync `.none`/联机/订阅未启用），**推 1.x，不挡首发**。地基保留 |
| 9 | 上架工程（9A 前置/9B RC/9C 提审上线后） | 🟡 | 2026-06-16 已启动 9A：Apple Developer Team `567SK733UN` 可用于本地签名；`Ohana Local Device` 真机构建、安装、启动均通过（bundle id `com.guanchen.li.Ohana.LocalDevice`，设备 `Guanchen’s iPhone` iOS 27.0）。Dogfooding 入口已打开；下一步按 `docs/planning/gap-acceptance-track-list.md` 燃尽 101 项真机/真实 UI/通知/定位/App Icon/长语言验收债，并准备 9B RC。 |

> **首发路径（2026-06-15 塌缩）**：旧路径"Phase 7 逐模块×N 轮 → 🏁 → Phase 8 → Phase 9"已被发布门槛框架压成 **首发门槛扫描（Phase 8，吸收 Phase 7 剩余）→ 修 P0+可达 → Phase 9（真机/RC/提审）**。Phase 8.5 与所有 🏁/P1/P2/不可达项推上线后。

## 快速测试协议（Domain/Economy Kernel）

> 目标：停止把人工深复审当主测试系统。人工复审只负责发现新 bug 形状；一旦发现，必须固化为 audit / fixture / targeted test。日常修复批次优先跑快速 lane，phase 结束才跑完整门禁与 CI。

| Lane | 命令 | 触发时机 | 状态 | 备注 |
|---|---|---|---|---|
| K0 | `scripts/domain-kernel-fast-gate.sh --audit-only` | 每个 domain/economy 修复批次后；只读复审前 | 🏁 | 2026-06-16 验证通过：critical audits + audit fixtures，约 10s；`derived-state lifecycle` 仍打印既有 checklist warnings 但退出码为 0 |
| K1 | `scripts/domain-kernel-fast-gate.sh --changed` | 代码改动后、交接前；允许 SwiftFormat | 🏁 | 2026-06-16 验证通过：changed gate + critical audits + fixtures，约 11s；会调用 `scripts/dev-check-changed.sh` |
| K2 | `scripts/domain-kernel-fast-gate.sh --tests` | P0/P1 修复批次完成、需要行为信心时 | 🏁 | 2026-06-16 验证通过：449 tests / 12 suites，约 37s；首跑红转绿记录见本节日志 |
| K3 | `scripts/domain-kernel-fast-gate.sh --build` | Swift API / compiler-surface 改动后 | 🏁 | 2026-06-16 验证通过：`iPhone 17` simulator Debug build，约 26s；只在编译面变化时加 |
| K4 | `scripts/module-exit-gate.sh [--full]` + phase-end CI | phase 结束、commit/push 前后 | ⬜ | 仍是最终门禁；CI 不再作为心跳或每个小 patch 的验证方式 |

复审停止规则：K0/K1/K2/K3 中与本批风险匹配的 lane 通过，且新增 P0/P1 形状已固化进 fixture 或 targeted test 后，本轮复审停止；P2 进入 `docs/task-follow-ups.md` 或模块备注，不继续拖住当前修复轮。

本节日志：
- 2026-06-16：新增 `scripts/domain-kernel-fast-gate.sh`。验证：`bash -n scripts/domain-kernel-fast-gate.sh` PASS；`scripts/domain-kernel-fast-gate.sh --tests --build --dry-run` PASS；`scripts/domain-kernel-fast-gate.sh --audit-only` PASS（architecture、economy --all、member lifecycle --all、derived-state --all、audit fixture self-tests）。
- 2026-06-16：K1 changed lane 验证通过：`scripts/domain-kernel-fast-gate.sh --changed` PASS；同批次 root-cause 修复后复跑仍 PASS。`derived-state lifecycle` 仍只打印既有 checklist warnings，退出码为 0。
- 2026-06-16：K2 tests lane 首跑 FAIL，抓到 2 个 `HomeCommandExecutorTests` 回归：typed effect plan 收窄 revision `affectedEntityIDs` 后，event/log/ledger read-model refresh id 丢失。修复方式不是恢复 raw effects 写权，而是在 `CareWriteOutcome.derivedMutation` 中要求 `AuthorizedDomainEffectWrite`，同时允许 `additionalRevisionEntityIDs` 只补 revision payload。
- 2026-06-16：K2 红转绿：窄复测 `DOMAIN_KERNEL_TEST_TARGETS="OhanaTests/HomeCommandExecutorTests" scripts/domain-kernel-fast-gate.sh --tests --no-audits --no-fixtures` PASS（182 tests / 1 suite）；全量 targeted kernel suite `scripts/domain-kernel-fast-gate.sh --tests --no-audits --no-fixtures` PASS（449 tests / 12 suites，约 37s）。
- 2026-06-16：K3 build lane 验证通过：`scripts/domain-kernel-fast-gate.sh --build --no-audits --no-fixtures` PASS（`** BUILD SUCCEEDED **`，`iPhone 17` simulator，约 26s）。

## 模块明细

| 模块 | Phase | 状态 | 开工日期 | 余留项（P1/P2 指针） | 门禁 commit | 交接备注 |
|---|---|---|---|---|---|---|
| Models | 1 | 🟢 | 2026-06-12 | TFU-20260612-012 已由 Domain gate commit `304971af` 关闭；TFU-20260612-013 已关闭 | `db44afe1` | V68 tombstone 默认值、fallback indicator、V67→V68 临时磁盘迁移测试已补并执行 |
| Domain | 2 | 🟢 | 2026-06-12 | TFU-20260615-001 的 Feeding plan/stock 首发可达 P1 已本地修复并由修复会话复审为该面 P0/P1=0；TFU-20260615-002 已本地关闭并恢复 module gate。关闭仍等 push/CI 与全新纯复审。CloudSync remote/live apply 项 TFU-20260614-014/012/011/010 首发不可达，推 `docs/cloud-sync-todo.md` 1.x。旧架构清理见 TFU-20260612-014。 | `3aa7e464a` | 2026-06-15 TFU-20260615-002 修复轮：shared walk 现在区分 effective primary actor 与 participant executor list，`SharedCareSession.executorIds` / `PetWalkLog.executorIds` 保留 secondary co-walker，ledger/reward 仍使用 effective actor；旧 CareEventService happy-path 测试改为插入真实 Human executor，避免断言无效 `"human-1"` 可伪装成 actor。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/SharedPetActionRecorderTests` PASS（25 tests）；`scripts/module-exit-gate.sh` PASS（changed audits、localization、876 unit tests + 3 UI template tests）。本轮仍非纯复审，未 push/CI，Domain 保持 🟢，不得 🏁。 |
| Shared | 3 | 🟢 | 2026-06-12 | P1 余留见 TFU-20260612-015 | `fcf998088` | 附件隐私清理器已改为 ImageIO 重编码，Shared smoothness 阻塞解除；QuickCare 阶段迁移 executor picker 的 Shared `@Query` |
| App | 4 | 🟢 | 2026-06-12 | 无 | `e48c13af7` | 启动/路由/运行时策略审计通过；隐私快照遮罩 a11y 与 shell 文案本地化已修复 |
| Home | 5 | 🟢 | 2026-06-12 | 无 | `b8e8710e` | Phase 5 审计与门禁通过；本轮未发现需修改的 Home P0/P1 项 |
| TodayFocus | 5 | 🟢 | 2026-06-12 | 无 | `b8e8710e` | Phase 5 审计与门禁通过；本轮未发现需修改的 TodayFocus P0/P1 项 |
| QuickCare | 5 | 🟢 | 2026-06-12 | 无 | `b8e8710e` | `ExecutorPickerBar` 的 SwiftData 查询已迁入 QuickCare feature 容器，Shared 只保留纯展示组件；空/多成员 picker smoke tests 已补 |
| Feeding | 6 | 🟢 | 2026-06-12 | 跨模块 read-model 余留见 TFU-20260612-006 | `b49134977` | CloudSync `Event` / `PetFoodRecord` 上传与 apply 支持已补；Feeding 计划、粮仓、断粮提醒、自动投喂日志会写入 dirty/tombstone；dashboard 内容 revision 与本地化已修复；模块门禁通过 |
| Members | 6 | 🟢 | 2026-06-12 | P1/P2 余留见 TFU-20260612-018~021 | `ead1e5fe4` | 2026-06-14 删除模型已改为不可恢复物理删除：Human/Pet/Plant 及相关 Event/Reminder/从属记录经 `PhysicalDeletionService` 写 sync tombstone 后删除；离世成员资料更新、首页显示开关、清空记录命令 no-op 且不发布假 revision；成员创建生日/到家日 Event 与 Reminder 写 sync metadata；RequiredHumanProfileView a11y 修复；真实 UI 抽查见统一 track list |
| Oasis | 6 | 🟢 | 2026-06-12 | 无 | `87423afd8` | 开工：2026-06-12；规则书见 `docs/specs/Oasis-logic.md`；当前主人钱包门、预算/冷却产出、一次性幂等奖励、休眠可唤回语义已落地；`scripts/module-exit-gate.sh` PASS；真实 UI 抽查见 `docs/planning/gap-acceptance-track-list.md#phase-6-oasis`；未改 schema / 路由 / 启动路径 / CloudKit |
| Settings | 6 | 🟢 | 2026-06-12 | P2 余留见 TFU-20260612-022 | `5d4e71928` | 规则书见 `docs/specs/Settings-logic.md`；开发/测试入口收进 Debug-only，通知开关接入 `NotificationDeliveryPolicy`，空 About 入口隐藏；目标测试、UI/a11y/smoothness/runtime 审计与 `scripts/module-exit-gate.sh` PASS；真实 UI / 真机通知抽查见统一 track list |
| Health | 6 | 🟢 | 2026-06-12 | 无 P1/P2 代码余留；真实 UI 抽查见统一 track list | `5d4e71928` | 规则书见 `docs/specs/Health-logic.md`；2026-06-14 删除模型已改为确认后物理删除 + sync tombstone，健康记录删除清理派生费用/日历事件/提醒/ledger，症状/发情记录不进入恢复态；已故宠物只读；schema 升至 `ArkSchemaV70` 且 legacy 字段仅为存储兼容；目标测试与 `scripts/module-exit-gate.sh` PASS；未启用 CloudKit、未改路由或启动路径 |
| Economy | 6 | 🏁 | 2026-06-14 | 无 P0/P1/P2 代码余留；真实 UI 抽查见统一 track list | `92133da2a` | 规则书见 `docs/specs/Economy-logic.md`；2026-06-14 care-derivation executor 架构会话已按产品主人 option A 收口：用户可见回收站和可恢复软删退役，删除走 `PhysicalDeletionService` 物理删除 + 不可见 sync tombstone，离世成员只读且照护写入/编辑/历史补记/派生 no-op；`CareFactWriteDisposition` 二态化，脏 executor id 不再丢 active target 事实；raw `DomainRevisionPublishing.publish` 加 token，care-family command/revision 路径消费 typed executor outcome；R5/derived lifecycle 审计和 fixture 已同步。最终 P1（Insurance expense fact 缺同边界 ledger）已由 `92133da2a` 修复：自动保费与报销统一进入 `ExpenseCommandService.recordPetExpense(... awardsReward: false)` 并补 `pet-expense-ledger-boundary` 审计。最终纯复审会话（Codex，2026-06-14，Insurance ledger 收口后全新纯复审）发现 P0=0 / P1=0 / P2=0；`scripts/module-exit-gate.sh` PASS（837 unit tests + 3 UI template tests），CI run `27505314629` 全绿（audits/lint/build-test）。legacy recycle 字段仅为既有 store compatibility 保留，活跃产品流不得读写。 |
| Medication | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；健康提醒真实 UI / 真机通知抽查债 | `8f6c792dc` | 完整轨批量重走；pet/human medication 的 schedule、通知、删除/离世边界被 `HomeCommandExecutorTests`、`MemberLifecycleGateTests`、`OhanaNotificationsSchedulingTests`、`PhysicalDeletionServiceTests` 覆盖；全仓 audits、fixture tests、`scripts/module-exit-gate.sh --full` 与 CI run `27607807044` PASS。 |
| Walks | 7 | 🟢* | 2026-06-13 | 无本轮 P0/P1；真机定位 / 真实 UI 抽查见统一 track list | `e0c1d69d3` / `8f6c792dc` recheck | 规则书见 `docs/specs/Walks-logic.md`；`WalkFeaturePolicy` 统一 active dog/lifecycle 判定，非狗 / 已离世宠物不可启动遛狗；删除后的 walk / poop marker 已物理移除且不作为产品可见状态；遛狗中便便写 `PetPottyLog` + `CareLedgerEvent` 并进入奖励管线；共享遛狗调用收进基础设施适配器；2026-06-16 批量重走复验全仓 gate/CI PASS，仍待真机定位抽查。 |
| FamilyTasks | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；联机/协作真实 UI 抽查债 | `8f6c792dc` | 功能门后轻量核验 + schedule writer 收口回归；`OnlineFeatureGateTests`、`HomeCommandExecutorTests`、`MemberLifecycleGateTests`、`EconomyBackdateSettlementTests` 覆盖任务门禁、完成、账本/奖励和 schedule 状态；全仓 gate/CI PASS。 |
| Expenses | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实票据/长语言 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；expense fact + care ledger 同边界由 `HomeExpensePreviewStoreTests`、`ExpenseReceiptSupportTests`、`InsuranceExpenseLedgerTests`、`CareLedgerBackfillActorTests`、`CloudSyncMetadataServiceTests` 覆盖；全仓 gate/CI PASS。 |
| DashboardRecords | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；历史阻塞 TFU-20260612-013 已关闭；真实密集数据 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；聚合/快照/趋势面由 `DenseDataSnapshotPerformanceTests`、`HomeReadModelStoreTests`、`HomeSnapshotBuilderTests`、`WeightTrendDataBuilderTests` 间接覆盖；确认 `WeightHistoryView 2.swift` 历史编译阻塞未复现；全仓 gate/CI PASS。 |
| Calendar | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实日历 UI / 长列表抽查债 | `8f6c792dc` | 完整轨批量重走；schedule writer、reminder action、计划日历快照和路由由 `HomeCommandExecutorTests`、`QuickFeedPlanCalendarSnapshotStoreTests`、`ReminderActionCoordinatorTests`、`OhanaNotificationsSchedulingTests`、`HomeRouteCoordinatorTests` 覆盖；全仓 gate/CI PASS。 |
| CrewRoster | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实成员切换 UI 抽查债 | `8f6c792dc` | 轻量轨批量重走；成员名册/切换入口由 `AppRouteCoordinatorTests`、`HomeRouteCoordinatorTests` 覆盖，业务写入仍落 Members/FamilyTasks 等下游 writer；全仓 gate/CI PASS。 |
| Gacha | 7 | 🟢* | 2026-06-13 | 无本轮 P0/P1；真实 UI 抽查见统一 track list | `8f6c792dc` recheck | 与 Shop 合并一轮；规则书见 `docs/specs/GachaShop-logic.md`；大奖概率、概率区间、岛屿合资、冻结钱包、CloudSync serializer/applier 与 schema 迁移已由 `GachaSeriesCatalogTests`、`HomeCommandExecutorTests`、`AutomaticBackupServiceTests` 覆盖；2026-06-16 批量重走复验全仓 gate/CI PASS。 |
| Shop | 7 | 🟢* | 2026-06-13 | 无本轮 P0/P1；真机 App Icon / 真实 UI 抽查见统一 track list | `8f6c792dc` recheck | 与 Gacha 合并一轮；规则书见 `docs/specs/GachaShop-logic.md`；Shop 定价、合资购买、冻结钱包、非消耗品所有权、旧 defaults 迁移、备份恢复与 CloudSync 已由 `HomeCommandExecutorTests`、`AutomaticBackupServiceTests` 覆盖；2026-06-16 批量重走复验全仓 gate/CI PASS。 |
| Documents | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实附件/大文件 UI 抽查债 | `8f6c792dc` | 轻量轨批量重走；文档创建/更新/删除、附件隐私清理、物理删除级联由 `HomeCommandExecutorTests`、`PrivacyHardeningTests`、`PhysicalDeletionServiceTests`、`PetActivityRecordCleanupServiceTests` 覆盖；全仓 gate/CI PASS。 |
| Insurance | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实保单/理赔 UI 抽查债 | `8f6c792dc` | 轻量轨升格核验（已触 expense/ledger）；保费与理赔写 expense + care ledger 边界由 `InsuranceExpenseLedgerTests` 覆盖，删除/附件/成员生命周期由 `PhysicalDeletionServiceTests`、`ExpenseReceiptSupportTests`、`MemberLifecycleGateTests` 覆盖；全仓 gate/CI PASS。 |
| GrowthUnlock | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实解锁文案/UI 抽查债 | `8f6c792dc` | 完整轨批量重走；成长解锁策略与植物门联动由 `GrowthUnlockPolicyTests`、`PlantFeatureGateXCTests` 覆盖；全仓 gate/CI PASS。 |
| Privacy | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实锁定/解锁 UI 与长语言抽查债 | `8f6c792dc` | 完整轨批量重走；附件隐私、隐私偏好、首页读模型过滤和 member lifecycle 边界由 `PrivacyHardeningTests`、`PrivacyProtectionPreferencesTests`、`HomeReadModelStoreTests`、`MemberLifecycleGateTests` 覆盖；全仓 gate/CI PASS。 |
| Achievements | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实领奖 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；成就领奖、去重、预算/账本与路由由 `HomeCommandExecutorTests`、`AppRouteCoordinatorTests`、`HomeRouteCoordinatorTests` 覆盖；全仓 gate/CI PASS。 |
| Moments | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实 quick moment / 相册入口 UI 抽查债 | `8f6c792dc` | 轻量轨升格核验（含可重复奖励）；moment 记录、预算/冷却、路由 overlay 由 `HomeCommandExecutorTests`、`HomeRouteCoordinatorTests`、`AppRouteCoordinatorTests` 覆盖；全仓 gate/CI PASS。 |
| Hygiene | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实护理 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；hygiene fact、shared action、奖励/账本与首页状态由 `CatCareCommandTests`、`SharedPetActionRecorderTests`、`QuestManagerBatchAwardTests`、`HomeExpensePreviewStoreTests`、`CareLedgerBackfillActorTests` 覆盖；全仓 gate/CI PASS。 |
| HumanHealth | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实健康数据删除/展示 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；人类健康记录、删除级联和首页命令面由 `HomeCommandExecutorTests`、`PhysicalDeletionServiceTests`、`CareCompletionChokepointCharacterizationTests` 覆盖；全仓 gate/CI PASS。 |
| HumanNotes | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实笔记 UI 抽查债 | `8f6c792dc` | 轻量轨批量重走；human_note 成员归属与命令写入由 `MemberLifecycleGateTests`、`HomeCommandExecutorTests`、`CareCompletionChokepointCharacterizationTests` 覆盖；全仓 gate/CI PASS。 |
| Memorial | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实纪念模式/情感文案 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；规则书见 `docs/specs/Memorial-logic.md`；离世/纪念边界由 `RainbowBridgeServiceTests`、`MemberLifecycleGateTests`、`MemberCreationServiceTests`、`QuestManagerBatchAwardTests`、`OasisCritterDailyWishTests` 覆盖；全仓 gate/CI PASS。 |
| Milestones | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实里程碑 UI 抽查债 | `8f6c792dc` | 轻量轨升格核验（含 milestone reward）；家庭去重、预算旁路和物理删除边界由 `HomeCommandExecutorTests`、`MemberCreationServiceTests`、`OasisCritterDailyWishTests`、`PhysicalDeletionServiceTests`、`RecurringFindingsRepairTests` 覆盖；全仓 gate/CI PASS。 |
| Notifications | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真机通知权限/投递抽查债 | `8f6c792dc` | 完整轨批量重走；通知预算/调度/取消/route payload 与 app lifecycle 由 `OhanaNotificationsSchedulingTests`、`ReminderActionCoordinatorTests`、`AppLifecycleCoordinatorTests`、`AppRouteCoordinatorTests`、`CloudSyncMetadataServiceTests` 覆盖；全仓 gate/CI PASS。 |
| Onboarding | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实首启/长语言 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；首次体验、online feature gate 与 reset-to-onboarding 由 `OnboardingJourneyCoordinatorTests`、`OnlineFeatureGateTests`、`AppResetServiceTests` 覆盖；全仓 gate/CI PASS。 |
| PetCare | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实照护 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；pet care fact、派生、ledger、reward、CloudSync 与删除边界由 `HomeCommandExecutorTests`、`CareCompletionChokepointCharacterizationTests`、`CareDerivationExecutorSuccessCharacterizationTests`、`CareLedgerBackfillActorTests`、`CloudSyncMetadataServiceTests` 覆盖；全仓 gate/CI PASS。 |
| PhotoAlbum | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实大图/容量/流畅性 UI 抽查债 | `8f6c792dc` | 轻量轨批量重走；相册入口和成员生命周期边界由 `HomeCommandExecutorTests`、`MemberLifecycleGateTests` 覆盖，图片缓存内存告警由 `DecodedImageCacheMemoryWarningTests` 兜底；全仓 gate/CI PASS。 |
| Plants | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；植物门后真实 UI 抽查债 | `8f6c792dc` | 功能门后轻量核验；Plant feature gate、路由回退、首页读模型与成长解锁由 `PlantFeatureGateXCTests`、`HomeRouteCoordinatorTests`、`HomeReadModelStoreTests`、`GrowthUnlockPolicyTests` 覆盖；全仓 gate/CI PASS。 |
| Security | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；PIN/安全真实设备验收债 | `8f6c792dc` | 完整轨批量重走；当前自动层确认 Security 模块编译、architecture/a11y/runtime audits 通过，CloudSync/成员边界相关覆盖见 `CloudSyncMetadataServiceTests`、`MemberLifecycleGateTests`；PIN 真实流程仍需人工验收。 |
| Wishlist | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实心愿单购买/长语言 UI 抽查债 | `8f6c792dc` | 轻量轨升格核验（redeem 触经济/ledger）；创建、兑换、余额不足、删除和 revision 由 `HomeCommandExecutorTests`、`PhysicalDeletionServiceTests`、`HomeRouteCoordinatorTests` 覆盖；全仓 gate/CI PASS。 |
| Workouts | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实锻炼记录 UI 抽查债 | `8f6c792dc` | 轻量轨批量重走；workout 路由、记录/删除和成员边界由 `HomeCommandExecutorTests`、`PhysicalDeletionServiceTests`、`HomeRouteCoordinatorTests`、`CareCompletionChokepointCharacterizationTests` 覆盖；全仓 gate/CI PASS。 |
| CareLedger | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实账本筛选/长数据 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；账本骨干由 `CareLedgerBackfillActorTests`、`CareCompletionChokepointCharacterizationTests`、`CoconutWalletServiceTests`、`EconomyBackdateSettlementTests`、`HomeExpensePreviewStoreTests`、`CloudSyncMetadataServiceTests` 覆盖；全仓 gate/CI PASS。 |
| CatCare | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实猫砂/猫护理 UI 抽查债 | `8f6c792dc` | 完整轨批量重走；CatCare fact、hygiene 派生、undo/revision 与 deceased executor fallback 由 `CatCareCommandTests`、`HomeCommandExecutorTests`、`MemberLifecycleGateTests`、`RecurringFindingsRepairTests` 覆盖；全仓 gate/CI PASS。 |
| FamilyReports | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；无直接 unit 覆盖，真实周报 UI smoke 债 | `8f6c792dc` | 轻量轨批量重走；周报只读面当前以编译、全仓 UI/a11y/smoothness/runtime audits、localization、full test suite 兜底；未发现业务写入/经济/删除/schema 升轨信号，需 Phase 8/9 做真实周报打开 smoke。 |
| FunctionMenu | 7 | 🟢* | 2026-06-16 | 无本轮 P0/P1；真实全功能菜单 UI 抽查债 | `8f6c792dc` | 轻量轨批量重走；功能菜单路由拦截、隐藏功能回退和 app sheet sink 由 `AppRouteCoordinatorTests`、`HomeRouteCoordinatorTests` 覆盖；全仓 gate/CI PASS。 |

> Economy architecture note (2026-06-14, Codex care-derivation executor
> architecture session, not a pure review):
> Product-owner option A is now the governing model: user-visible recycle bin
> and recoverable soft delete are retired; member deletion is irreversible
> physical delete with invisible sync tombstones, and deceased members are
> read-only/no-write. The care write disposition has been simplified to active
> write vs no-op, dirty executor ids no longer drop active-target facts, raw
> domain revision publishing is token-gated, and care-family command/revision
> paths consume typed executor outcomes. Legacy recycle fields remain only for
> store compatibility until a future schema retirement; active product flows must
> not read/write them. Validation for this architecture work includes
> `scripts/build-debug-fast.sh` PASS, targeted simulator suites PASS, economy
> and derived-state audits PASS with existing baselines only, fixture tests PASS,
> and `scripts/module-exit-gate.sh` PASS. Economy remains 🟢 and must not be marked 🏁 until a separate fresh pure
> adversarial review reports P0/P1=0.
>
> Economy final pure review note (2026-06-14, Codex final pure adversarial
> review after CI-green commit `c3c5e5f53`):
> P0=0 / P1=1 / P2=0. P1 remains TFU-20260614-006: Insurance policy auto
> payment schedule and claim reimbursement still insert `PetExpenseLog` without
> same-boundary `CareLedgerEvent` / expense-ledger discipline
> (`Ohana/Features/Insurance/InsuranceCommands.swift:164`,
> `Ohana/Features/Insurance/InsuranceCommands.swift:335`,
> `Ohana/Features/Insurance/InsuranceCommands.swift:384`), unlike
> `ExpenseCommandService.recordPetExpense` which writes both expense fact and
> ledger. Validation: CI run `27502634100` PASS (`audits`, `lint`,
> `build-test`), `scripts/audit-economy-boundaries.sh --all` PASS,
> `scripts/tests/run-audit-fixture-tests.sh` PASS,
> `scripts/audit-derived-state-lifecycle.sh --all` exit 0 with existing
> baseline warnings, and targeted simulator suites PASS (48 tests). Economy
> remains 🟢 and must not be marked 🏁.
>
> Economy current review note (2026-06-14, Codex fresh pure adversarial review):
> P0=0 / P1=3 / P2=1. P1 findings are tracked as TFU-20260614-004:
> QuickPotty detail still performs selection/settings/reminder/UI success effects
> before or without consuming command no-op for unknown shared potty, scoop, and
> full-change paths; `scripts/audit-economy-boundaries.sh --all` misses these
> QuickPotty result-consumption bad examples; clean-DerivedData targeted simulator
> build fails compiling `OhanaTests/HomeCommandExecutorTests.swift` because several
> tests use `executorHuman` without declaring/inserting the fixture. P2:
> `recordUnknownSharedPotty` still returns a detached fallback `PetPottyLog`
> instead of a typed shared result. Current inspection shows the prior TFU-003
> QuickFeed / QuickWater / shared expense revision / plural walk executor issues
> appear patched in this worktree, but this review still has P1. Validation:
> `scripts/audit-economy-boundaries.sh --all` PASS (792 files),
> `scripts/tests/run-audit-fixture-tests.sh` PASS, `git diff --check` PASS, clean
> DerivedData target suite FAIL at compile. Economy remains 🟢 and must not be
> marked 🏁.
>
> Economy previous review note (2026-06-14, Codex fresh pure adversarial review):
> P0=0 / P1=4 / P2=1. P1 findings are tracked as TFU-20260614-003:
> QuickFeed detail manual/treat and QuickWater detail planned/manual/water-change/
> filter paths still show success feedback after typed no-op results; Walk secondary
> `executorIds` are not validated with `EconomyWalletWritePolicy.canWrite`; shared
> expense no-op still publishes a success revision. P2: `recordUnknownSharedPotty`
> still returns a detached fallback `PetPottyLog` instead of a typed shared result.
> Validation run in this review: `scripts/audit-economy-boundaries.sh --all` PASS
> (792 files), `scripts/tests/run-audit-fixture-tests.sh` PASS, `git diff --check`
> PASS. Targeted simulator tests were attempted but did not reach xcodebuild because
> another `scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests`
> process held `.build/locks/test-main-b6cf423d5931-tests.lock`; the waiting process
> was cancelled with exit 130. Economy remains 🟢 and must not be marked 🏁.

> Economy current review note (2026-06-14, Codex fresh pure adversarial review):
> P0=0 / P1=2 / P2=0. P1 findings are tracked as TFU-20260614-001:
> shared-care sourcePet is not a hard lifecycle gate when explicit active targets
> are supplied, and TFU-016's shared hygiene sessions are not recognized by
> `SharedCareSessionMaintenance` delete/reconcile/ledger paths. Validation run in
> this review: `scripts/audit-economy-boundaries.sh --all` PASS (792 files),
> `scripts/tests/run-audit-fixture-tests.sh` PASS, and targeted simulator suite
> PASS (46 tests). Economy remains 🟢 and must not be marked 🏁 until a later
> fresh pure review finds P0/P1=0.
>
> Economy repair note (2026-06-14, Codex fix session for TFU-20260614-001):
> Shared-care sourcePet lifecycle is now a hard no-op gate even when explicit
> active targets are supplied, and shared hygiene sessions now participate in
> `SharedCareSessionMaintenance` reconcile/delete/primary/ledger paths. New
> tests cover shared source no-op across feed/water/litter/generic care/walk/
> unknown potty plus hygiene session reconcile, cascade delete, and primary-fact
> deletion. Validation passed: `QuestManagerBatchAwardTests` 9 tests, key
> Economy simulator suite 55 tests, `scripts/audit-economy-boundaries.sh --all`,
> `scripts/tests/run-audit-fixture-tests.sh`, `scripts/dev-check-changed.sh`
> (exit 0; derived-state lifecycle checklist warnings only), and
> `git diff --check`; `QuestManagerBatchAwardTests` was rerun after SwiftFormat
> and remained PASS. Economy remains 🟢; a separate fresh pure adversarial
> review is still required before 🏁.
>
> Economy current review note (2026-06-14, Codex fresh pure adversarial review
> round 1 after TFU-20260614-001 repair):
> P0=0 / P1=1 / P2=0. P1 is tracked as TFU-20260614-002: non-empty explicit
> executor ids that are invalid, missing, or already purged are still treated as
> writable by `CareFactWritePolicy.executorCannotWrite`, so fact/ledger/
> reminder/revision/Oasis derived writes can occur before reward owner resolution
> rejects the wallet reward. Validation run in this review: targeted simulator
> suite PASS (55 tests), `scripts/audit-economy-boundaries.sh --all` PASS
> (792 files), and `scripts/tests/run-audit-fixture-tests.sh` PASS. Why this
> escaped prior rounds: previous rules and tests modeled frozen executor as an
> existing Human with `hasPassedAway`/`trashedAt`, but did not include the
> post-purge stale active-human id state; G4.1/ECO-026 and the module playbook
> were updated to make unresolved explicit executor a hard no-op boundary.
> Economy remains 🟢 and must not be marked 🏁.
>
> Economy current review note (2026-06-14, Codex fresh pure adversarial review
> round 2 with TFU-20260614-002 still open):
> P0=0 / P1=1 / P2=1. P1 remains TFU-20260614-002: non-empty explicit
> executor ids that are invalid, missing, or already purged still pass
> `CareFactWritePolicy.executorCannotWrite` as writable at the fact layer. P2:
> `CareEventRecording.recordUnknownSharedPotty` / `CareEventService.recordUnknownSharedPotty`
> still return a detached `PetPottyLog` fallback instead of a typed
> `SharedPetActionResult`; current QuickCare executor prechecks prevent an active
> success leak, so this is not upgraded to P1 in this round. Validation:
> targeted simulator suite PASS (242 tests), `scripts/audit-economy-boundaries.sh --all`
> PASS (792 files), and `scripts/tests/run-audit-fixture-tests.sh` PASS. Economy
> remains 🟢 and must not be marked 🏁.
>
> Economy previous review note (2026-06-13, Codex fresh pure adversarial review):
> P0=0 / P1=1 / P2=1. P1 is tracked as TFU-20260613-015
> (`PetWalkingManager.stop` single-walk path writes `PetWalkLog` + wallet reward /
> budget usage but no walk `CareLedgerEvent`; shared walk does write walk ledger).
> Economy remains 🟢 and must not be marked 🏁 until a later fresh pure review finds
> P0/P1=0.
>
> Economy repair note (2026-06-13, Codex fix session): TFU-20260613-015 is fixed
> and closed locally. Validation passed:
> `scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests -only-testing:OhanaTests/QuestManagerBatchAwardTests`,
> `scripts/audit-economy-boundaries.sh --all`,
> `scripts/tests/run-audit-fixture-tests.sh`, and `git diff --check`. Economy
> remains 🟢; a separate fresh pure adversarial review is still required before
> 🏁.
>
> Economy current review note (2026-06-13, Codex fresh pure adversarial review
> after TFU-20260613-015): P0=0 / P1=2 / P2=1. P1 is tracked as
> TFU-20260613-016: deceased-pet historical Calendar / Today Focus /
> notification care completion still writes the history fact and then completes
> occurrence / reminder / success revision; R5 still allows
> `CalendarTaskCompletionSyncService.awardGeneratedCare` by function name without
> proving same-function disposition consumption. P2: legacy `QuestManager.batchAward`
> non-litter potty / hygiene still uses private direct fact/ledger helpers rather
> than a shared typed chokepoint. Economy remains 🟢 and must not be marked 🏁.
>
> Economy repair note (2026-06-13, Codex fix session): TFU-20260613-016 P1 is
> fixed locally. Calendar / Today Focus / notification memorial historical
> fact-only now writes only the history fact, does not complete occurrence or
> reminder, does not publish success revision, and can clean legacy no-ledger
> fact-only records on reopen. R5 now requires `awardGeneratedCare` to consume
> `CareFactWritePolicy.disposition` in the same function and has a matching bad
> fixture. Validation passed: targeted simulator suites 214 tests,
> `scripts/audit-economy-boundaries.sh --all`,
> `scripts/tests/run-audit-fixture-tests.sh`, `scripts/dev-check-changed.sh`, and
> `git diff --check`.
>
> Economy repair note (2026-06-14, Codex fix session): TFU-20260613-016 P2 is
> fixed locally. Legacy `QuestManager.batchAward` non-litter potty / hygiene now
> delegates to `SharedPetActionRecorder` typed shared child strategies and
> creates `SharedCareSession`; non-litter potty facts link back through
> `sharedSessionId`, and hygiene sessions point at the first `PetHygieneLog`
> through `primaryLegacyModelName/Id`. Validation passed:
> `scripts/test-simulator.sh -only-testing:OhanaTests/QuestManagerBatchAwardTests`
> (5 tests), the key simulator suite (214 tests),
> `scripts/audit-economy-boundaries.sh --all`,
> `scripts/tests/run-audit-fixture-tests.sh`, `scripts/dev-check-changed.sh`, and
> `git diff --check`. Economy stays 🟢 and still needs a fresh pure adversarial
> review before 🏁.

## 建设工作（宪法差距，2026-06-12 盘点）

| 工作项 | 优先级 | 状态 | 范围/验收 | 门禁 commit | 备注 |
|---|---|---|---|---|---|
| GAP-1 联机功能门 | P0 上架前 | 🟢 | FamilyTasks+云同步设置+CKShare 入口统一收进 `OnlineFeatureGate`；FamilyReports 留周报剥悬赏 | `59b5ceedc` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；验收通过，真实设备 / 真实 UI 追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-1-联机功能门`；未做 CloudKit 启用 |
| GAP-2 删除模型 | P0 上架前 | 🟢 | 用户可见回收站按 2026-06-14 产品决策退役；成员删除为确认后不可恢复物理删除，CloudSync 仅保留用户不可见 sync tombstone；旧 V69/V70 schema 和 legacy 字段只为 store compatibility 保留 | `be0dd1eeb` / `8f6c792dc` recheck | 旧 GAP-2 回收站验收记录作历史参考，不再代表当前产品模型；当前模型为 `PhysicalDeletionService` 统一 tombstone+delete，移除回收站 UI/恢复路径/备份恢复态，并更新 derived-state lifecycle 审计。2026-06-16 全仓 gate / CI 复验 PASS；真实 UI / 真机通知验收项见 `docs/planning/gap-acceptance-track-list.md#gap-2-删除模型原回收站退役` |
| GAP-3 自动备份 | P0 上架前 | 🟢 | 自动备份至 iCloud Drive 文件+失败可见+恢复端到端测试 | `9b1ac1be1` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，真实设备 / 真实 iCloud 追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-3-自动备份`；未启用 CloudKit 同步 |
| GAP-4 总账恒等 | P1 上架前 | 🟢 | `QuestManager.coconutCount` 为钱包投影；正式岛屿总资产 ≡ 人类成员+宠物钱包；`system:legacy` 仅迁移兼容 | `1951f7834` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，真实 UI / 正式包追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-4-总账恒等` |
| GAP-5 触顶感知 | P1 上架前 | 🟢 | 奖励触顶温和文案，九语言；recordOnly 记录照常完成且奖励反馈不暴露预算数字 | `1a775bc7c` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，真实 UI / 长语言视觉追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-5-触顶感知` |
| GAP-6 通知分级 | P1 上架前 | 🟢 | 通知预算表 + 优先级/限额/合并/夜间免打扰 | `6bb766cc3` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，真机通知到达 / 点击动作追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-6-通知分级`；未启用远程推送或 CloudKit 通知 |
| GAP-7 补记结算 | P1 上架前 | 🟢 | 补记历史日期的记录，奖励计入操作当日预算/冷却；不满足则修 | `528cf2cdd` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，真实 UI 补记路径追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-7-补记结算`；未改 schema / 路由 / 启动路径 / CloudKit |
| GAP-8 单成员形态 | P1 上架前 | 🟢 | 单人单宠下排行榜、周报、心情、家人胶囊等逐面检查 | `6c4a98db2` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，真实 UI 单人单宠目检项见 `docs/planning/gap-acceptance-track-list.md#gap-8-单成员形态`；未改 schema / 路由 / 启动路径 / CloudKit |
| GAP-9 离世退场 | P1 上架前 | 🟢 | Memorial 规则书逐模块写明离世行为并测试 | `e6a45e72c` | 开工：2026-06-12；门禁：`scripts/module-exit-gate.sh` PASS；自动验收通过，未来计划可逆退场、离世成员活跃入口过滤、奖励冻结已补；真实 UI / 真机通知验收项见 `docs/planning/gap-acceptance-track-list.md#gap-9-离世退场`；未改 schema / 路由 / 启动路径 / CloudKit |
| GAP-12 植物功能门 | P0 上架前 | 🟢 | D19：植物全部表面收进独立功能门（添加植物/植物卡/植物 quest/心情信号/FunctionMenu 与路由入口），代码与 Plant 模型保留；已知表面分布：AppFeatureRouteGuard、AddEntityRoute、FunctionMenu、Onboarding/必填主人页、GrowthUnlock、TodayFocusService/QuestEngine/Card、Home snapshot/components、Oasis、Plants 模块本体 | `a1e0e0376` / `8f6c792dc` recheck | 开工：2026-06-13；机制复用 OnlineFeatureGate 模式但独立开关；Q1~Q6 全选 A；规则书见 `docs/specs/PlantFeatureGate-logic.md`；门禁与 2026-06-16 全仓 gate / CI 复验 PASS；真实 UI 追踪项见 `docs/planning/gap-acceptance-track-list.md#gap-12-植物功能门`；未删 Plant 模型或 Plants 模块，未启用 CloudKit |
| GAP-10/11 合资+联机设计 | 1.x | ⬜ | 推迟，见 inventory | | |

## 待拍板问题（提问协议的异步兜底；产品主人答复后销项）

| 编号 | 提出会话/模块 | 问题（含选项与推荐） | 状态 |
|---|---|---|---|
| | | （暂无） | |

## 填写规则

- **状态**：模块会话开工时改 🟡 并填开工日期；`scripts/module-exit-gate.sh` 通过且 commit 后改 🟢 并填门禁 commit 哈希；全新会话对抗复审零 P0/P1 后改 🏁，复审轮次与最后一轮发现数记入交接备注。
- **余留项**：未修的 P1/P2 写入 `docs/task-follow-ups.md`，此处只留一句指针（如「2 条 P2，见 task-follow-ups #Feeding」）。
- **交接备注**：写给下一个会话的人话，例如「HomeReadModelStore 主线程聚合未迁移，触碰 Home 时优先处理」。
- 总览会话每次对账后，在下方「对账日志」追加一行。

## 对账日志（总览会话维护）

| 日期 | 对账范围 | 结论/动作 |
|---|---|---|
| 2026-06-12 | 初始化 | Phase 0 完成（`ff7ac89f`）；总账与门禁脚本建立 |
| 2026-06-12 | 宪法差距盘点 | 产品宪法 v1.1（D1~D18/G1~G10）逐条对照代码：3 个 P0 建设模块（联机门/回收站/自动备份，已拍板都上架前做）、6 项 P1 并入模块会话、合资推 1.x；裁剪三项全部拍板（联机面全收进门、周报留悬赏剥）。明细见 `docs/planning/constitution-gap-inventory.md`，新增 Phase 6.5 |
| 2026-06-12 | 9A 前置项 | **9A.1 付费 Apple Developer 账号已办妥** ✅。解锁：真机签名安装（dogfooding）、App Store Connect 建档、TestFlight（9B 时用）。注意：CloudKit 验证虽技术上解锁，但按 D4 仍属 1.x，不得因账号到位而提前开工。待办：App Store Connect 注册 Bundle ID + 建档抢注 app 名称 |
| 2026-06-12 | GAP 批次对账 | 账实核对：GAP-1~9、Members、Oasis 共 11 项的门禁 commit 全部存在且成对（fix+gate 记录）、工作区干净、AGENTS.md schema 行已同步 V69、九本规则书在 `docs/specs/`。发现：① main 领先 origin 9 个提交未推送→CI（含 SwiftLint 严格）未验证最近批次；② 64 项人工/真机验收债集中在 track list，已在 Phase 6.5 行标注 🟢*；③ 总览会话重跑 `module-exit-gate.sh` 抽查 **PASS**（全量单测+审计绿）。流程修正：验收债规则与 🟢* 状态入手册、收工协议加推送+CI 要求、复审采样节奏入手册 |
| 2026-06-12 | Phase 6 收尾检验 + 宪法 v1.3 | Settings/Health（`5d4e71928`）与 Economy（`662852a01`）账实一致、schema V70 已同步、工作区干净。**违规两项**：① main 领先 origin 14 提交未推送（收工协议第 5 条连续未执行，CI 空转）；② 对抗复审为零（采样规则被跨过两次）。**裁定：Phase 7 开工前必须先 push+CI 绿 + 完成 Economy 与 RecycleBin 两个复审采样**。新决策入宪：D19 植物功能门（GAP-12 登记，P0）、D20 多语言扩展就绪（8.5 增审查项） |
| 2026-06-13 | Economy 复审修复轮 | 对 7 条 P1 + 2 条 P2 按根因归组执行：表征测试提交 `7c7f18b08`，纯重构提交 `67afc59a7`，行为修复提交 `1679ddd66`。新增 legacy 无 actor 不写 system、FamilyTasks 悬赏失败保持待审核且无半笔账测试；`scripts/module-exit-gate.sh` PASS。Economy 状态降回 🟢，需下一轮重新对抗复审清零后才可回 🏁 |
| 2026-06-14 | 架构改造执行检阅 | 读 Codex 完整执行过程 + 核实代码。**纠错**:一度疑 token 是空壳（`rg` 把 `CareDerivationToken` 渲染成 `n`），读实际文件确认 token 真实（`fileprivate init`），方向对、executor 框架与一批 command 迁移有实质进展。**但发现三问，不能放行**：① **P0 数据丢失**——`executorCannotWrite`（CareEventService.swift:145）对无效 UUID/已删 Human 返回 true→disposition `.noOp`→连事实都不写，违反 G3（删成员后缓存 id 当 executor 记照护→事实静默丢失）；② **证人被改**——Codex 把旧表征测试 `executorId:"human-1"`→真实 UUID 让其变绿，把 #1 回归洗白，无测试盯防；③ **焊死未完成**——token 没加到 `DomainRevisionPublishing.publish` 闸门（无 token 参数），command 仍能直接 publish 绕过，验收"构造不出绕过"不满足，主防线退回 R6 采样；④ 工程卫生：巨大未提交脏树、无分簇提交（不可二分回滚）、停在全量门禁红。**新增手册铁律「表征测试不可篡改」**。待产品决策：无效/脏 executorId 是否该丢事实 |
| 2026-06-14 | 复审熔断 → 架构改造 | Economy/care-completion 连续 14 轮复审未收敛。诊断：复审驱动横切不变量数学上不收敛（违反点=分支×派生点，组合爆炸，复审只采样）；且 `disposition{didWriteFact,allowsDerivedEffects}` 字段早已存在但只是建议——发副作用权力仍散落在 12+ command（各自手动逐分支 guard，且只挡 revision、漏 reminder/stock/feedback/Oasis）。**用户选路线 A（架构封死）**：建 `CareDerivationExecutor` 单一派生执行器，收走 command 的副作用触发权，让违反编译期/审计期无法表达。规格 `docs/planning/care-derivation-executor-spec.md`。**熔断规则入手册**（同根因≥3轮或"加了字段还在漏"=权力没收走→禁止再开复审修复轮，转架构改造，验收=构造不出绕过，全程只结尾做一次复审）。**停止 Economy 复审-修复循环**，TFU-013~007 系列以架构改造统一终结 |
| 2026-06-13 | 评估 Codex 自诊断 | Codex 给 5 条原因,经评估重排:#2 返回类型不表达状态=杠杆根因(#1 入口驱动是其果、#4 审计抓不到语义是二阶);**采纳 Codex #3**——不变量是 fact→ledger→budget→reminder→revision→stock→UI 整条派生链,故 `allowsDerivedEffects` 必须是**每个派生点的统一闸门**,不能只在 reward 层消费(否则下一轮又漏一层);#5（规则当天加严）半真,但 TFU-011 的 executor 回落/共享未过滤在旧 G4/ECO-018 下本就违规,不可全归于"标准变了"。**Codex 漏掉的元因**:协议规定同根因≥3轮必开重构轮,却连续点对点=AI 能分析却不反思自己选了治标→靠流程强制不靠自觉。下一轮结构验收钉死:disposition 沿完整派生链做闸门+强制消费+审计 |
| 2026-06-13 | 收敛诊断（冻结门打地鼠） | TFU-011→012→013 同一根因（冻结成员生命周期门）连续 3 轮复现，沿调用栈层层外推（owner→事实写入→命令层），属点对点打地鼠而非结构封死。诊断：① 它是负向不变量（要求副作用不发生），开放式枚举难穷尽；② 真根因=命令层用 `reward==0` 猜状态，缺类型化处置信号；③ 根因重构轮该触发未触发。**裁定：下一轮是结构轮不是补丁轮**——收口返回 `CareFactWriteDisposition{didWriteFact, allowsDerivedEffects}`，command 必须消费，配审计禁止猜状态；UI 层硬挡回收/离世成员进选择器。收敛判据=下一轮复审"构造不出绕过"而非"又找到第 N 个绕过点"。手册「根因重构轮」加强制触发（同根因≥2轮即停止点对点）|
| 2026-06-13 | 收口重构检阅 | 收口重构落地核验:护栏三件套 commit 分离正确(表征`471b22d41`→纯重构`6ce358843`→行为修复独立)、`EconomyRewardDiscipline` 10+ 文件采纳、R5 `reward-direct-awardaction` 审计绿(792 文件)+fixture 自检过、推送/工作区干净。**纠正:Economy 🏁→🟢**——重构改了 10+ 奖励路由,重构后只有机器门禁+自我断言,缺全新会话对抗复审(全新复审发生在重构之前,抓到 Streak P1 已修)。**新增「🏁 完整性防呆」入手册**:🏁 只能纯复审会话授予、需引用复审会话独立证据、改业务代码自动失效。GAP-2 的"收口重构后复审仍零"亦属自我断言,但 GAP-2 未被本重构结构性改动,风险低,保留 🏁 但注记 |
| 2026-06-13 | 收口 plan 决策 | Codex 只读 plan 完成，归类表已确认。三问拍板：Q1 花费不迁入 CareEventService（家族 2，含 human，共享奖励原语+R5）、Q2 时刻豁免、Q3 全清/补签豁免。统一原则「收口=奖励派生纪律非事实类型统一，两家族均受 R5 覆盖」已记入 `Economy-logic.md` ECO-024 与规格文档。花费 farm-risk 登记 ECO-025 / TFU-20260613-010。后续收口重构与复审确认见同日「收口重构落地 + Economy/GAP-2 复审确认」。 |
| 2026-06-13 | 检阅 + 收口根因 | 对账：推送/CI 绿、工作区干净、R1-R4 审计落地（棘轮 baseline 63 条债、fixture 自检过、Economy boundaries 清版）。**根因发现**：Economy 三轮复审反复捞绕过入口的真因是「一动作一事实一次派生」靠约定（10 个 command 各自调 `awardAction`）而非结构。收口点 `CareEventService.recordCareFact` 已存在但采纳不完整。产出重构规格 `docs/planning/care-completion-chokepoint-spec.md`（升为唯一收口 + R5 审计锁死），待派重构会话。状态未变：🏁 仍为 0、验收债 91/0 未燃尽（真机签名未配） |
| 2026-06-13 | 流程优化轮（4 项决策） | ① 复发问题机械化：规格 `docs/planning/recurring-findings-audit-spec.md`（R1 钱包写入白名单/R2 奖励归属/R3 派生状态生命周期/R4 服务层硬门），待建设会话实现并进门禁。② 验收债：已涨到 91 项且真机签名未配——绑定 dogfooding 燃尽、签名先配，入手册 9A.3。③ 双轨制：高风险走完整轨、低风险走轻量轨，分级清单 `docs/planning/phase7-risk-tiers.md` 待主人圈定。④ 流程治本轮：新增 Phase 7.9 整合轮（Phase 7 后做）。另：核验发现 Gacha+Shop 已完成（schema V71）、Phase 7 已实际启动 |
| 2026-06-13 | Phase 7 开工前裁定 | 核验：推送已清零 ✓、Economy 修复轮按治本协议执行（表征→重构→行为三连 commit）✓、GAP-2 四条 P1 全修 ✓、GAP-12 植物门完成 ✓。**修正：GAP-2 的 🏁 降回 🟢**（修复后无新一轮复审记录，不符 🏁 判定）。**硬阻塞：CI build-test 红**（TFU-20260613-007 P1：FamilyTasks 地图视图等非 Economy 面编译失败；TFU-006：CI 工具版本 pin 过期）。**裁定：Phase 7 前先开 CI 修复轮清掉 TFU-006/007；Economy 与 GAP-2 的重新复审与 Phase 7 并行，不阻塞开工** |
| 2026-06-13 | Phase 7 调度补充 | Economy 与 GAP-2 重新复审改为 Phase 7 并行项，只挡 Phase 8 不挡 Phase 7；Phase 7 模块 boot prompt 必须追加「复审模式预检清单」：奖励预算/冷却管线、executor/system 钱包归属、删除/恢复派生状态生命周期、服务层硬门 vs UI 软门，命中任一类直接列 P1。批次顺序：第一批 Walks、Gacha+Shop、Medication、Memorial、Onboarding；第二批 Calendar、Notifications、Expenses、DashboardRecords、Achievements、GrowthUnlock；第三批 Documents、Insurance、Wishlist、Moments、PhotoAlbum、CrewRoster、FunctionMenu 等低风险模块可并行快跑。FamilyTasks 与 Plants 已在门后，仅做编译通过 + 不可达轻量核验 |
| 2026-06-13 | CI 修复轮收口 | TFU-20260613-006/007 已关闭：工具 pin 已刷新并经 CI tool-version 步验证，`build-test` 在 GitHub Actions run `27452421109` 对 commit `33f32ef1a` 绿；本地补充验证 `scripts/dev-check-changed.sh`、`scripts/build-debug-fast.sh`、`scripts/test-simulator.sh -only-testing:OhanaTests/CoconutWalletServiceTests`、SwiftLint 0.63.3 strict lint 均通过。剩余 CI 红点仅 `Architecture boundaries audit`，已单独登记 TFU-20260613-008，不并入本 CI 修复轮；Phase 7 可按第一批从 Walks 开始 |
| 2026-06-13 | Phase 7 Gacha + Shop 收口 | Gacha 与 Shop 合并建设完成：规则书 `docs/specs/GachaShop-logic.md` 已落地；概率 / 定价 / 汇率 / 合资 / 冻结钱包 / App Icon 失败退款 / SwiftData 所有权迁移 / 备份恢复 / CloudSync serializer-applier / schema V71 均有目标测试覆盖；`scripts/dev-check-changed.sh` 与 `scripts/module-exit-gate.sh` PASS。真机 App Icon、真实 UI 分类遍历、真实扭蛋动画、旧安装样本迁移和长语言目检留在统一人工验收 track list |
| 2026-06-13 | 流程/CI 收口推送 | 已推送 `e6f47f843`、`8634babe0`、`92763c164`、`c43dfaf36` 至 `origin/main`。GitHub Actions run `27463715466`：`build-test` 绿（iPhone 17 simulator，UITests 按新 CI 规则跳过，unit suite 完成）、`lint` 绿（SwiftLint strict + SwiftFormat lint），`audits` 红在已登记 TFU-20260613-008 的 `Architecture boundaries audit`；日志显示本轮还新增 / 加重 Gacha+Shop 相关架构信号，已补进 TFU-008，后续单独开架构审计修复轮，不重复触发同一红点 |
| 2026-06-13 | Architecture boundaries 修复轮收口 | TFU-20260613-008 已关闭：规则书 `docs/specs/ArchitectureBoundaryRepair-logic.md` 落地，architecture audit 本地 PASS；修复点包括 `@Query` 数据容器归位、OnlineFeatureGate typed notice publisher、Shop/RecycleBin/Memorial 静态服务调用收进 `AppServices` / infrastructure adapters，并按批准刷新 oversized Swift file ratchet baseline。追加修复 release data-safety audit 版本 pin（`ArkSchemaV71` / backup schema `25`）。GitHub Actions run `27464485820` 对 `90ee16ba4` 全绿：`audits`、`lint`、`build-test` 均 PASS。下一步回到 Economy 与 GAP-2/RecycleBin 重新对抗复审，二者只挡 Phase 8 |
| 2026-06-13 | 复发问题机械化审计建设 | R1/R2/R4 落地为 `scripts/audit-economy-boundaries.sh`，R3 落地为 `scripts/audit-derived-state-lifecycle.sh`；两者均有 bad/good fixture 并纳入 `scripts/tests/run-audit-fixture-tests.sh`、`scripts/dev-check-changed.sh`、`scripts/module-exit-gate.sh` 与 CI `audits` job。全仓棘轮基线写入 `docs/governance/manifests/recurring-findings-audit-baseline.json`：Economy 边界存量 5 条，派生状态生命周期存量 65 条；存量债登记为 TFU-20260613-009。该机制只防新增/加重，不替代 Economy 与 GAP-2/RecycleBin 的重新对抗复审 |
| 2026-06-13 | Economy + GAP-2 重新对抗复审 | 复审未清零，二者继续只挡 Phase 8 不挡 Phase 7。Economy P1：5 个奖励入口仍缺 executor 显式传入，`scripts/audit-economy-boundaries.sh --all` 复现 5 条 `reward-actor-boundary` baseline；会导致业务事实 actor 与钱包收入 owner 分离。GAP-2 / RecycleBin：上一轮四条 P1 已被 `RecycleBinServiceTests` 覆盖，但全仓删除边界仍有 P1 复发，`scripts/audit-derived-state-lifecycle.sh --all` 复现 65 条 baseline，其中 Calendar / PetCare / Hygiene / DashboardRecords / CatCare 存在上传管线实体物理删除无 tombstone。状态：Economy 与 GAP-2 均降为 🔵，修复后需再次对抗复审零 P0/P1 才能 🏁 |
| 2026-06-13 | TFU-20260613-009 P1 修复轮 | 已修复 Economy 与 GAP-2 重新复审中命中的 P1 子集：奖励归属入口补显式 executor / active-human 归属测试，Calendar / PetCare / Hygiene / DashboardRecords / CatCare 删除边界补 CloudSync tombstone。新增 `OhanaTests/RecurringFindingsRepairTests.swift` 6 条回归测试；`scripts/audit-economy-boundaries.sh --all` baseline 归零，`scripts/audit-derived-state-lifecycle.sh --all` baseline 从 65 降到 63。TFU-20260613-009 仍保持 Open，因为剩余 63 条 derived-state 存量债尚未逐项修复或批准 allow；Economy 与 GAP-2 状态回到 🟢，但必须再次对抗复审零 P0/P1 才可 🏁 |
| 2026-06-24 | TFU-20260613-009 recurring-findings baseline 清零 | 关闭存量 recurring-findings P1：derived-state audit 不再把纯 `delete` 命令 enum case、FamilyTasks delegated delete、Hygiene / Calendar View 层 command-executor trigger 误判成物理删除；真实 Economy prune 缺口已修，`EconomyDailyBudgetStore.pruneOldUsageEvents` 删除旧 `EconomyBudgetUsageEvent` 前写 CloudSync tombstone，并由 `HomeCommandExecutorTests.economyBudgetUsagePruneKeepsRecentRows` 断言。`docs/governance/manifests/recurring-findings-audit-baseline.json` 已通过 `scripts/audit-derived-state-lifecycle.sh --update-baseline` 更新为 Economy=0 / Derived=0。验证：`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-derived-state-lifecycle.sh --all` PASS（846 files）；`scripts/audit-economy-boundaries.sh --all` PASS（846 files）；`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（183 tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_01-58-46-+0200.xcresult`）。TFU-20260613-009 改为 Done。 |
| 2026-06-13 | Economy + GAP-2 第二轮重新对抗复审 | 复审仍未清零，二者继续只挡 Phase 8 不挡 Phase 7。自动审计结果：`scripts/audit-economy-boundaries.sh --all` PASS（791 files，baseline 0）；`scripts/audit-derived-state-lifecycle.sh --all` 仍有 63 条既有 baseline。新增 P1：① Calendar / Today Focus / 通知完成照护任务入口未统一进入照护事实 + 奖励预算管线，导致同一动作不同入口奖励 / ledger 不等价；② Calendar 生成的 `PetCareLog` / `PetPottyLog` / `PetHygieneLog` / `CareLedgerEvent` 撤销时物理删除无 tombstone；③ CarePlan / Water 计划删除 `Event` 时无 tombstone。规则冲突待拍板：Health 规则书与现代码将健康 / 症状 / 发情单条记录放进回收站，但宪法 D16 要求单条高频流水直删且底层 tombstone。状态：Economy 与 GAP-2 均降为 🔵，修复前不能 🏁 |
| 2026-06-13 | Economy + GAP-2 第二轮 P1 修复轮 | 已按产品选择修复第二轮复审新增 P1：Calendar / Today Focus / 通知照护完成统一写照护事实并走 `QuestManager.awardAction`；旧 Calendar 完成奖励服务保留 no-op；Calendar occurrence 撤销冲销钱包并 tombstone 生成事实 / ledger / budget 事件；CarePlan / Feeding / Water 计划删除写 `Event` / `Reminder` tombstone；SymptomLog / HeatCycleLog 改为直删 + tombstone，PetHealthLog 保持回收站。新增 / 更新 `OhanaTests/RecurringFindingsRepairTests.swift` 与 `OhanaTests/HomeCommandExecutorTests.swift` 覆盖；本地窄测试 PASS，`scripts/module-exit-gate.sh` PASS（771 个单测 + 3 个模板 UI tests）；CI 按用户确认跳过。Economy 与 GAP-2 状态回到 🟢，但仍需全新会话重新对抗复审零 P0/P1 才可 🏁 |
| 2026-06-13 | Economy + GAP-2 新视角清零复审 | RecycleBin 未发现新的 P0/P1；Economy 发现 1 条 P1（Streak 连击奖励无 active human 时仍 fallback 写 system wallet），已修为无 active human 或冻结 pet 时直接不发奖、不记 claimed、不写 system。验证：`scripts/dev-check-changed.sh` PASS，`scripts/audit-economy-boundaries.sh --all` PASS，`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（159 tests），`scripts/test-simulator.sh -only-testing:OhanaTests/RecycleBinServiceTests` PASS（9 tests）。Economy 与 GAP-2 均标 🏁，人工 / 真机 UI 验收债仍留统一 track list |
| 2026-06-13 | 收口重构落地 + Economy/GAP-2 复审确认 | 按护栏三件套完成 Economy 收口：表征测试 `CareCompletionChokepointCharacterizationTests` 先行，纯重构新增 `EconomyRewardDiscipline`，家族 1 照护奖励与家族 2 非照护奖励均通过共享奖励纪律入口，未把花费/时刻/全清/补签伪装成 `CareType`；R5 `reward-direct-awardaction` 审计与 bad/good fixture 纳入 `scripts/audit-economy-boundaries.sh`。验证：`scripts/tests/run-audit-fixture-tests.sh` PASS，`scripts/audit-economy-boundaries.sh --all` PASS（792 files），`scripts/audit-derived-state-lifecycle.sh --all` 无新增债（仍有既有 baseline），`scripts/module-exit-gate.sh` PASS。落地后重新攻击 Economy 与 RecycleBin，未发现新的 P0/P1，二者保持 🏁；CI run `27468716740` 已推送后检查，检查时状态为 in_progress。 |
| 2026-06-14 | Economy 全新纯对抗复审 | 本轮纯复审会话（Codex，2026-06-14，用户要求“全新纯对抗复审”）发现 Economy P0=0 / P1=4 / P2=1，不能标 🏁，状态保持 🟢。P1：shared manual feed 在 recorder no-op gate 前触发 first-meal special reward / flag；planned feed / water catch-up 被错误固化为 0 奖励而非 operation-day 预算结算；Insurance 保费 / 报销直接写 `PetExpenseLog` 但缺同边界 expense ledger 纪律；`HomeCommandExecutorTests` 目标套件仍有 1 个失败。P2：`recordUnknownSharedPotty` no-op 时仍返回 detached `PetPottyLog`，API 容易误判成功。已登记 TFU-20260614-006。验证：`scripts/audit-economy-boundaries.sh --all` PASS（792 files），`scripts/tests/run-audit-fixture-tests.sh` PASS，`DERIVED_DATA_PATH=/tmp/OhanaDerivedData/economy-pure-review-20260614 scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` FAIL（174 tests，1 failure），`git diff --check` PASS。 |
| 2026-06-14 | Care 派生执行器架构改造收口 | 按产品主人 option A 落地二态删除/离世模型：用户可见回收站与可恢复软删退役，删除走物理删除 + 不可见 sync tombstone，离世成员只读且不写照护事实/编辑/派生；`CareFactWriteDisposition` 简化为 active/no-op，dirty executor 不再导致 active target 事实丢失；raw domain revision publish 加 token，care-family command/revision/UI feedback 入口消费 typed `CareWriteOutcome`；旧 `RecycleBinService`/UI/备份恢复态移除，GAP-2 改为删除模型历史项。TFU-20260614-002~005/007 已由架构/新产品规则关闭，TFU-20260614-006 仍保留 Insurance expense ledger 与最终纯复审债。验证截至本地：targeted simulator suites PASS、`scripts/build-debug-fast.sh` PASS、economy/derived audits 与 fixture tests PASS、`scripts/module-exit-gate.sh` PASS（834 unit tests + 3 UI template tests）。push/CI 待本会话结尾执行。Economy 保持 🟢，不得 🏁，完成后还需全新纯对抗复审 P0/P1=0。 |
| 2026-06-14 | 总览检阅简化落地 | 实质核查：**进步**——工程卫生 A（分阶段 commit `689329394`/`be0dd1eeb`、工作区干净、已推送）；旧语义扫净（`memorialHistoricalFactOnly`=0、活跃 `trashedAt` 过滤=0、`RecycleBinService`=0、`PhysicalDeletionService` 建好）；token 真焊到通用 `publish(_:token:)` 闸门（上轮缺陷已修）。**P0（确凿，不能 🏁）**——缺陷3「脏 executor 丢事实」**未真修，且代码直接违反刚写进宪法的 ECO-026④/G4.1④**：测试 `executorId:"human-1"`→`PetCareLog isEmpty` 锁定了"非法/已删 executor → no-op 丢事实"，而宪法明文要求"不可解析/已删/不可写 executor 仍写 active 对象事实+fallback 归属"。Codex 对账日志(283)自称"dirty executor 不再丢事实"与该测试矛盾。真实风险：删成员后缓存 id 当 executor 记照护→事实静默丢失。只修了 nil/空 executor，未修 invalid/已删。**P1（轻，可接受）**——照护类 `publishPetCareRecord` 等仍不要 token 且 command 仍持有 `revisions`，"编译期构造不出绕过"未 100%，靠 R6 文本审计兜底。**裁定**：修复 P0（让 executorCannotWrite 的"不可解析/fetch 不到"分支对 active 对象走 fallback 写事实，而非 noOp；改正锁定错误行为的测试），CI 待绿，之后才可开最终纯复审 |
| 2026-06-14 | 最终纯复审检阅(收敛确认) | Economy 架构改造后最终纯复审 P0=0/P1=1/P2=0。**核实 P1 属实**：InsuranceCommands 自动保费/报销手搓 `PetExpenseLog`（:384/392）但整文件无 `CareLedgerEvent`，绕过统一 expense 入口，违反 ECO-024 家族2。**元判断=收敛铁证**：14 轮泥潭的 care-completion/executor 线本轮 **0 发现**（token 闸门+单一 executor 通道封死成功），唯一余留是一个**无关的孤立问题**（Insurance 绕 ledger，根因同构=绕过统一入口但不同域），非同根因反复——路线 A 架构封死达成收敛。P1 不破坏 G2 椰子余额（保费是真钱非椰子），是花费两源一致性问题。**裁定**：小修（优先让 Insurance 走统一 expense 入口治本，否则补同边界 ledger）+ 给审计加"写 PetExpenseLog 必须配 ledger"堵盲区，修后开最后一次纯复审零 P0/P1 → Economy 🏁。care 线视为已收敛 |
| 2026-06-14 | 总览自纠(P0 误判) | **撤回 2026-06-14「总览检阅简化落地」记录的 P0**：那条"脏 executor 丢事实违反 ECO-026④/证人被改"是**误判**。当时只 `sed` 了 QuickWaterCommandTests 230-250 行，错过测试名 `quickWaterExecutorNoopsForDeceasedPetAtCommandBoundary` 与 `pet.passedAwayDate` setup——该测试是**离世 pet → no-op**（符合宪法 ECO-026②），`human-1` 只是顺带 executor，非"脏 executor 丢事实"。代码铁证：`CareEventService.disposition`（:114）`executorId _:` 被忽略，写不写事实只看 `EconomyWalletWritePolicy.canWrite(pet)`，executor 仅决定归属——`7ded53a6a` 已正确修复。**元教训：检阅者不免检**；这是第二次因读片段未读全文/上下文差点冤判（前次 token 被 rg 渲染成 `n`）。手册「测试断言必须符合宪法」铁律保留（规则对），但当时举的 human-1 例子是误读。Economy 🏁 在可核实维度成立（见下条） |
| 2026-06-14 | Economy 🏁 核查 | Economy 标 🏁（`92133da2a`）核实：① Insurance P1 真修——`InsuranceCommands` 保费/报销改走统一入口 `ExpenseCommandService.recordPetExpense`（:335/386），治本（自动继承 ledger 纪律），非补丁；② disposition 二态正确（pet active→写、离世/删除→no-op，executor 不进判定）；③ CI run `27505314629` = success，headSha=`92133da2a`（含 Insurance 修复）；④ git 干净已推送，治理改动（oversized 豁免/baseline 瘦身/手册铁律）已入库。轻提醒：`docs: record final pure review`(67937) 在 Insurance fix(92133) 之前、`mark mature`(f88818) 纯 docs，"修复后复审"未单独成 commit，时序命名略乱但 CI 在修复后全绿+总账声称修后复审零发现，🏁 成立。**Economy = 核心五模块首个 🏁** |
| 2026-06-14 | Domain 复审优先级纠偏 | Codex Domain 复审 6 轮（TFU-009~014）同根因未收敛，**自己**诊断出横切根因并提议熔断成 CloudSyncApplyDisposition policy 层——方法论对（内化了 care-executor 教训）。**但总览核实**：CloudSync remote/live apply 首发不可达（`cloudKitDatabase: .none`，apply 靠 CloudKit 触发，单机跑不到）。**裁定**：① 首发只收口**本地物理删除级联**（可达真 P1，用"删 parent 构造不出漏 owned entity"注册矩阵审计封死）；② TFU-010/011/012/014 的 remote/live apply policy 层（Codex 建议的架构对、时机错）推 1.x，移交 `cloud-sync-todo.md`「CloudKit-Enable-Time Architecture」，1.x 有真实远端数据再做；③ 立**可达性规则**入 cloud-sync-todo：复审发现若可证首发不可达（门挡/死代码），不阻塞该模块首发 🏁，但须登记+复审确认。避免在 `.none` 死代码上烧架构弹药。Domain 首发 🏁 判据=首发可达面零 P0/P1 |
| 2026-06-14 | Economy 最终纯复审清零 | 修复提交 `92133da2a` 后，本轮全新纯复审会话（Codex，2026-06-14，Insurance ledger 收口）结论：P0=0 / P1=0 / P2=0，Economy 标 🏁。核验：Insurance 自动保费与报销均走 `ExpenseCommandService.recordPetExpense(... awardsReward: false)`，无裸 `PetExpenseLog`；新增 R7 `pet-expense-ledger-boundary` 审计能抓坏例并放行统一入口；`scripts/audit-economy-boundaries.sh --all` PASS（804 files），`scripts/tests/run-audit-fixture-tests.sh` PASS，`scripts/module-exit-gate.sh` PASS（837 unit tests + 3 UI template tests），CI run `27505314629` 全绿（audits/lint/build-test）。 |
| 2026-06-14 | Domain 当前代码纯对抗复审 | 本轮是 Domain 模块（`Ohana/Domain/`）针对 care-derivation executor、PhysicalDeletionService、二态 disposition、CloudSync 后的首次当前代码纯复审；会话身份：Codex Domain pure adversarial review，2026-06-14。结论：P0=0 / P1=5 / P2=2，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-009。P1：care-family typed revision publish/no-op 仍可无 token 绕过 executor；Pet/Human 物理删除未清理钱包/ledger/shared session 等字符串 ID orphan；`CareLedgerEvent` 写入不建 CloudSync dirty state；远端 `CoconutLedgerEntry` apply 后不重放 `CoconutAccount` projection；raw `CoconutWalletDelta` 可绕过冻结钱包硬门。P2：unknown shared potty no-op 返回 detached log；shared manual feed first-meal 派生仍在 recorder 事实前。验证：`scripts/audit-economy-boundaries.sh --all` PASS（804 files）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（804 files，Domain 无新增命中但有既有跨模块 warnings）；targeted simulator 命令 `scripts/test-simulator.sh -only-testing:OhanaTests/SharedPetActionRecorderTests/deletingPetReconcilesSurvivingSharedFeedSessionStockOwner` TEST SUCCEEDED 但 Swift Testing 过滤命中 0 tests，仅证明测试目标可编译，不作为行为证明。 |
| 2026-06-14 | Domain P1/P2 修复会话 | Codex Domain repair session 针对 TFU-20260614-009 完成当前 worktree 修复：移除 care-family typed revision/no-op publisher 出口并让 no-op care command 走 `CareDerivationExecutor`；`PhysicalDeletionService` 清理 Pet/Human 相关 wallet account、wallet ledger、care ledger、shared session 字符串引用并保留 sync tombstone；`CareLedgerService.record` 写 CloudSync dirty state；CloudSync 远端 `CoconutLedgerEntry` apply 后通过 `CoconutWalletService.reconcileFormalAccountBalancesWithLedger` 重放 projection；raw wallet delta 冻结门按 owner id/account key fail closed；unknown shared potty no-op 返回 nil；shared manual feed first-meal 延后到 recorder 成功后。验证 PASS：`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests -only-testing:OhanaTests/CloudSyncMetadataServiceTests -only-testing:OhanaTests/CoconutWalletServiceTests`、`scripts/test-simulator.sh -only-testing:OhanaTests/SharedPetActionRecorderTests -only-testing:OhanaTests/QuestManagerBatchAwardTests`、`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests`、`scripts/tests/run-audit-fixture-tests.sh`、`scripts/module-exit-gate.sh`（841 unit tests + 3 UI template tests）。Domain 仍保持 🟢，不得 🏁；待推送 CI 绿后开全新纯对抗复审。 |
| 2026-06-14 | Domain 全新纯对抗复审（`f5cc637c8` 后） | 本轮纯复审会话（Codex，2026-06-14，用户要求“全新纯对抗复审”）结论：P0=0 / P1=2 / P2=0，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-010。P1-1：`CloudSyncRecordApplier.apply`/`applyHardDeletedRecord` 对远端 Pet/Human 删除只调用 `deleteLocalModel` 裸 `context.delete`，绕过 `PhysicalDeletionService.deletePet/deleteHuman` 的 wallet account、wallet ledger、care ledger、shared session 字符串引用级联，本机可留下 orphan 派生状态。P1-2：同一 delete applier 对远端 `CoconutLedgerEntry` tombstone 裸删 ledger，未调用 `CoconutWalletService.reconcileFormalAccountBalancesWithLedger`，`CoconutAccount` / member balance projection 会保留旧余额，违反 G2。验证：`scripts/audit-economy-boundaries.sh --all` PASS（804 files）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（804 files，报告既有 warnings，未拦 CloudSync delete 盲区）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests -only-testing:OhanaTests/PhysicalDeletionServiceTests -only-testing:OhanaTests/CoconutWalletServiceTests` PASS（Swift Testing 89 + XCTest 16）；`scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests -only-testing:OhanaTests/ReminderActionCoordinatorTests -only-testing:OhanaTests/SharedPetActionRecorderTests` PASS（Swift Testing 54；XCTest 过滤 0）。 |
| 2026-06-14 | Domain CloudSync remote-delete 修复 + 后续纯对抗复审 | Codex 修复 TFU-20260614-010 原两项 P1：远端 Pet/Human tombstone/hard deletion 现在调用 `PhysicalDeletionService.deletePet/deleteHuman`，远端 `CoconutLedgerEntry` tombstone 删除后调用 `CoconutWalletService.reconcileFormalAccountBalancesWithLedger`；新增 `CloudSyncMetadataServiceTests` 红测覆盖 pet cascade 与 ledger projection replay。按用户要求跳过 CI、不推送。本轮随后开启全新纯对抗复审，结论 P0=0 / P1=2 / P2=1，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-011。P1-1：`CloudSyncRecordApplier.deleteLocalModel` 对远端 `Event` 删除仍裸 `context.delete`（`Ohana/Domain/Services/CloudSyncRecordApplier.swift:993`），绕过 `PhysicalDeletionService.deleteEvent` 的通知取消与 reminder delete/mark 边界（`Ohana/Domain/Services/PhysicalDeletionService.swift:13`）。P1-2：远端 `SharedCareSession` 删除仍裸删 session（`Ohana/Domain/Services/CloudSyncRecordApplier.swift:1029`），绕过 `SharedCareSessionMaintenance.deleteCascade` 的 child facts + ledger 级联（`Ohana/Domain/Services/SharedCareSessionMaintenance.swift:132`）。P2：远端 pet-scoped fact tombstones 仍裸删单 fact（`Ohana/Domain/Services/CloudSyncRecordApplier.swift:997` 等），不消费本地 command delete 的 ledger 清理和 shared session reconcile 纪律。验证：新增 red tests 初跑失败符合预期；修复后 `scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（87 Swift Testing tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests -only-testing:OhanaTests/CoconutWalletServiceTests -only-testing:OhanaTests/SharedPetActionRecorderTests` PASS（XCTest 16 + Swift Testing 29）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（804 files，既有 warnings）；`scripts/audit-economy-boundaries.sh --all` PASS；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/dev-check-changed.sh` PASS；`git diff --check` PASS。 |
| 2026-06-14 | Domain CloudSync delete-dispatch 修复后纯对抗复审 | Codex 修复 TFU-20260614-011 原 P1/P2：远端 Event 删除走 `PhysicalDeletionService.deleteEvent`，远端 SharedCareSession 删除走 `SharedCareSessionMaintenance.deleteCascade`，远端 pet-scoped fact 删除走 `PhysicalDeletionService.deletePetScopedRecord` 并清 ledger / reconcile shared session；按用户要求跳过 CI、不推送。本轮随后开启全新纯对抗复审，结论 P0=0 / P1=1 / P2=0，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-012。P1：CloudSync 远端 feeding `PetCareLog` 或 `PetFoodRecord` tombstone/hard deletion 删除事实后不触发 `FeedingPlanWriter.rebuildFoodStockReminders`，与本地 `FeedRecordCommand.deleteLog` / `deleteFoodRecord` 不等价，可留下旧 `pet_food_stock` Event/Reminder。验证：新增 red tests 初跑失败符合预期；修复后 `scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（91 Swift Testing tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests -only-testing:OhanaTests/CoconutWalletServiceTests -only-testing:OhanaTests/SharedPetActionRecorderTests -only-testing:OhanaTests/OhanaNotificationsSchedulingTests` PASS（XCTest 16 + Swift Testing 38）；`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（176 tests，修复过程中曾红 1 条后已修绿）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（804 files，既有 warnings）；`scripts/audit-economy-boundaries.sh --all` PASS；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/dev-check-changed.sh` PASS；`git diff --check` PASS。 |
| 2026-06-14 | Domain feeding stock reminder 修复后纯对抗复审 | Codex 修复 TFU-20260614-012 原 P1：远端 feeding `PetCareLog` / `PetFoodRecord` tombstone 或 hard deletion 现在通过 delete dispatcher 捕获 affected pet 并重建 `pet_food_stock` reminders；按用户要求跳过 CI、不推送。本轮随后开启全新纯对抗复审，结论 P0=0 / P1=2 / P2=0，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-013。P1-1：`CloudSyncEntityRegistry.uploadPipelineEntityNames` 包含 `GachaOwnedItem` / `GachaDrawLog` / `ShopPurchaseRecord`，业务入口会 `markModified`，serializer/applier 也支持，但 `CloudSyncUploadBatchBuilder.localModel` 无三者 fetch case，dirty payload 构建会抛 `missingLocalModel`。P1-2：`PhysicalDeletionService.deleteHuman` 的 human-scoped cascade 未删除 `GachaOwnedItem.ownerHumanId`、`GachaDrawLog.ownerHumanId`、`ShopPurchaseRecord.buyerHumanId` 记录；删除成员后人属 Gacha/Shop 数据残留，且 Gacha 缺本地 `markDeleted` tombstone overload。验证：新增 red tests 初跑失败符合预期；修复后 `scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（93 Swift Testing tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests -only-testing:OhanaTests/SharedPetActionRecorderTests -only-testing:OhanaTests/CoconutWalletServiceTests -only-testing:OhanaTests/OhanaNotificationsSchedulingTests -only-testing:OhanaTests/ManualFeedCommandTests -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（XCTest 16 + Swift Testing 227）；`scripts/dev-check-changed.sh` PASS；纯复审只读护栏：`scripts/audit-derived-state-lifecycle.sh` PASS（12 files）；`scripts/audit-economy-boundaries.sh` PASS（12 files）；`scripts/tests/run-audit-fixture-tests.sh` PASS（804 files floor）；`git diff --check` PASS。 |
| 2026-06-14 | Domain TFU-20260614-013 Gacha/Shop lifecycle 修复 | Codex repair session 本地修复 Gacha/Shop CloudSync upload 与 human-delete lifecycle P1：`CloudSyncUploadBatchBuilder` 补 `GachaOwnedItem` / `GachaDrawLog` / `ShopPurchaseRecord` fetch case；`CloudSyncMutationRecorder` 补 Gacha delete tombstone overload；`PhysicalDeletionService.deleteHuman` 级联删除人属 Gacha/Shop 记录并写 tombstone。新增红测：registered Gacha/Shop dirty payload 构建、成员删除后 Gacha/Shop 物理删除 + tombstone；新增 `cloudsync-upload-builder-coverage` derived-state 审计和 bad/good fixture，堵住“注册 upload pipeline 但 builder 漏接”的矩阵盲区。验证：修复前 `CloudSyncMetadataServiceTests` 红在 `missingLocalModel`，`HomeCommandExecutorTests` 红在 3 类记录/3 个 tombstone 未删除；修复后 `scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（94 tests）、`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（177 tests）、`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests` PASS（4 tests）、`scripts/tests/run-audit-fixture-tests.sh` PASS、`scripts/audit-derived-state-lifecycle.sh --all` exit 0（804 files，只有既有 baseline warnings）、`scripts/audit-economy-boundaries.sh --all` PASS、`scripts/dev-check-changed.sh` PASS、`git diff --check` PASS。按用户方向跳过 CI；Domain 保持 🟢，不得 🏁，下一步必须全新纯对抗复审 P0/P1=0。 |
| 2026-06-14 | Domain TFU-013 后全新纯对抗复审 | 本轮纯复审会话（Codex，2026-06-14，用户要求“复审”）结论：P0=0 / P1=3 / P2=0，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-014。P1 同根因：CloudSync live apply 缺少统一 deletion-wins / parent lifecycle / natural identity policy。P1-1：live remote `Pet` / `Human` 若 `lastModifiedAt` 新于本地 deletion tombstone，会绕过 stale gate、重新 insert/update 并清掉 tombstone，违反 D8/G5。P1-2：live child/fact apply 不校验 parent active/existing；pet-scoped facts 在 `petReference` 为 nil 时仍 insert，Gacha/Shop 也接受已删除 human id，late remote record 可在成员删除后重建 orphan。P1-3：`GachaOwnedItem` 按随机 id 合并，不按 owner+series+item natural key 合并，和 registry 的 `ownedCount maxValue` 策略不一致，可产生重复 ownership projection。验证：`scripts/audit-derived-state-lifecycle.sh --all` exit 0（804 files，仅既有 baseline warnings，未抓本 P1）、`scripts/audit-economy-boundaries.sh --all` PASS、`scripts/tests/run-audit-fixture-tests.sh` PASS、`scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（94 tests，证明现有测试未覆盖该竞争）、`git diff --check` PASS。 |
| 2026-06-14 | Domain 本地物理删除级联架构收口 | 按总览裁定，本轮只收首发可达的本地路径，CloudSync remote/live apply policy 继续推 `docs/cloud-sync-todo.md` 1.x。新增 `CloudSyncEntityRegistry.physicalDeletionOwnerships` 与 `PhysicalDeletionService.localPhysicalDeletionCascadeCoverage` 注册矩阵；`deletePet/deleteHuman` 级联覆盖 Event/Reminder、PetRelationship、FamilyCollaborationTask、SharedCareSession、wallet account/ledger、CareLedgerEvent、EconomyBudgetUsageEvent、GachaOwnedItem/GachaDrawLog/ShopPurchaseRecord 及其他 Pet/Human owned/scoped 记录，并写不可见 sync tombstone。新增入口族红测：`physicalDeletionCoverageMatchesCloudSyncRegistryOwnershipManifest`、`deletePetCascadesFirstReleaseRegisteredOwnedEntities`、`deleteHumanCascadesFirstReleaseRegisteredOwnedEntities`；新增 derived-state lifecycle `physical-deletion-cascade-coverage` 审计和 bad/good fixture，确保 registry 新增 owned entity 但 deletion coverage 漏接会红门。验证：修复前 `PhysicalDeletionServiceTests` 红在 Pet Event/Reminder、PetRelationship、FamilyCollaborationTask 与 Human Event/Reminder、SharedCareSession、CoconutExchangeRequest、FamilyCollaborationTask 残留；修复后 `PhysicalDeletionServiceTests` PASS（7 tests）、`PhysicalDeletionServiceTests + CloudSyncMetadataServiceTests + HomeCommandExecutorTests` PASS（278 tests）、`scripts/tests/run-audit-fixture-tests.sh` PASS、`scripts/audit-derived-state-lifecycle.sh --all` exit 0（仅既有 baseline warnings）、`scripts/audit-architecture-boundaries.sh --all` PASS、`scripts/dev-check-changed.sh` PASS、`scripts/module-exit-gate.sh` PASS（854 unit tests + 3 UI template tests）。Domain 保持 🟢，待推送 CI 绿后开首发可达面纯复审，P0/P1=0 才能标 🏁。 |
| 2026-06-14 | Domain 本地物理删除级联最终纯复审 | 本轮首发可达面纯对抗复审（Codex，2026-06-14，commit `3aa7e464a` + CI run `27510220670` 全绿后）结论：P0=0 / P1=1 / P2=0，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-015。P1：`PhysicalDeletionService.deleteHuman` 把 `SharedCareSession.executorIds` 只含被删 human 的 session 物理删除，但 active pet child facts 必须保留，当前不会清 `PetCareLog` / `PetWalkLog` / `PetExpenseLog` 等 child 的 `sharedSessionId`，可留下指向已删除 session 的 orphan。现有 `deleteHumanCascadesFirstReleaseRegisteredOwnedEntities` 只构造 session，不构造 child log，入口族测试漏断言。验证：只读复审命令包括 `git status --short`（纯复审开始前干净）、`rg cloudKitDatabase`（确认首发 `.none`）、schema/model/registry/PhysicalDeletionService/SharedCareSessionMaintenance 代码扫描、`scripts/audit-derived-state-lifecycle.sh --all`（exit 0，仅既有 baseline warnings）。 |
| 2026-06-14 | Domain TFU-20260614-015 shared-care child orphan no-CI 修复 | Codex repair session 按用户要求“修复，不ci”本地修复最终纯复审 P1。`PhysicalDeletionService.deleteHuman` 删除 human executor 时，现在统一 scrub shared-care child facts：唯一 executor 被删导致 `SharedCareSession` 删除时，保留 active pet 的 `PetCareLog` / `PetPottyLog` / `PetHygieneLog` / `PetExpenseLog` / `PetWalkLog` 并清空 `sharedSessionId` 与已删 executor；多 executor 时 session 保留，child executor metadata 同步收敛到 survivor。实现拆入 `PhysicalDeletionService+SharedCareScrubbing.swift`，未增长 oversized baseline 文件。新增入口族红测覆盖五类 child facts 的 detach/scrub 两路径。验证 PASS：修复前新增红测失败；修复后 `scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests` PASS（9 tests）、`scripts/dev-check-changed.sh` PASS、`scripts/audit-architecture-boundaries.sh --all` PASS、`scripts/audit-derived-state-lifecycle.sh --all` exit 0（仅既有 baseline warnings）、`scripts/audit-economy-boundaries.sh --all` PASS、`git diff --check` PASS。按用户要求未推送/未跑 CI；误触发的 docs-only CI run `27510604402` 已取消。Domain 保持 🟢，不得 🏁；后续需用户批准 push/CI 后再开首发可达面纯复审。 |
| 2026-06-14 | Domain 首发可达面纯对抗复审（`3cc140333` 后） | 本轮纯复审会话（Codex，2026-06-14，用户要求“Domain 纯对抗复审会话(首发可达面)”）明确排除 CloudSync remote/live apply：主容器三层配置均 `cloudKitDatabase: .none`，`OnlineFeatureGate.allows(.onlineCollaboration)` 恒 false，remote/live apply 继续按 `docs/cloud-sync-todo.md` 推 1.x。首发可达面结论：P0=0 / P1=1 / P2=0，Domain 保持 🟢，不得 🏁；已登记 TFU-20260614-016。P1：`PhysicalDeletionService.deleteHuman` 只 scrub shared-care child facts；普通 active pet facts（care/potty/hygiene/health/walk/weight/food/pet expense）保留被删 human 的 `executorId`，同时 `deleteHumanScopedRows` 删除 `actorId == humanId` 的 `CareLedgerEvent`。结果是 active pet fact 留存但 ledger 消失；`CareLedgerBackfillService.backfill` 又会从 retained `log.executorId` 把 deleted human id 回填到 ledger，备份编码也会导出这些 executor id。验证：`scripts/audit-economy-boundaries.sh --all` PASS（805 files）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（805 files，仅 review warnings）；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests -only-testing:OhanaTests/CoconutWalletServiceTests -only-testing:OhanaTests/SharedPetActionRecorderTests` PASS（XCTest 16 + Swift Testing 34）。 |
| 2026-06-14 | Domain TFU-20260614-016 retained executor no-CI 修复 | Codex repair session 按用户要求“修复，不ci”本地修复首发可达面 P1。`PhysicalDeletionService.deleteHuman` 现在先维护 shared-care session，再对 retained active pet fact keys 做统一兜底：普通 active pet care/potty/hygiene/health/walk/weight/food/pet expense facts 保留但清 deleted human executor；matching `CareLedgerEvent` 与 pet-owned `CoconutLedgerEntry` 保留，actor 有 retained survivor executor 时回落到 survivor，没有 survivor 时收敛为 unknown/nil，避免删除 human 后 active fact/ledger/backfill/backup 重新出现 deleted human id。新增红测覆盖普通 fact 入口族、shared-only-executor ledger、shared survivor ledger、backfill 幂等与 backup 导出。验证：新增红测先红（23 issues）；修复后 `scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests` PASS（10 tests，格式化后复跑仍 PASS）；`scripts/test-simulator.sh -only-testing:OhanaTests/CareLedgerBackfillActorTests -only-testing:OhanaTests/CoconutWalletServiceTests -only-testing:OhanaTests/SharedPetActionRecorderTests` PASS（XCTest 16 + Swift Testing 31）；`scripts/dev-check-changed.sh` PASS（SwiftFormat checked 2 Swift files, 0 formatted；changed-file audits PASS，提示已用 targeted tests 覆盖）；`scripts/audit-economy-boundaries.sh --all` PASS（805 files）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（805 files，仅既有 review warnings）；`scripts/audit-architecture-boundaries.sh --all` PASS；`git diff --check` PASS。按用户要求未跑 CI、未推送、未开最终纯复审；Domain 保持 🟢，不得 🏁。 |
| 2026-06-14 | Domain 首发可达面继续纯对抗复审（本会话扩展结论） | Codex 纯复审继续横扫本地首发可达面；仍明确排除 CloudSync remote/live apply（`cloudKitDatabase: .none`，1.x 再审）与 PlantFeatureGate 关闭路径。最终结论：P0=0 / P1=3 / P2=3，Domain 保持 🟢，不得 🏁。P1：① TFU-20260614-017，`PetMilestoneCommandService.deleteMilestone` 删除 milestone fact 后留下 matching `CareLedgerEvent(legacyModelName: "PetMilestone")`，既有测试断言 orphan ledger 为正确；② TFU-20260614-018，existing deceased explicit executor 被 `EconomyRewardOwnerResolver.rewardHuman` 当作 unresolved 后回落 active human，`CareCompletionChokepointCharacterizationTests` 还保护该反宪法行为；③ TFU-20260614-019，D7 纪念只读未落服务层硬门，Pet/Human feature hub 可打开 memorial 写入型目的地，文档/照片/保险/里程碑、人类体重/运动/笔记/健康/心愿单等 command service 可对去世成员写事实或编辑资料。P2：Calendar 历史补完成 dirty timestamp 用历史事实日；Reminder dedupe 裸 delete 不写 tombstone；upload-pipeline/registered 本地 fact 创建缺统一 dirty 标记（PetExpenseLog/PetWeightLog/PetHealthLog/Event/HumanWeightLog 等代表入口）。验证：`scripts/audit-economy-boundaries.sh --all` PASS；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（仅既有 review warnings，含 Reminder dedupe）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests` PASS（21 tests，但其中多项测试断言离世 executor fallback 为正确，故作为反证而非通过证据）；单用例 Swift Testing filter 命中 0 tests，不作为证据。 |
| 2026-06-14 | Domain 按更新宪法 v1.5 重跑首发可达面复审 | Codex 纯复审按新 D7/G4.1 重判上一轮发现：D7 已改为“照护只读，回忆可写”，G4.1 明确 active target + missing/deleted/unwritable executor 不得丢 fact，可 fallback 到可写 active human。结论仍为 P0=0 / P1=3 / P2=3，Domain 保持 🟢，不得 🏁，但 P1 口径已修正。P1：① TFU-20260614-017 仍成立，PetMilestone 纪念日内容可写不代表可留下 orphan `CareLedgerEvent`；② TFU-20260614-018 从“禁止 fallback”改判为“fallback 后 fact/ledger/reward effective actor 不一致”，现有测试只查 wallet 不查 ledger actor；③ TFU-20260614-019 从“memorial 全只读”改判为“memorial-safe 内容与 active-only 派生入口未分流”，PetPhotoAlbum/纯悼念内容可写，但 expense/document cost/insurance/medication/moment/milestone/human health/workout/wishlist/note reminder 等入口不得对 deceased target 写 ledger/reward/reminder/health trend。P2：Calendar 历史补完成 dirty timestamp 用历史事实日；Reminder dedupe 裸 delete 不写 tombstone；upload-pipeline/registered 本地 fact 创建缺统一 `markModified`。验证：`scripts/audit-economy-boundaries.sh --all` PASS；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（review warnings）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests` PASS（21 tests，但覆盖缺 actor 一致性断言）；`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（177 tests，PetMilestone 仍断言 orphan ledger 为正确）。 |
| 2026-06-15 | Domain 第二横切根因熔断裁定（总览） | **总览自纠 + 熔断**：前一条根因分析（"首发可达面接近收口、80% 是 CloudSync"）基于只读 TFU-009~014 片段，错。完整对账日志显示：CloudSync 推迟后首发可达面**自身**持续发现 TFU-015~019 + 20260615-001 至少 6 个本地 P1，当前门禁仍红（TFU-20260615-002）。聚类后这些是**第二个横切根因**：成员状态变化（删除/离世）后的**引用完整性 + 写入门**靠各 command 约定判断、无统一服务层硬门（TFU-019 明示"D7 只读未落服务层，各 feature command 可对去世成员写入"）。与删除级联（已用注册矩阵封死）、care-executor 同构。**熔断失效根因**：归因责任悬空——无记忆复审只看本轮、修复只管修、总览（我）该跨轮聚类却读片段没做；line 319 单轮 017/018/019 三表现本应触发"单轮≥3"但因表面不同未被抽象为同根因。**裁定**：① 立即熔断，停止 015→001 逐条修复，转架构——建统一「成员生命周期门」（active/deceased/deleted 服务层硬门），所有 feature command 对成员写入/派生强制过它，配审计禁绕过；② 新增手册「跨轮强制归因」铁律（复审出 P1 前须读总账历史、按根因层聚类、历史根因第3次复现即熔断）。Domain 🏁 前必须做完此架构封死 |
| 2026-06-15 | Domain Feeding plan/stock P1 修复 + 修复会话复审 | Codex repair session 修复 TFU-20260615-001：Feeding 计划/粮仓/断粮提醒/自动投喂维护命令族接入 active-only lifecycle 门，粮仓费用 payer 写入 effective actor，QuickFeed revision 发布消费 no-op/didChange，UI payer menu 过滤已故 human；修复会话复审未在 Feeding 首发可达面发现新的 P0/P1。验证通过：ManualFeedCommandTests 17、HomeCommandExecutorTests 182、Domain 聚焦组 58、dev-check、fixture tests、economy --all、derived --all（仅既有全库 review warnings）、git diff --check。`scripts/module-exit-gate.sh` 仍因 SharedPetActionRecorder shared walk secondary executor / backup round-trip 测试失败而红，已登记 TFU-20260615-002；本会话不是纯复审且门禁/CI 未全绿，Domain 保持 🟢，不得 🏁。 |
| 2026-06-15 | Domain TFU-20260615-002 shared walk secondary executor 修复 | Codex repair session 修复 module gate blocker：`SharedPetActionRecorder` 的 shared walk 写入现在保留 participant `executorIds` 到 `SharedCareSession` 与 `PetWalkLog`，同时继续以 resolved effective actor 写 primary `executorId`、ledger actor 与 reward owner；备份 DTO 原有 `executorIdsRaw` round-trip 随上游存储恢复。同步修正旧 CareEventService happy-path 测试，插入真实 Human executor，避免测试继续保护无效 `"human-1"` actor。验证 PASS：`scripts/test-simulator.sh -only-testing:OhanaTests/SharedPetActionRecorderTests`（25 tests）；`scripts/module-exit-gate.sh`（changed audits、runtime/economy/derived/localization、876 unit tests + 3 UI template tests）。Domain 本地门禁恢复绿；仍未 push/CI，且本会话不是全新纯复审，不能授 🏁。 |
| 2026-06-15 | 立发布门槛、🏁 降级（破复审怪圈） | 产品主人拍板：诊断"一直能扫出问题"是数学必然（bug 空间组合爆炸 + 对抗复审设计目标就是无限发现），把无限过程（复审零发现/🏁）当首发门槛=怪圈制度根源。**改首发门槛为：全局「P0 且首发可达」清零**（崩溃/数据丢失/隐私/核心流程断/经济算错 × 单机核心流程会触发），其余（P1/P2/不可达/边角）登记 backlog 带上线、按版本排期。**🏁 降级**为上线后长期成熟度目标，"核心五模块必须 🏁 才进 Phase 8"作废。已落地：手册新增「发布门槛（Release Bar）」章节 + 改成熟度准则标题、总账头部准则、`task-follow-ups.md` 重定位为上线后 backlog（要求标 severity + reachability）。这同时化解了 write kernel 切片/时机、是否重写等纠结——均同一病（用完美做门槛）。下一步：开"首发门槛扫描"全局列 P0+可达有限清单，清零即冻结首发 |
| 2026-06-15 | write kernel 回归风险 + 重验顺序 | 产品主人问"Phase 0-6.5 是否因 write kernel 重过"。核实：write kernel + member gate（全局重写 ~9600 行）动了 **15+ 已🟢模块**（Economy/Feeding/Members/Home/Health/Calendar/TodayFocus/Walks 等），把逐模块已收敛状态整个推回待验证——这是 big bang 重写"重新打开所有模块"代价的兑现。**当前 main `build-test` CI 红**（编译或单测失败，lint/audits 绿），是回归未收尾的铁证。**裁定**：① 第一步紧急修绿 build-test+推送（全量单测套件绿=行为等价第一层机器证据，Phase1-6.5 测试套件守住表征）；② 重验用**门槛标准**（P0+可达扫描），不是逐模块重新🏁（那回怪圈）；③ kernel 动过的 15 模块列为 Phase 8 首发门槛扫描最高优先区。教训：增量绞杀本可只验成员照护一小块，big bang 让 15 模块全要重验——既成事实，现在收拾 |
| 2026-06-16 | Phase 8 首发门槛扫描（总览） | Codex 总览会话按 Release Bar 对账：当前 `docs/task-follow-ups.md` 只有历史 P0 Done/Closed 项，唯一 Open 旧 P0（TFU-20260612-006 CareLedger read-model migration）经现代码与测试证据重判为 P1 backlog：首发可达但不属于崩溃 / 数据丢失 / 隐私泄漏 / 核心流程完全断 / 经济算错。自动横向回归与发布硬化证据：`scripts/module-exit-gate.sh --full` PASS（933 unit + 3 UI）、`scripts/domain-kernel-fast-gate.sh --audit-only` PASS、`scripts/release-hardening-check.sh --skip-build` PASS、latest CI run `27607807044` PASS。Phase 8 标 🟢*：Open P0+首发可达=0；仍需 Phase 9 真机/RC 端到端人工验收与各 `🟢*` 模块的真实 UI/通知/定位/PIN/周报 smoke。 |
| 2026-06-16 | Phase 9A 真机签名与 dogfooding 入口 | Codex 9A 启动检查：`xcodebuild -list` 确认存在 `Ohana Local Device` scheme；LocalDeviceDebug 使用 Team `567SK733UN`、bundle id `com.guanchen.li.Ohana.LocalDevice`、`Ohana/InfoLocalDevice.plist`、空 local entitlements。`Guanchen’s iPhone`（iOS 27.0）在线。真机构建 PASS：`xcodebuild -project Ohana.xcodeproj -scheme 'Ohana Local Device' -configuration LocalDeviceDebug -destination 'id=00008150-001270342EA3401C' -derivedDataPath .build/DerivedData/phase9a-local-device build CODE_SIGNING_ALLOWED=YES`；安装 PASS：`xcrun devicectl device install app --device 00008150-001270342EA3401C .build/DerivedData/phase9a-local-device/Build/Products/LocalDeviceDebug-iphoneos/Ohana.app`；首次启动因开发者 profile 未信任被 iOS 拒绝，用户信任后复跑 `xcrun devicectl device process launch --device 00008150-001270342EA3401C com.guanchen.li.Ohana.LocalDevice` PASS。9A 状态转 🟡：dogfooding 可开始；统一验收债当前为 101/101 未勾（2026-06-16 track list 校正后新增 Phase 7 合并 smoke 10 项），下一步必须装机验收轮。 |
| 2026-06-17 | 首发 P0 真机覆盖安装 / bootstrap 复验 | Codex 在修复 Home/Oasis/Settings 首帧 P0 后推进真机前置验证：原 `.build/DerivedData/phase9a-local-device` 产品仍因 bundle root `com.apple.FinderInfo` 被 CodeSign 拒绝；按签名污染例外改用 `/tmp/ohana-local-device-p0` DerivedData 后 `Ohana Local Device` 真机构建 PASS，`codesign --verify --deep --strict /tmp/ohana-local-device-p0/Build/Products/LocalDeviceDebug-iphoneos/Ohana.app` PASS，`xcrun devicectl device install app --device 00008150-001270342EA3401C /tmp/ohana-local-device-p0/Build/Products/LocalDeviceDebug-iphoneos/Ohana.app` 覆盖安装 PASS，`xcrun devicectl device process launch --device 00008150-001270342EA3401C com.guanchen.li.Ohana.LocalDevice` PASS，进程列表显示 `/Ohana.app/Ohana` 运行。拉取 `Library/Application Support/ohana-startup-probe.log`：本次启动记录 `bootstrap.appear -> first-frame-yield-complete -> container-start -> container-ready -> services-ready -> payload-set`。物理 UITest 自动化未能运行：标准 Debug bundle 需要个人团队不支持的 Push/iCloud provisioning；`LocalDeviceDebug` 未配置 test targets 的 bundle id / Swift version / Info.plist。结论：覆盖安装 + 启动层 P0 由真机证据推进为通过；首建主人、首宠、Home Oasis FAB、Settings 的手动交互链仍是 P0 最后关闭条件。 |
| 2026-06-17 | 首发 P0 物理 UI smoke + Settings 栈溢出收口 | Codex 继续修复 TFU-20260617-001：补齐 `LocalDeviceDebug` 下 `OhanaTests` / `OhanaUITests` 的签名、bundle id、Swift version 与 generated plist 配置，使物理 XCTest 能在 `com.guanchen.li.Ohana.LocalDevice` 上运行。第一次物理 smoke 已覆盖首建主人、starter gift、Today Focus 建首宠、Home Oasis、FAB 注入、Settings；但在点击 Settings 后 app crash。xcresult crash log 指向 `SettingsView.body.getter -> LazyVStack.init -> Swift runtime generic metadata demangle`，属于设置页首帧巨大 SwiftUI 泛型树在真机 Debug/XCTest 下触发 stack guard，而不是普通数据等待。处理：移除设置页首帧 DEBUG 开发工具区；进一步将 `SettingsView.body` 的 inline 巨大 `LazyVStack` 拆入 `SettingsView+MainSections.swift`，在 Header/Data/CloudSync/Preferences/Privacy/Notifications/Backup/About/Reset 等段边界用 `AnyView` 做类型擦除，并把 reset alerts 移到 Settings 根视图，避免列表首帧展开整棵设置树。验证：`git diff --check -- Ohana/Features/Settings/Views/SettingsView.swift Ohana/Features/Settings/Views/SettingsView+MainSections.swift` PASS；物理设备 `Guanchen’s iPhone` 运行 `xcodebuild test -allowProvisioningUpdates -project Ohana.xcodeproj -scheme Ohana -configuration LocalDeviceDebug -destination 'id=00008150-001270342EA3401C' -derivedDataPath /tmp/ohana-device-ui-p0-localconfig -only-testing:OhanaUITests/OhanaUITests/testFirstReleaseReachableHomeOasisAndSettingsSmoke CODE_SIGNING_ALLOWED=YES` PASS（1 UI test，46.921s，xcresult `/tmp/ohana-device-ui-p0-localconfig/Logs/Test/Test-Ohana-2026.06.17_13-20-42-+0200.xcresult`）。结论：覆盖安装启动层、首建主人、首建宠、Home Oasis Lv0 -> Lv1 注入、Settings 可打开这条首发可达 P0 链路已由真机自动化清零；剩余真实 UI / 文案 / 通知 / 长语言抽查继续按 `docs/planning/gap-acceptance-track-list.md` 作为非 P0 验收债推进。 |
| 2026-06-17 | Route first-frame 基础设施小收口 | Codex 按 P0 后续裁定补轻量结构件：新增 `RouteFirstFrameDeferredLoad`，让新 route/data container 默认获得“首帧先呈现、下一 main-queue turn 后加载、revision token 可合并刷新、消失取消任务”的薄组件；更新 route-first-frame good fixture 使用该组件，保留 bad fixture 捕捉宽 `@Query` 与首帧 sync fetch；新增 `scripts/report-route-first-frame-inventory.sh` 与 `docs/planning/route-first-frame-inventory.md`，inventory 由脚本生成，不手写旧 116/240 数字。当前快照：845 个 Ohana Swift 文件、64 个 route/data container 文件、全仓 `@Query` 84、全仓 direct fetch 263、route/data direct fetch 13 且 13 个均有 deferred marker、route/data unmarked fetch 0。验证：`scripts/audit-route-first-frame.sh --all` PASS（845 files）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/report-route-first-frame-inventory.sh` 输出与文档 diff 为 0；`scripts/build-debug-fast.sh` PASS。结论：首发 P0 不再依赖手工约定，新挂载面的首帧同步宽查询 / 多表 fetch / 重服务树由 strict audit 与薄组件共同收口；存量非 route `@Query/fetch` 只作为成熟度 inventory，不是首发阻塞。 |
| 2026-06-16 | 检阅 route-first-frame=覆盖边界漏两类卡死 | 核实 Codex 上条收口：基础设施/audit/inventory/CI 真做了，但**收口边界划在"route container 的 direct fetch"，漏了用户四个卡死所在的两类**：① **route container 的首帧 `@Query` 订阅**——`audit-route-first-frame.sh Ohana/Features/Settings/SettingsRouteContainer.swift` 报 passed，但 `AppAccountSwitcherRouteContainer`(:5/6/7)有 3 个裸首帧 `@Query`(Pet/Human/electronicPets)未被 audit/inventory 捕捉（inventory 只数 direct fetch，@Query 被归"非 route 存量不阻塞"，但它就在 route container 首帧）；② **首帧路径上的 service fetch**——`OasisCritterEconomyService.swift:43` 同步 `context.fetch(Human)`，切 oasis 首帧被调，归"非 route 存量"漏掉。结果 `audit --all` 假绿。零卡死入口迁移到 `RouteFirstFrameDeferredLoad`。**裁定**：① audit 必须覆盖 route container 首帧 @Query + 首帧路径 service fetch（用 AppAccountSwitcher / Oasis 当真实测试，audit 先对它们报红才可信）；② 用基础设施真迁移四个卡死入口；③ 验收=**真机过建人/建宠/设置/oasis 不卡死**（代码层假绿不算修，UI 卡死终验只能真机，见 track list Settings 三轮"自动验证过、真机仍闪退"）|
| 2026-06-17 | Route first-frame 假绿修复 + 首批真实消费者迁移 | Codex 按用户铁证重修：先用真实文件证明旧 audit 假绿已变红：`scripts/audit-route-first-frame.sh Ohana/Features/Settings/SettingsRouteContainer.swift` 对 `AppAccountSwitcherRouteContainer` 的 3 个首帧 `@Query` 报 `route-first-frame-query`；`scripts/audit-route-first-frame.sh Ohana/Features/Oasis/OasisRewardCommandExecutor.swift` 对 `rewards.currentHumanBalance(context:)` 报 `route-first-frame-service-fetch`。随后迁移真实消费者：`AppAccountSwitcherRouteContainer` 改用 `RouteFirstFrameDeferredLoad` 首帧后加载 Pet/Human/OasisElectronicPet；`AppSettingsSheetRouteContainer` 改用 `RouteFirstFrameDeferredMount` 先呈现轻壳，再用 `RouteFirstFrameDeferredLoad` 加载 Household/Pet/Human/OasisElectronicPet；`OasisRewardCommandExecutor.makeCritterSnapshots` 改为消费调用方传入的 active balance，不再在 render snapshot builder 内同步 fetch 当前 Human。审计升级为三件事：① route/data `@Query` 按 `docs/governance/manifests/route-first-frame-baseline.json` 文件级 ratchet，新增/超额即红；② route/data unmarked direct fetch 零容忍；③ first-frame service fetch bypass（`rewards.currentHumanBalance(context:)`）零容忍。更新 bad/good fixture，bad 同时覆盖 `@Query` / sync fetch / service fetch 三类真实形态。补充 UI smoke 发现 frozen Oasis 首帧树卡缺 `oasis-tree-level-control` 且一度重复命中，已把可访问性锚点收在 frozen tree card；Settings 点击后曾长时间等不到 `settings-screen`，已用轻壳 mount 收口。当前 inventory：845 个 Ohana Swift 文件、64 个 route/data 文件、route/data `@Query` 79（46 文件 baseline allowance=79）、route/data direct fetch 13/13 deferred、unmarked fetch 0、service fetch bypass 0。验证：真实 Settings/Oasis 文件 red -> green；`scripts/audit-route-first-frame.sh --all` PASS（845 files）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/dev-check-changed.sh` PASS；`scripts/report-route-first-frame-inventory.sh` 与 `docs/planning/route-first-frame-inventory.md` diff=0；`scripts/build-debug-fast.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaUITests/OhanaUITests/testFirstReleaseReachableHomeOasisAndSettingsSmoke` PASS（1 UI test，40.276s，覆盖首建主人、starter gift fallback、Today Focus 建首宠、Home Oasis、FAB 注入、Settings 打开，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.17_14-03-11-+0200.xcresult`）；`xcodebuild test -allowProvisioningUpdates -project Ohana.xcodeproj -scheme Ohana -configuration LocalDeviceDebug -destination 'id=00008150-001270342EA3401C' -derivedDataPath /tmp/ohana-device-route-first-frame-p0 -only-testing:OhanaUITests/OhanaUITests/testFirstReleaseReachableHomeOasisAndSettingsSmoke CODE_SIGNING_ALLOWED=YES` PASS（真机 1 UI test，53.049s，xcresult `/tmp/ohana-device-route-first-frame-p0/Logs/Test/Test-Ohana-2026.06.17_14-07-40-+0200.xcresult`）。结论：这次不再只建脚手架，两个已知 P0 卡死形态已接入消费者和审计；route-first-frame 假绿修复后的首发 UI 链路已由模拟器和真机同路径复验通过。 |
| 2026-06-17 | Feeding UI smoke 复现与回归 | Codex 按用户要求不再让真机逐项探测，新增并运行喂食端到端 UI smoke。初跑在 `home-quick-action-menu-feed` 点击后复现主线程 busy 30s；后续样本又定位到 `CoconutRewardFeedbackOverlay.body` / feedback enqueue / haptic 循环。修复三类根因：QuickFeed settings-only sheet 保存可见 50g 时不再因隐藏 toggle 写成 0g，并强制 route snapshot 刷新；喂食后 Today Focus completion、Home revision、Growth sync 改为下一帧 / 延迟合并，避免把照护事实、奖励、Oasis、Home 刷新塞进同一手指帧；`ReadModelRevisionCenter.coconutRewardEvents` 从 replaying `@Published` 改为 one-shot `PassthroughSubject`，overlay 按 event id 去重并延后 haptic，避免重订阅重放同一奖励事件。验证：`scripts/test-simulator.sh -only-testing:OhanaUITests/OhanaUITests/testFeedingManualPlanAndHomeQuickActionSmoke` PASS（iPhone 17 simulator，1 UI test，70.422s，覆盖 fresh 首建主人、starter gift、Today Focus 建宠、进入 Feed、保存 manual 默认、首页 quick feed、连续打卡确认、切 plan mode、首页 plan quick feed，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.17_18-44-33-+0200.xcresult`）。真机二次确认仍按 `docs/planning/gap-acceptance-track-list.md` 的 Feeding / Food Log 项继续。 |
| 2026-06-17 | 成员永久删除 UI 卡死修复 | 用户真机报告“点击彻底删除 xxx 后卡死”。本轮按首帧/手指帧根因收口，而不是只等删除更久：永久删除入口先关闭当前 sheet/detail/overlay，再延迟提交物理删除级联；新增 `DeferredDomainCommandQueue.destructiveRouteDismissDelayMilliseconds`，Pet Basic Info、宠物卡背、CrewRoster、人类详情/基础资料/ExpandedHumanFeatures 删除入口统一使用；宠物删除确认移除自动键盘 focus，避免确认 sheet 首帧额外拉起键盘系统；补充 `pet-danger-delete-action`、`pet-delete-confirm-*`、`pet-basic-info-screen`、feature hub tile 与 Home expanded shortcut identifiers，并在 Pet 全功能 Archive 区补 Profile 入口，使回归走真实可达路径。验证：`scripts/test-simulator.sh -only-testing:OhanaUITests/OhanaUITests/testPetPermanentDeleteFromBasicInfoSmoke` PASS（iPhone 17 simulator，1 UI test，52.207s，覆盖首建主人、starter gift、首建宠、进入基础资料、点击彻底删除、输入宠物名确认、回首页可响应且宠物卡消失，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.17_22-44-42-+0200.xcresult`）。真机同一路径仍需用户二次确认后在 GAP-2 勾选。 |
| 2026-06-24 | Domain PetMilestone legacy ledger P1 guard 补强 | 针对 TFU-20260614-017 重新核当前代码：新建 `PetMilestone` 已不再写 `CareLedgerEvent`，但仍需要保护旧数据中已存在的 `legacyModelName == "PetMilestone"` ledger。补强 `HomeCommandExecutorTests.petMilestoneCommandServiceCreatesRewardsAndDeletesLedger`：手动构造 matching legacy ledger 和 unrelated legacy ledger，删除 milestone 后断言返回 matching ledger id、`PetMilestone` 与 matching `CareLedgerEvent` 均写 deletion tombstone，unrelated ledger 保留。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（183 tests，iPhone 17 simulator）；`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests` PASS（12 tests，iPhone 17 simulator）；`scripts/audit-economy-boundaries.sh --all` PASS（846 files）；`scripts/audit-derived-state-lifecycle.sh --all` exit 0（846 files，仅既有 review warnings）；`scripts/tests/run-audit-fixture-tests.sh` PASS。本轮只推进该 P1 的本地 guard 和证据，尚未跑 module exit gate / push / CI / 独立纯复审，Domain 不能因此标 🏁。 |
| 2026-06-24 | Domain notification scheduler injection P1 子项收口 | 针对 TFU-20260612-014 的通知 side-effect 边界，新增 `ReminderNotificationSchedulerRegistry` 并保留 `OhanaNotifications` 兼容壳；`ReminderCompletionService`、`CareEventServiceDependencies`、`CareEventService+Feed` planned completion、`PhysicalDeletionService` 级联删除、`PetActivityRecordCleanupService`、schedule/rehydrate dispatcher 均改为消费注入的 `ReminderNotificationScheduling`，`AppServices` 显式把 live `NotificationManager` 串入服务栈。补强 `OhanaNotificationsSchedulingTests`：skip、physical deletion、schedule delete dispatcher 均通过 fake scheduler 验证取消路径。验证：`rg -n "OhanaNotifications\\.current" Ohana/Domain` 无匹配；`git diff --check` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaNotificationsSchedulingTests` PASS（12 tests，iPhone 17 simulator）。本轮只关闭该 P1 子项；TFU-20260612-014 仍等 localization/concrete adapter cleanup、push/CI 与 fresh pure review。 |
| 2026-06-24 | Domain architecture/localization P1 本地硬门复核 | 针对 TFU-20260612-014 刷新当前 worktree 本地硬门：`rg -n "^import SwiftUI" Ohana/Domain -g '*.swift'` 无匹配，`rg -n "OhanaNotifications\\.current" Ohana/Domain -g '*.swift'` 无匹配，`scripts/audit-architecture-boundaries.sh --all` PASS，`scripts/audit-localization-coverage.sh` PASS。结论：本地 architecture / notification injection / localization gates 已有当前证据，但该 TFU 自身 close condition 仍包含 push/CI/fresh pure review，所以保持 Open。 |
| 2026-06-24 | Members human memorial read-only P1 本地证据复核 | 针对 TFU-20260612-019 核当前 worktree：人类详情与基础资料页共享 `HumanLifecycleDangerZone`，标记/撤销离世经 `MemberLifecycleCommandService`，人类 profile edit 经 `MemberProfileCommandService.updateHuman` 和 `MemberLifecycleGate`，privacy / passcode / settings / presentation / economy 写入对 deceased human 均 fail closed；删除保留为显式 lifecycle/data-removal 操作。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（50 tests，iPhone 17 simulator），覆盖 deceased profile edit deny、human memorial note with reminder、presentation/security/economy/settings writes deny、family task mutation deny 等。该 TFU 仍需 broader gate、push/CI 和 fresh pure review 才能关闭。 |
| 2026-06-24 | Domain actor fallback consistency P1 Home guard 补强 | 针对 TFU-20260614-018，把 `HomeCommandExecutorTests` 的 Home quick-action fallback 表征从“active wallet 有奖励、deceased/missing wallet 没写”提升到 fact/ledger actor 一致性：missing executor、passed-away executor、deceased executor quick care、deceased executor quick potty 四条代表路径现在都 fetch 对应 `PetCareLog` / `PetPottyLog` 与 `CareLedgerEvent`，断言 fact `executorId` 和 ledger `actorId` 均落到 fallback active human。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（183 tests，iPhone 17 simulator）。这只是 Home 代表路径 guard，non-Home 入口族、module exit、push/CI 与 fresh pure review 仍未完成。 |
| 2026-06-24 | Domain human deletion scrub P1 本地验证刷新 | 针对 TFU-20260614-015 / TFU-20260614-016 重跑 `PhysicalDeletionServiceTests`，确认删除 human executor 后 shared-care child facts 与 retained active pet facts 的本地修复仍绿：唯一 executor session 删除时 child facts 保留并清空 `sharedSessionId` / executor；多 executor session 保留时 child metadata 收敛到 survivor；普通 care/potty/hygiene/health/walk/weight/food/pet expense facts、matching care/coconut ledger、backfill 和 backup projection 均不得回灌 deleted human id。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/PhysicalDeletionServiceTests` PASS（12 tests，iPhone 17 simulator）。两条 TFU 仍需 push/CI 与 fresh pure review 才能关闭。 |
| 2026-06-24 | Economy Insurance expense ledger 旧 P1 状态纠偏 | TFU-20260614-006 的剩余 Insurance expense ledger P1 已由历史修复 `92133da2a` 关闭，但 `task-follow-ups.md` 条目仍保持 Open。本轮核当前实现：Insurance 保费与报销均通过 `ExpenseCommandService.recordPetExpense(... awardsReward: false)`，没有裸写 `PetExpenseLog`；`InsuranceExpenseLedgerTests` 覆盖自动保费、批准理赔创建、理赔状态批准幂等。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/InsuranceExpenseLedgerTests` PASS（3 tests，iPhone 17 simulator）。已将 TFU-20260614-006 改为 Done，并保留原 CI run `27505314629` 与 Economy P0=0/P1=0/P2=0 终审证据。 |
| 2026-06-24 | Economy no-op / derived effect 旧 P1 状态纠偏 | TFU-20260613-012 / 013 / 014 的 no-op、command-layer side effect、derived effect 漏洞已被后续 Economy 最终纯复审与 CI 关闭，但 `task-follow-ups.md` 仍保持 Open。本轮刷新代表性覆盖：`CareCompletionChokepointCharacterizationTests`、`CareDerivationExecutorSuccessCharacterizationTests`、`RecurringFindingsRepairTests`、`ReminderActionCoordinatorTests`、`CatCareCommandTests` 覆盖 deceased target no-op、missing/deceased executor fallback、Calendar/notification completion、manual feed stock、medication dose、cat care、ledger/reward/revision 边界。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests -only-testing:OhanaTests/CareDerivationExecutorSuccessCharacterizationTests -only-testing:OhanaTests/RecurringFindingsRepairTests -only-testing:OhanaTests/ReminderActionCoordinatorTests -only-testing:OhanaTests/CatCareCommandTests` PASS（58 tests，iPhone 17 simulator）。已将三条 TFU 改为 Done，并保留 Economy final P0=0/P1=0/P2=0 终审证据。 |
| 2026-06-24 | Memorial active-only / memorial-safe 分流 P1 本地证据复核 | 针对 TFU-20260614-019 核当前 worktree：`MemberLifecycleGateTests` 覆盖 feature hub 只暴露 memorial-safe destination、deceased pet feed/stock/water/care-plan/walk/derived wrapper no-op、deceased human note with reminder 写 memorial content only、deceased pet memorial calendar/photo 允许但无 care/economy derivation；`RecurringFindingsRepairTests` 覆盖 active-only pet/human commands no-op、deceased pet memorial moment writes photo only、human medication active-only no-op。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（50 tests，iPhone 17 simulator）；Economy representative suite PASS（58 tests，iPhone 17 simulator）。该 TFU 已推进为“本地 command/UI membrane evidence verified”，仍需 broader gate、push/CI 与 fresh pure review 才能关闭。 |
| 2026-06-24 | 标准 Ohana 真机签名能力复验 | 有付费 Developer Program 后，标准 `Ohana` scheme 已不再被 Personal Team 能力限制挡住：`xcrun devicectl list devices` 显示 `Guanchen's iPhone` 可用；`xcodebuild -allowProvisioningUpdates -project Ohana.xcodeproj -scheme Ohana -configuration Debug -destination 'id=4CDF4314-5293-5DFE-AD4E-B510473B1367' -derivedDataPath /tmp/ohana-device-capability-debug build CODE_SIGNING_ALLOWED=YES` PASS。签名产物使用 `Apple Development: guanchen.li.119@gmail.com (62Y338Q9SK)`、profile `iOS Team Provisioning Profile: com.guanchen.li.Ohana`，entitlements 包含 `application-identifier = 567SK733UN.com.guanchen.li.Ohana`、`aps-environment = development`、`iCloud.com.guanchen.li.Ohana`、`CloudDocuments`、`CloudKit`。结论：当前最该推进 Phase 9 真机验收和 App Store Connect/TestFlight 闭环；GAP-6/GAP-9 仍因真实通知到达、Focus/DND、通知 action、memorial 可见体验未跑，保持 P1 Open。 |
| 2026-06-24 | CareEventService planned-care lifecycle-denied P1 guard | 针对 TFU-20260612-008 增补 `careEventServicePlannedCareDeniedByLifecycleWritesNoDerivedSideEffects`：构造已离世 pet + feed/water reminder，断言 planned feed/water 返回 nil，且不写 `PetCareLog`、`CareLedgerEvent`、reminder completed/status、occurrence completion、family-task completion、notification cancellation、first-meal state 或 economy award。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests` PASS（135 tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_01-35-31-+0200.xcresult`）。TFU-20260612-008 仍保持 Open，因为剩余 service failure / recovery edge tests 还未全部补齐。 |
| 2026-06-24 | CareEventService planned-care schedule-boundary denial guard | 针对 TFU-20260612-008 继续扩展 `careEventServicePlannedCareDeniedByLifecycleWritesNoDerivedSideEffects`，补第二类 no-write 失败路径：active pet 的 care fact 授权可成立，但 planned reminder/event 的 schedule subject 指向 missing human，导致 schedule mutation 授权失败；断言 feed/water completion 返回 nil，且不写 `PetCareLog`、`CareLedgerEvent`、reminder completed/status、event occurrence completion、family-task completion、notification cancellation、first-meal state 或 economy award。先尝试新增独立 Swift Testing 用例，但精确 `-only-testing` 命中 0，因此已改为并入现有可发现测试，避免假绿。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests` PASS（135 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_01-49-42-+0200.xcresult`）。TFU-20260612-008 仍保持 Open，因为剩余 service failure / recovery edge tests 还未全部补齐。 |
| 2026-06-24 | 标准 Ohana 真机 UI runner 前置验证 | 针对 Developer Program 解锁后的 GAP-6/GAP-9 前置面，尝试运行 `xcodebuild test -allowProvisioningUpdates -project Ohana.xcodeproj -scheme Ohana -configuration Debug -destination 'id=4CDF4314-5293-5DFE-AD4E-B510473B1367' -derivedDataPath /tmp/ohana-device-standard-ui-smoke -only-testing:OhanaUITests/OhanaUITests/testFirstReleaseReachableHomeOasisAndSettingsSmoke CODE_SIGNING_ALLOWED=YES`。结果：`Ohana.app` 与 `OhanaUITests-Runner.app` 均完成 iPhoneOS 编译/签名；app entitlements 复核包含 `application-identifier = 567SK733UN.com.guanchen.li.Ohana`、`aps-environment = development`、`iCloud.com.guanchen.li.Ohana`。XCTest launch 未开始，Xcode preflight 报 `Unlock Guanchen's iPhone to Continue`，等待后手动取消，xcresult `/tmp/ohana-device-standard-ui-smoke/Logs/Test/Test-Ohana-2026.06.24_01-40-47-+0200.xcresult` 记录 `Testing was canceled`。结论：标准 paid-team scheme 已到真机 UI runner 签名阶段；GAP-6/GAP-9 的通知到达、action、Focus/DND、纪念模式可见行为仍需解锁真机后继续验收，P1 保持 Open。 |
| 2026-06-24 | CareEventService direct lifecycle-denied P1 closure | 针对 TFU-20260612-008 新增 `careEventServiceDirectPetFactsDeniedByLifecycleWriteNoDerivedSideEffects`：构造已离世 pet + valid executor，直接调用 `recordCareFact`（含 linked potty）、`recordPottyFact`、`recordHygieneFact`、`recordHealthFact`，断言全部返回 no-op / zero reward，且不持久化 `PetCareLog`、`PetPottyLog`、`PetHygieneLog`、`PetHealthLog`、`CareLedgerEvent`，不触发 economy award / first-meal / quick-action reminder handoff。验证：`git diff --check -- OhanaTests/OhanaTests.swift` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests` PASS（136 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-05-24-+0200.xcresult`）。已将 TFU-20260612-008 改为 Done；未来若发现具体 untested care failure path，应开更窄的新 TFU。 |
| 2026-06-24 | CloudSync delete-dispatch stale P1 去重 | 复核 TFU-20260614-011 / 012 / 013：011 的远端 Event / SharedCareSession / pet-scoped fact delete dispatcher 原始缺口已由 2026-06-14 修复并通过 CloudSync、PhysicalDeletion、SharedCare、notification、CareLedger gates；后续复审发现的 feeding stock-reminder 缺口已迁入 012。012 的 feeding `PetCareLog` / `PetFoodRecord` remote delete rebuild stock reminder 原始缺口也已由 2026-06-14 修复并通过 CloudSync tests / changed-file gates；后续复审发现的 Gacha/Shop upload + human-delete lifecycle 缺口已迁入 013。为避免重复计数，本轮将 011/012 改为 Done（stale tracker correction），保留 TFU-20260614-013 作为当前活跃 CloudSync/Gacha/Shop P1。验证：文档证据复核 + `git diff --check -- docs/task-follow-ups.md docs/testing-progress.md` PASS；本轮无代码改动。 |
| 2026-06-24 | Domain actor fallback consistency P1 non-Home 证据刷新 | 针对 TFU-20260614-018 重跑 non-Home chokepoint：`CareCompletionChokepointCharacterizationTests` 覆盖 direct manual feed、shared watering、medication dose、calendar completion、walk、pet weight、pet health 等入口，在 missing/deceased explicit executor 下断言 active target fact 保留，同时 fact `executorId`、`CareLedgerEvent.actorId`、wallet、budget、Oasis/feedback、reminder/event metadata 收敛到 fallback active human；deceased target 保持 no-op。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests` PASS（24 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-11-00-+0200.xcresult`）。TFU-018 状态改为 Home + non-Home 本地 guard 已验证，仍需 module exit、push/CI 和 fresh pure review。 |
| 2026-06-24 | Domain pure-review parent P1 去重 | TFU-20260614-009 是 2026-06-14 Domain 首次纯审父级条目；原始 P1/P2 修复已在当日修复会话完成并通过 local targeted suites、fixture tests 与 module-exit gate，后续纯审发现的风险已经接力登记到 TFU-20260614-010/011/012/013/014。本轮将 009 改为 Done（stale tracker correction），避免与当前活跃 successor P1 重复计数；Domain 成熟态仍由剩余 Open P1、push/CI、真机/纯审 gates 决定。验证：文档证据复核 + `scripts/test-simulator.sh -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests` PASS；后续继续以 013/014 等 successor 条目为准。 |
| 2026-06-24 | Domain backup error localization P1 小切片 | 针对 TFU-20260612-014 的 Domain user-visible string cleanup，`BackupError` 与 `AutomaticBackupFileStoreError` 已改为通过 `localizedMessage(l:)` / `L10n.current.tr` 输出 zh/en/de 错误文案，不再从 `LocalizedError.errorDescription` 直接返回硬编码中文；新增 `LocalizationTests.domainBackupErrorsResolveLocalizedCopy` 与 `automaticBackupErrorsResolveLocalizedCopy`。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/LocalizationTests` PASS（13 tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-14-48-+0200.xcresult`）。TFU-014 保持 Open：CarePlan calendar titles、PetMedicationDoseLogging titles、CarePlanOverdueStatus labels、shared-care reward titles 等 Domain generated copy 仍需后续本地化、push/CI 与 fresh pure review。 |
| 2026-06-24 | CarePlan calendar generated titles localization P1 小切片 | 继续推进 TFU-20260612-014：`CarePlanCalendarSync` 的默认护理计划、breed-risk、换水、滤芯、换猫砂、铲屎和陪玩 calendar `Event.title` 现在通过 `L10n.current` 生成，不再直接写中文标题；legacy cleanup 继续匹配旧中文别名以清理既有默认事件。新增 `OhanaTests.carePlanCalendarSyncGeneratesLocalizedDefaultTitles`，覆盖英文 `Bubbles Water change` / `Bubbles Filter check` 与德文 `Bubbles Wasserwechsel` / `Bubbles Filtercheck`，并把既有中文 care-plan 行为测试显式 pin 到 `appLanguage = zh`。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests` PASS（137 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-52-44-+0200.xcresult`）。TFU-014 保持 Open：PetMedicationDoseLogging titles、CarePlanOverdueStatus labels、shared-care reward titles 和剩余 infrastructure adapter cleanup 仍需后续本地化/复审。 |
| 2026-06-24 | Hygiene detail CareLedger value projection P1 小切片 | 针对 TFU-20260612-006 的 CareLedger read-model migration，Hygiene detail 现在在 `PetHygieneDetailDataContainer` 中把 `CareLedgerEvent` 映射为纯值 `PetHygieneLedgerEntry`，`PetHygieneDetailContentView` 渲染路径不再直接过滤 SwiftData ledger object 数组；`PetHygieneLog` 仍只作为显式删除兼容桥。新增 `HomeExpensePreviewStoreTests.petHygieneDetailEntriesFilterSortAndKeepLegacyDeleteId`，覆盖当前宠物过滤、错误 kind/type 排除、倒序排序和 legacy delete id 保留。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（9 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-19-38-+0200.xcresult`）。TFU-006 保持 Open：QuickFeed stock/treat/overview 与 delete/claim 兼容路径仍有 legacy log 依赖，需要后续结构化迁移。 |
| 2026-06-24 | QuickPlay detail CareLedger value projection P1 小切片 | 继续推进 TFU-20260612-006：QuickPlay detail 现在由 `QuickPlayRouteData` 在 route-scoped load 阶段把 `CareLedgerEvent` 映射为纯值 `QuickPlayLedgerEntry`，`QuickPlayDetailSheet` 渲染路径不再直接过滤 SwiftData ledger object 数组；`PetCareLog` 仍只作为 explicit delete compatibility bridge。新增 `HomeExpensePreviewStoreTests.quickPlayDetailEntriesFilterSortAndKeepLegacyDeleteId`，覆盖当前宠物过滤、错误 kind/action 排除、倒序排序和 legacy delete id 保留。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（10 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-22-21-+0200.xcresult`）。TFU-006 保持 Open：QuickFeed stock/treat/overview 与其它 delete/claim 兼容路径仍需后续迁移或明确收口。 |
| 2026-06-24 | QuickWater detail CareLedger value projection P1 小切片 | 继续推进 TFU-20260612-006：QuickWater detail 现在由 `QuickWaterRouteData` 在 route-scoped load 阶段把 watering / water-change / filter-clean `CareLedgerEvent` 映射为纯值 `QuickWaterLedgerEntry`，`QuickWaterDetailSheet` / `QuickWaterRenderSnapshot` 不再接收或过滤 SwiftData ledger object 数组；`PetCareLog` 仍只作为 explicit delete compatibility bridge。新增 `HomeExpensePreviewStoreTests.quickWaterDetailEntriesFilterSortClampAndKeepLegacyDeleteId`，覆盖当前宠物过滤、错误 kind/action 排除、倒序排序、负饮水量归零和 legacy delete id 保留。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（11 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-27-08-+0200.xcresult`）。TFU-006 保持 Open：QuickFeed stock/treat/overview、QuickPotty claim/delete 和其它兼容路径仍需后续迁移或明确收口。 |
| 2026-06-24 | QuickPotty detail CareLedger value projection P1 小切片 | 继续推进 TFU-20260612-006：QuickPotty detail 现在由 `QuickPottyRouteData` 在 route-scoped load 阶段把 potty / litter `CareLedgerEvent` 映射为纯值 `PoopPottyLedgerEntry` / `PoopLitterLedgerEntry`，`QuickPottyDetailSheet` 渲染路径不再直接过滤 SwiftData ledger object 数组；未知共享 potty claim 与显式删除仍保留为 action-time / explicit delete compatibility bridge。新增 `HomeExpensePreviewStoreTests.quickPottyDetailEntriesFilterSortAndKeepLegacyDeleteId` 与 `quickPottyLitterEntriesFilterSortAndKeepLegacyDeleteId`，覆盖当前宠物过滤、错误 kind/action 排除、倒序排序和 legacy delete id 保留。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（13 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-30-22-+0200.xcresult`）。TFU-006 保持 Open：QuickFeed stock/treat/overview 与其它兼容路径仍需后续迁移或明确收口。 |
| 2026-06-24 | QuickFeed detail snapshot CareLedger value projection P1 小切片 | 继续推进 TFU-20260612-006：QuickFeed detail 现在由 `QuickFeedRouteData` / `QuickFeedCommandExecutor.fullFeedingLedgerEntries` 在 route-scoped fetch 后把 feeding `CareLedgerEvent` 映射为纯值 `QuickFeedLedgerEntry`；`QuickFeedDetailSheet`、`QuickFeedDataController`、overview / plan-calendar / treat snapshot stores 和 anti-repeat screen model 不再接收或过滤 SwiftData ledger object 数组。计划日历 auto completion 现在携带 `autoLedgerEntry` 纯值而不是 `CareLedgerEvent`；`PetCareLog` 仍只作为 food-kind/treat-kind enrichment、stock calculator 和 explicit edit/delete compatibility bridge。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests -only-testing:OhanaTests/QuickFeedPlanCalendarSnapshotStoreTests -only-testing:OhanaTests/QuickFeedTreatSnapshotStoreTests -only-testing:OhanaTests/QuickFeedDataControllerTests` PASS（9 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-39-46-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility、moment/media history 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | IslandFoodDashboard CareLedger value projection P1 小切片 | 继续推进 TFU-20260612-006：IslandFoodDashboard 现在由 `IslandFoodDashboardRouteData` 在 route-scoped load 阶段把 feeding `CareLedgerEvent` 映射为纯值 `FoodLedgerEntry`，`IslandFoodDashboardSnapshot.build`、snapshot revisions 和 `IslandFoodDashboardContentView` 不再接收或过滤 SwiftData ledger object 数组；`PetCareLog` 仍只作为 stock calculator compatibility input。新增 `IslandFoodDashboardSnapshotStoreTests.foodLedgerEntriesFilterSortAndClampInvalidRows`，覆盖当前宠物过滤、错误 kind/action 排除、倒序排序和负数量归零。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/IslandFoodDashboardSnapshotStoreTests` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_02-44-22-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility、moment/media history 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | IslandHygieneDashboard CareLedger value projection P1 小切片 | 继续推进 TFU-20260612-006：IslandHygieneDashboard 现在由 route/container 在 SwiftData fetch 后立即把 hygiene / care-maintenance `CareLedgerEvent` 映射为纯值 `HygieneDashboardLedgerEntry`；`IslandHygieneDashboardContentView` 不再接收 `[CareLedgerEvent]`，也不再 import SwiftData 或在 body/computed render path 过滤 SwiftData ledger object 数组。新增 `HomeExpensePreviewStoreTests.islandHygieneDashboardEntriesFilterSortAndKeepCareMaintenanceRows`，覆盖 pet subject、hygiene type、care-maintenance action、非 hygiene care 排除、非 pet subject 排除和倒序排序。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（14 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-31-38-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility、moment/media history 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | PetMedicationDoseLogging generated titles localization P1 小切片 | 继续推进 TFU-20260612-014：`PetMedicationDoseLogging` 的 medication dose `Event.title`、镜像该 title 的 `CareLedgerEvent.note`、以及 `.general` reward title 现在通过 `L10n.current` 生成，不再直接写中文标题/奖励文案。更新 `HomeCommandExecutorTests.petMedicationDoseRewardUsesExecutorBudgetAndCooldownPipeline`，显式 pin `appLanguage = en`，断言英文 event title、ledger note、budget action key，并拒绝旧中文 reward key。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests/petMedicationDoseRewardUsesExecutorBudgetAndCooldownPipeline()` PASS（1 Swift Testing test，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-00-31-+0200.xcresult`）；`scripts/audit-localization-coverage.sh` PASS；`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（因当前大工作区有大量 compiler-surface 改动仍提示 build 建议；本小切片已由 targeted simulator test 覆盖编译与行为）。TFU-014 保持 Open：CarePlanOverdueStatus labels、shared-care reward titles 和剩余 infrastructure adapter cleanup 仍需后续本地化/复审。 |
| 2026-06-24 | Domain reward/shared-care title localization P1 小切片 | 继续推进 TFU-20260612-014：`DomainCareRewardAction.title(pet:)` 改为接收 `L10n` 快照，feed/water/walk/hygiene/health/expense/weight/milestone/Today Focus/potty reward titles 与 `DomainCareRewardQuality` badge 不再直接返回中文硬编码；`QuestManager.awardAction` / `awardSharedCareAction` 复用同一语言快照生成钱包标题、幸运后缀、shared-care pet-name 列表和 human shared-care reward title。新增 `LocalizationTests.domainCareRewardCopyResolvesLocalizedTitles` 覆盖 zh/en/de reward title 与 badge。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/LocalizationTests` PASS（14 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-08-35-+0200.xcresult`）；`scripts/audit-localization-coverage.sh` PASS；`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（因当前大工作区有大量 compiler-surface 改动仍提示 build 建议；本小切片已由 targeted simulator test 覆盖编译与行为）。TFU-014 保持 Open：CarePlanOverdueStatus labels 和剩余 infrastructure adapter cleanup 仍需后续本地化/复审。 |
| 2026-06-24 | Domain overdue/status localization P1 小切片 | 继续推进 TFU-20260612-014：`CarePlanOverdueStatus` 增加 `compactText(l:)`、`localizedTitle(l:)`、`localizedOverdueText(l:)`，`WaterCareCycleStatus` 增加 `compactDueText(l:)`；Home card badge、interaction snapshot 与 expanded quick-action overdue/due 文案改为读这些纯值本地化 helper，不再沿用中文 fallback 状态字符串。新增 `LocalizationTests.carePlanOverdueCopyResolvesLocalizedStatus` 覆盖 zh/en/de overdue 与 water-cycle copy，并扩展 `HomeSnapshotBuilderTests.snapshotDecoratesOverduePetStatus` 断言同一 Home overdue card 可按显式 `L10n` 输出中文/英文。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/LocalizationTests -only-testing:OhanaTests/HomeSnapshotBuilderTests` PASS（35 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-14-26-+0200.xcresult`）。TFU-014 保持 Open：剩余 concrete-adapter cleanup、broader generated-copy review、push/CI 与 fresh pure review。 |
| 2026-06-24 | Domain generated-copy / adapter cleanup P1 收口证据 | 继续推进 TFU-20260612-014：shared multi-pet reward title、Calendar completion general reward title / undo wallet title、CareLedger report fallback/title、AntiRepeat executor label、EntityKind display name now use explicit `L10n` helpers; Domain adapter hard gates are clean. 剩余 CJK 命中为 parser/legacy cleanup matcher 或内部 fallback title，Home/status render paths 已走 localized helper。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/LocalizationTests` PASS（16 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-24-53-+0200.xcresult`）；`scripts/test-simulator.sh -only-testing:OhanaTests/SharedPetActionRecorderTests -only-testing:OhanaTests/QuestManagerBatchAwardTests` PASS（34 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-23-26-+0200.xcresult`）；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/audit-localization-coverage.sh` PASS；`git diff --check` PASS。TFU-014 保持 Open：push/CI 与 fresh pure review 未完成。 |
| 2026-06-24 | Home expanded moment read-model value projection P1 小切片 | 继续推进 TFU-20260612-006：Home expanded “moment/记录” 状态现在由 `HomeReadModelActor` bounded fetch `PetPhotoLog` 后投影成 `HomePetMomentQuickActionEntry`，并通过 `VerticalSolidHomeSourceState.petMomentEntries` / Home signature / `HomeInteractionSnapshot` 驱动；`ExpandedQuickActionLogic.countText` 不再读 `pet.photoLogs` 关系数组，用户点击后的 moment quick/history route 仍保留为 route/action-time 窄路径。新增 `HomeExpensePreviewStoreTests.expandedMomentQuickActionUsesReadModelEntriesInsteadOfPetRelationshipLogs`，证明关系数组有照片时空 read-model entries 仍显示空状态，只有纯值 entries 才显示今日计数；新增 `HomeReadModelStoreTests.forcedRefreshProjectsPetPhotoLogsIntoMomentInteractionState`，证明 actor/store refresh 会把 `PetPhotoLog` 投影进 interaction state 与 signature。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（15 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-38-08-+0200.xcresult`）；`scripts/test-simulator.sh -only-testing:OhanaTests/HomeReadModelStoreTests` PASS（8 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-41-10-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | Today Focus empty ledger snapshot fallback boundary P1 小切片 | 继续推进 TFU-20260612-006：`IslandQuestEngine.todayQuests` 现在显式区分“未提供 ledger snapshot”和“提供了空 ledger snapshot”；Home `TodayFocusSnapshot` 与 `TodayFocusEconomyService` 将 read-model `careLedgerEntries` 标记为 authoritative，所以空 ledger 快照不会再回落读取 `pet.careLogs` / `pet.walkLogs` / `pet.pottyLogs` 来完成 care-plan、压掉 family play quest 或生成 routine actor subtitle。旧默认仍保留给未提供 snapshot 的兼容调用。新增 `OhanaTests.islandQuestEngineTreatsEmptyReadModelLedgerAsAuthoritative`，并把 `todayFocusDailyCompletionAwardsV2OncePerDay` 夹具补齐匹配 `CareLedgerEvent`，防止 daily completion 继续依赖旧 `PetCareLog`。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests` PASS（138 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-49-41-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | QuickFeed detail row render legacy guard P1 小切片 | 继续推进 TFU-20260612-006：`QuickFeedDetailContent+FeedRows` 移除 `feedLogRow(_ log: PetCareLog, ...)` overload，`QuickFeedDetailContent+Helpers` 移除 `feedLogBadge(for log: PetCareLog)`，QuickFeed history / mode-history / overview / auto-feed / treat rows 只能以 `QuickFeedLedgerEntry` 渲染；`PetCareLog` 仍保留在显式 edit/delete compatibility 和 stock calculator 路径。新增 `QuickFeedDataControllerTests.quickFeedRenderRowsDoNotAcceptLegacyCareLogModels` 防止 row render 回退到 legacy model。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedDataControllerTests` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_03-56-55-+0200.xcresult`；首次运行因新测试变量名遮蔽 helper 编译失败，已修正后重跑通过）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | QuickFeed Home visible-progress ledger input P1 小切片 | 继续推进 TFU-20260612-006：`FeedHomeSnapshotInput` 新增 authoritative `[QuickFeedLedgerEntry]` 输入，QuickFeed route 将 `observedFeedingLedgerEntries` 传入；`FeedHomeSnapshotBuilder` 现在用这些纯值计算今日干/湿主粮、零食克数/次数、自动喂食次数/最近时间和 7 天主粮曲线，`PetCareLog` 留给 stock calculator compatibility 和未提供 ledger entries 的 legacy caller。`FeedHomeSnapshotRevision` 也单独 hash ledger entry 变化，避免 visible progress 因 legacy stock logs 未变而不刷新。新增 `QuickFeedOverviewSnapshotStoreTests.feedHomeSnapshotUsesLedgerEntriesForVisibleProgress` 与 `feedHomeSnapshotTreatsEmptyLedgerEntriesAsAuthoritative`。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests` PASS（4 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_04-00-23-+0200.xcresult`；首次运行因误把 `feedingLedgerEntries` 传给 stock snapshot builder 编译失败，已修正后重跑通过）；post-check：`git diff --check` PASS，`scripts/dev-check-changed.sh` PASS，combined `scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests` PASS（7 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_04-02-59-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | Home feed quick-command legacy log fetch removal P1 小切片 | 继续推进 TFU-20260612-006：Home 快捷 feed command 路由不再为 manual/manual-reminder/auto 判断预取 recent `PetCareLog`；`HomeCommandExecutor` 删除 `fetchRecentCareLogs`，`ExpandedQuickActionExecutor` / `ExpandedQuickActionLogic` 移除 `allFeedCareLogs` 参数，改用 feed rules + reminder state 决策。food records 仍保留给真实 stock-affecting write，CareLedger anti-repeat 检查仍走 ledger 事件。新增 `HomeCommandExecutorTests.homeFeedQuickCommandDoesNotFetchLegacyCareLogsForRouting` 防止旧 fetch/参数回潮。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests` PASS（184 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_04-06-24-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | FeedToday/Dashboard explicit legacy-log boundary P1 小切片 | 继续推进 TFU-20260612-006：`FeedTodayState` 与 `FeedingDashboardState` 移除默认 `pet.careLogs` 关系 fallback，`careLogs` 现在是显式数组且默认空；需要 legacy log 兼容的测试/调用点必须自行传入已 fetch 的 logs，默认状态构造不再触碰 SwiftData relationship arrays。新增 `QuickFeedDataControllerTests.feedTodayStateDoesNotFallbackToPetCareLogRelationship`，并补齐 legacy assertion tests 的显式 logs 输入。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/OhanaTests` PASS（142 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_23-34-03-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | Home water-cycle read-model boundary P1 小切片 | 继续推进 TFU-20260612-006：`WaterCareCycleStatusCalculator` / `CarePlanOverdueStatusCalculator` 新增显式 `WaterCareCycleLogSnapshot` 输入；Home card badge 与 expanded quick-action attention/count 现在从 `HomeCareQuickActionEntry` 纯值生成水循环日期快照。空 Home read model entries 代表 authoritative empty，不再为 water-change / filter-cycle 状态回退读取 `pet.careLogs`。新增 `HomeExpensePreviewStoreTests.expandedWaterCycleUsesReadModelEntriesInsteadOfPetCareLogRelationship` 与 `HomeSnapshotBuilderTests.snapshotWaterCycleStatusUsesReadModelEntriesInsteadOfPetCareLogRelationship`，覆盖关系数组有旧 water-change log 时空 read-model 不报警、显式 ledger entry 才报警。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests -only-testing:OhanaTests/HomeSnapshotBuilderTests` PASS（37 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_23-44-26-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-24 | Feed auto-log materializer ledger dedup P1 小切片 | 继续推进 TFU-20260612-006：`FeedAutoLogMaterializer` 不再用 `pet.careLogs` 关系数组生成 auto-feed dedup keys，改为按当前 pet / care / feeding 从 `CareLedgerEvent` 窄查询并还原 key；`CareLedgerMetadata` 新增通用 metadata 字段读写，新写与 backfill 的 feeding ledger 会保存 `autoFeedDedupKey`，backfill 还会从结构化 legacy key 恢复 `sourceEventId`，让启动时 CareLedger backfill 可在 auto materialization 前保护旧日志。QuickFeed overview / plan-calendar 纯值 entries 也纳入 metadata hash。新增 `ManualFeedCommandTests.autoFeedMaterializerUsesLedgerKeysInsteadOfPetCareLogRelationship`、`careLedgerBackfillPreservesStructuredAutoFeedDedupKey`，并扩展 auto-feed idempotency metadata 断言。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/ManualFeedCommandTests -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/QuickFeedPlanCalendarSnapshotStoreTests` PASS（32 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.24_23-52-10-+0200.xcresult`）。TFU-006 保持 Open：feeding stock calculators、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-25 | Feeding stock explicit legacy-log boundary P1 小切片 | 继续推进 TFU-20260612-006：`FeedStockCalculator` 不再接受 optional `careLogs` 或回退读 `pet.careLogs`；`FeedingPlanWriter` 在 stock reminder rebuild 前执行 pet-scoped `stockCareLogs` 窄查询；`Pet` 无参 stock convenience 不再通过 `careLogs` 扫 shared feeding session；`IslandFoodDashboardSnapshotStore` 的 legacy precise-stock fallback 改为用显式 `legacyStockCareLogs`。`QuickFeedDataControllerTests.feedTodayStateDoesNotFallbackToPetCareLogRelationship` 扩展到 stock support / Pet convenience guard。同步把 stock 行为测试改为显式 snapshot 输入，并修正 `SharedPetActionRecorderTests.sharedLitterWritesOneSessionTwoCareLogsAndNoPottyProjection` 让它显式使用 live care-event dependencies，避免依赖全局 registry 副作用。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/OhanaTests` PASS（142 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-02-03-+0200.xcresult`）；`scripts/test-simulator.sh -only-testing:OhanaTests/SharedPetActionRecorderTests` PASS（25 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-06-17-+0200.xcresult`）；`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（当前大脏树仍提示 build/targeted-test 建议；本切片 compiler surface 已由上述 simulator tests 覆盖）。TFU-006 保持 Open：food-kind/treat-kind enrichment、explicit edit/delete/claim compatibility 与剩余 legacy bridges 仍需后续迁移或明确收口。 |
| 2026-06-25 | PetMilestone legacy ledger 写入路径移除 P1 小切片 | 推进 TFU-20260614-017 并纠偏 stale tracker：重新审计发现 `PetMilestoneCommandService.createMilestone` / `seedSystemMilestones` 仍通过私有 `recordLedger(...)` 写 `CareLedgerEvent(legacyModelName: "PetMilestone")`。本轮删除该写入 helper 和 unused `careLedger` 注入参数，只保留删除时的 `ledgerEvents(for:)` 窄查询用于清理历史 legacy ledger；新增 `HomeCommandExecutorTests.petMilestoneCommandsDoNotCreateLegacyCareLedgerEvents` 源码守卫，拒绝 `recordLedger(` / `careLedger.record(` 回潮，同时保留行为测试：新 milestone 不产 CareLedgerEvent，手动构造的 matching legacy ledger 会随 milestone tombstone，unrelated legacy ledger 保留。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/RecurringFindingsRepairTests` PASS（201 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-13-14-+0200.xcresult`）；`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（当前大脏树仍提示 build/targeted-test 建议；本切片 compiler surface 已由上述 simulator tests 覆盖）。TFU-017 保持 Open：module exit、push/CI 和 fresh pure review 未完成，不能因此把 Domain 标 🏁。 |
| 2026-06-25 | QuickFeed feeding ledger metadata enrichment P1 小切片 | 继续推进 TFU-20260612-006：`CareLedgerMetadata` 新增 `feedFoodKind` / `feedTreatKind` / `sharedSessionId`，`CareLedgerService.recordPetCare` 对 feeding log 自动写这些结构化 metadata，并保留现有 reward / auto-feed metadata；`QuickFeedLedgerEntry.entries` 现在优先从 ledger metadata 读取 food kind、treat kind 和 shared-session id，旧 ledger 缺 metadata 时才回退到 legacy `PetCareLog` 或 event-derived food kind。新增 `ManualFeedCommandTests.feedingCareLedgerWritesStructuredKindMetadata` 与 `QuickFeedOverviewSnapshotStoreTests.feedingEntriesPreferLedgerMetadataForKindAndSession`，证明写入与 projection 闭环。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/ManualFeedCommandTests -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/QuickFeedTreatSnapshotStoreTests -only-testing:OhanaTests/QuickFeedPlanCalendarSnapshotStoreTests` PASS（35 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-19-33-+0200.xcresult`）；`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（当前大脏树仍提示 build/targeted-test 建议；本切片 compiler surface 已由上述 simulator tests 覆盖）。TFU-006 保持 Open：显式 edit/delete/claim compatibility、stock calculator historical bridge 与剩余 legacy read 收口仍需后续迁移或明确边界。 |
| 2026-06-25 | QuickFeed edit/delete UI-state legacy object removal P1 小切片 | 继续推进 TFU-20260612-006：QuickFeed 历史行现在只从 `QuickFeedLedgerEntry.legacyModelId` 解析可编辑的 `UUID`，不再在 row render 阶段对 `observedCareLogs` 做逐行扫描；`QuickFeedAlertRoute.deleteFeedLog` 与 `QuickFeedDraftStore` 不再保存 `PetCareLog` 对象，而是保存 pending/editing log id。用户点击 edit/delete 后通过 `QuickFeedCommandExecutor.feedLog(id:)` 做单条窄查询，找不到旧记录时 toast 并退出，符合 action-time compatibility 边界。扩展 `QuickFeedDataControllerTests.quickFeedRenderRowsDoNotAcceptLegacyCareLogModels`，同时拒绝 route/draft/alert host 把 `PetCareLog` 放回 UI 状态。验证：`git diff --check` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/QuickFeedDraftStoreTests -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests -only-testing:OhanaTests/ManualFeedCommandTests` PASS（33 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-26-18-+0200.xcresult`）。TFU-006 保持 Open：claim/typed-metric compatibility、stock calculator historical bridge 与剩余 legacy read 收口仍需后续迁移或明确边界。 |
| 2026-06-25 | QuickCare/Hygiene delete bridge 批量 ID-only 收口 P1 | 按“主题批处理”提速推进 TFU-20260612-006：QuickPlay / QuickWater / QuickPotty route data 不再预取 delete-only legacy log 数组，detail row 仅保留 ledger `legacyLogId`，用户点击删除后才通过 `PetCareCommandExecutor.careLog(id:)`、`pottyLog(id:)` 或 `QuickWaterCommandExecutor.careLog(id:)` 做单条窄查询；PetHygiene detail container 删除 `PetHygieneLog` 的 delete-only `@Query`，改为 `PetHygieneCommandExecutor.hygieneLog(id:)` action-time 查询。unknown potty claim 保持明确兼容桥，因为该 row 本身代表未知 legacy potty 记录。新增 `HomeExpensePreviewStoreTests.quickCareDeleteBridgesUseActionTimeIdFetches`，拒绝 QuickCare route/detail 重新挂 delete-only legacy arrays。验证：首次 targeted run 因 guard 误禁必要的 `"PetHygieneLog"` legacy-model-name 字符串失败，收窄断言后 `scripts/test-simulator.sh -only-testing:OhanaTests/HomeExpensePreviewStoreTests` PASS（17 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-35-47-+0200.xcresult`）。TFU-006 保持 Open：unknown claim、typed-metric compatibility、stock calculator historical bridge 与剩余 legacy read 收口仍需后续迁移或明确边界。 |
| 2026-06-25 | QuickFeed route preload legacy scope 收窄 P1 批处理 | 继续按加速批处理推进 TFU-20260612-006：QuickFeed route 不再按最近 7 天 broad fetch feeding `PetCareLog`；`QuickFeedRouteData` 先取 feeding `CareLedgerEvent`，再仅按这些 ledger row 的 `legacyModelId` 做单条 `fetchLimit = 1` 补齐历史 metadata。`QuickFeedDetailSheet` 入参改名为 `legacyCareLogs`，明确它不是渲染/route 的全量 care log 数据源；完整历史和 stock-heavy 面仍保留用户打开 sheet 后的 lazy `fullCareLogs`。新增 `QuickFeedDataControllerTests.quickFeedRouteUsesLedgerScopedLegacyCareLogs`，拒绝 route 恢复 `allCareLogs` 或日期范围 `PetCareLog` 预取。验证：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickFeedDataControllerTests -only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests -only-testing:OhanaTests/ManualFeedCommandTests` PASS（31 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-41-28-+0200.xcresult`）。TFU-006 保持 Open：unknown claim、typed-metric compatibility 与 stock calculator historical bridge 仍需后续迁移或明确边界。 |
| 2026-06-25 | PetAllFeatures route-scoped activity summary P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`PetAllFeaturesSheet` 不再通过 `pet.careLogs`、`pet.pottyLogs`、`pet.walkLogs` 关系数组计算功能入口 subtitle/counter；`PetDetailSheetRouteContainer` 在 all-features route 可见后用 `OhanaFrameScheduler.runAfterNextFrame` 延后加载 `PetAllFeaturesActivitySummary`，通过 pet-scoped `fetchCount` 和 date-bounded walk fetch 提供纯值 summary。新增 `MemberLifecycleGateTests.petAllFeaturesUsesRouteScopedActivitySummaryInsteadOfPetRelationshipCareLogs`，拒绝 sheet 重新读取这些 relationship arrays，并断言 route container 持有 summary state。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/deceasedFeatureHubsExposeOnlyMemorialSafeDestinations()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petAllFeaturesUsesRouteScopedActivitySummaryInsteadOfPetRelationshipCareLogs()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-47-39-+0200.xcresult`）。TFU-006 保持 Open：unknown claim、typed-metric compatibility、family/report/achievement 剩余 relationship summary 读与 stock historical bridge 仍需继续批量收口。 |
| 2026-06-25 | FeatureAggregate legacy care subtitle dead-code removal P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`FeatureAggregateView` 的 food / hygiene / potty / walks / retention / health / medication / weight / expense 当前都会进入 dashboard 分支，旧 `subtitle(for:)` 中对应 per-pet subtitle 是不可达 UI 代码，却仍保留 `pet.careLogs`、`pet.pottyLogs`、`pet.walkLogs`、`pet.hygieneLogs`、`pet.expenseLogs`、`pet.weightLogs` 等 broad relationship reads。本轮删除这些 dead legacy subtitle 统计，只保留 summary list 实际渲染的 basic info / documents / moments / achievements cases；dashboard-backed cases 返回空占位，防止未来重构时把旧关系数组读重新带回 aggregate 页面。新增 `MemberLifecycleGateTests.featureAggregateViewDoesNotCarryLegacyCareRelationshipSubtitles`。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/deceasedFeatureHubsExposeOnlyMemorialSafeDestinations()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petAllFeaturesUsesRouteScopedActivitySummaryInsteadOfPetRelationshipCareLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/featureAggregateViewDoesNotCarryLegacyCareRelationshipSubtitles()'` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_00-52-38-+0200.xcresult`）。TFU-006 保持 Open：unknown claim、typed-metric compatibility、family/report/achievement 剩余 relationship summary 读与 stock historical bridge 仍需继续批量收口。 |
| 2026-06-25 | FamilyActivityStrip ledger-entry pure value P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`FamilyActivityStripView` 不再在 render/computed path 扫 `pet.careLogs`、`pet.pottyLogs`、`pet.walkLogs`、`pet.expenseLogs`；`FamilyActivityStripRouteContainer` 改为首帧后用 `ModelContext` fetch 当前宠物今日 `CareLedgerEvent`，投影为纯值 `FamilyActivityEntry` 后传给 view，避免新增 route first-frame `@Query` subscription。新增 `CareLedgerMetadata.stringArrayValue(named:in:)`，让 shared walk 的 `executorIds` metadata 能展开为多位家人头像，避免为了多人展示回退 legacy `PetWalkLog`。新增 `MemberLifecycleGateTests.familyActivityStripUsesLedgerEntriesInsteadOfPetRelationshipLogs` 与 `familyActivityEntriesProjectTodayLedgerEventsAndSharedWalkExecutors`。验证：首次 targeted run 因 rename 后漏改两个 `ActivityEntry` 参数类型编译失败；后续 changed-file gate 又抓到初版新增 ledger `@Query` 违反 route-first-frame 规则，已改为 deferred fetch。修正后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/familyActivityStripUsesLedgerEntriesInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyActivityEntriesProjectTodayLedgerEventsAndSharedWalkExecutors()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-02-34-+0200.xcresult`）。TFU-006 保持 Open：FamilyWeeklyReport、BountyBoard、FamilyCollaborationDashboard activity/gap summary、Achievements/Economy summary 仍有 relationship-array 读，需要继续批处理。 |
| 2026-06-25 | FamilyTasks activity summary ledger projection P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：新增 `FamilyCareLedgerEntry` 作为 FamilyTasks 共享纯值投影；`BountyBoardView` 本周家人统计改为首帧后读取 bounded week-window `CareLedgerEvent` 并从纯值统计 care/potty/walk/expense，不再扫 `pet.careLogs`、`pet.pottyLogs`、`pet.walkLogs`、`pet.expenseLogs`；`CrewRosterRouteData` 把同一批 pure entries 传给 `FamilyCollaborationDashboardView`，latest activity、today count、board progress 和 care-gap detection 全部读 `careLedgerEntries`。shared walk 继续通过 ledger metadata `executorIds` 保留多人执行者。新增 `MemberLifecycleGateTests.familyTaskActivitySurfacesUseLedgerEntriesInsteadOfPetRelationshipLogs` 与 `familyCareLedgerEntriesProjectWeeklyStatsAndExecutorLists`。验证：首次 targeted run 因新 executor helper 空数组被推断为 `[Any]` 编译失败，显式 `[String]` 后又清掉 FamilyActivityEntry actor-isolation warning；最终 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/familyActivityStripUsesLedgerEntriesInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyActivityEntriesProjectTodayLedgerEventsAndSharedWalkExecutors()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyTaskActivitySurfacesUseLedgerEntriesInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyCareLedgerEntriesProjectWeeklyStatsAndExecutorLists()'` PASS（4 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-16-17-+0200.xcresult`）。TFU-006 保持 Open：FamilyWeeklyReport、Achievements/Economy summary 与其它 legacy compatibility bridge 仍需继续批处理。 |
| 2026-06-25 | FamilyWeeklyReport deferred ledger read model P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`FamilyWeeklyReportDashboardView` 删除首帧 live `@Query` subscriptions，改为 `FamilyWeeklyReportRouteData` 在首帧后加载 pets/humans 和 bounded four-week pet `CareLedgerEvent` window；dashboard 删除 `pet.careLogs`、`pet.pottyLogs`、`pet.walkLogs`、`pet.expenseLogs` fallback，体重趋势改读 `.weight` ledger；`WeeklyReportCard` 也改用 ledger 事件生成 walk/potty/expense/active-days/latest-weight 展示，walk duration 仅在 ledger metadata 存在时显示。新增 `CareLedgerMetadata.doubleValue(named:in:)` 和 `MemberLifecycleGateTests.familyWeeklyReportsUseDeferredLedgerReadModelInsteadOfPetRelationshipLogs`。验证：`git diff --check` PASS；`scripts/audit-route-first-frame.sh --changed` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/familyActivityStripUsesLedgerEntriesInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyActivityEntriesProjectTodayLedgerEventsAndSharedWalkExecutors()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyTaskActivitySurfacesUseLedgerEntriesInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyWeeklyReportsUseDeferredLedgerReadModelInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyCareLedgerEntriesProjectWeeklyStatsAndExecutorLists()'` PASS（5 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-24-01-+0200.xcresult`）；`scripts/dev-check-changed.sh` PASS；post-format `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/familyWeeklyReportsUseDeferredLedgerReadModelInsteadOfPetRelationshipLogs()'` PASS（1 Swift Testing test，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-26-29-+0200.xcresult`）。TFU-006 保持 Open：Achievements/Economy summary 与剩余 legacy compatibility bridge 仍需继续批处理。 |
| 2026-06-25 | StreakManager ledger narrow check-in P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`StreakManager.refreshStreak(for:context:)` 不再用 `pet.pottyLogs`、`pet.walkLogs`、`pet.foodRecords` 判断当天是否打卡，改为按当前 pet、当天时间窗、potty / walk / feeding `CareLedgerEvent` 做 `fetchLimit = 1` 窄查询；legacy relationship 有记录但 ledger 为空时不会触发派生连击，避免写入派生路径继续依赖 broad relationship arrays。新增 `MemberLifecycleGateTests.streakManagerUsesLedgerNarrowCheckInInsteadOfPetRelationshipLogs` 与 `streakManagerCheckInReadsLedgerInsteadOfPetRelationshipLogs`。验证：`git diff --check` PASS；`scripts/audit-route-first-frame.sh --changed` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/streakManagerUsesLedgerNarrowCheckInInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/streakManagerCheckInReadsLedgerInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyWeeklyReportsUseDeferredLedgerReadModelInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/familyCareLedgerEntriesProjectWeeklyStatsAndExecutorLists()'` PASS（4 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-33-27-+0200.xcresult`）。TFU-006 保持 Open：`AchievementManager` summary 与剩余 legacy compatibility bridge 仍需继续批处理。 |
| 2026-06-25 | IslandProsperity ledger-count API P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`IslandProsperityManager` 当前没有活跃调用点，但预留 API 仍携带 `pet.walkLogs`、`pet.pottyLogs`、`pet.hygieneLogs`、`pet.healthLogs`、`pet.weightLogs`、`pet.foodRecords`、`pet.milestones` 的 dormant relationship reads。本轮删除 `pets:` API，改为接收已投影 total count 或 filtered `CareLedgerEvent` rows；等级阈值 50 / 200 与进度语义保持。新增 `MemberLifecycleGateTests.islandProsperityUsesLedgerCountsInsteadOfPetRelationshipLogs` 与 `islandProsperityCountsOnlyPetLedgerActivity`。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/streakManagerUsesLedgerNarrowCheckInInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/streakManagerCheckInReadsLedgerInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/islandProsperityUsesLedgerCountsInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/islandProsperityCountsOnlyPetLedgerActivity()'` PASS（4 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-38-52-+0200.xcresult`）。TFU-006 保持 Open：`AchievementManager` summary 与剩余 legacy compatibility bridge 仍需继续批处理。 |
| 2026-06-25 | AchievementWall/AchievementManager ledger summary P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`AchievementWallRouteData` 现在首帧后加载 pet-subject `CareLedgerEvent` rows，并通过 `AchievementComputationContext(careLedgerEvents:)` 传入 `AchievementWallContentView` / `AchievementManager`；新增 `AchievementCareLedgerSummary`，用于 care、potty、walk、hygiene、health、weight、expense、feeding achievements 的 unlock、progress 和 completion-date 计算。传入空 ledger 数组时按 authoritative empty 处理，不再回退 relationship arrays；未传入时保留 legacy fallback 给未迁移 caller。新增 `MemberLifecycleGateTests.achievementWallRoutesLedgerEventsIntoAchievementContext` 与 `achievementManagerPrefersAuthoritativeLedgerCareSummaryWhenProvided`。验证：`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS；post-format `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/streakManagerUsesLedgerNarrowCheckInInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/streakManagerCheckInReadsLedgerInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/islandProsperityUsesLedgerCountsInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/islandProsperityCountsOnlyPetLedgerActivity()' '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementWallRoutesLedgerEventsIntoAchievementContext()' '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementManagerPrefersAuthoritativeLedgerCareSummaryWhenProvided()'` PASS（6 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_01-54-57-+0200.xcresult`）。TFU-006 保持 Open：typed-metric compatibility、stock calculator historical bridge 与非 AchievementWall caller 仍需迁移或明确边界。 |
| 2026-06-25 | QuickPotty unknown claim ID-only render boundary P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：未知共同便便 claim 不再在 `QuickPottyDetailSheet` render/computed path 调 `QuickPottyUnknownClaimStore.items(for:context:)` 拉 `PetPottyLog` 对象；`QuickPottyRouteData` 首帧后加载 `PoopUnknownPottyEntry` 纯值，`PoopLogItem.unknownPotty` 只保存 log id、date、targetCount。用户点击 claim/delete 后，`QuickPottyDetailSheet` 才通过 `PetCareCommandExecutor.pottyLog(id:)` 做单条窄查询并调用原 command。更新 `HomeCommandExecutorTests.quickPottyUnknownSharedFlowCanBeClaimedAndRefreshesSessionAndLedger`，新增 `HomeExpensePreviewStoreTests.quickPottyUnknownClaimUsesRoutePureEntriesAndActionTimeFetches`。验证：`git diff --check` PASS；`scripts/dev-check-changed.sh` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/HomeCommandExecutorTests/quickPottyUnknownSharedFlowCanBeClaimedAndRefreshesSessionAndLedger()' '-only-testing:OhanaTests/HomeExpensePreviewStoreTests/quickCareDeleteBridgesUseActionTimeIdFetches()' '-only-testing:OhanaTests/HomeExpensePreviewStoreTests/quickPottyUnknownClaimUsesRoutePureEntriesAndActionTimeFetches()'` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-02-40-+0200.xcresult`）。TFU-006 保持 Open：typed-metric compatibility、stock calculator historical bridge 与非 AchievementWall caller 仍需迁移或明确边界。 |
| 2026-06-25 | QuickFeed/FeedHome stock ledger-authoritative P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：`FeedStockCalculator` 新增 optional `[QuickFeedLedgerEntry]` 输入，QuickFeed/FeedHome stock 快照在已提供 ledger entries 时用纯值计算库存消耗、recent average、remaining/runout；传入空数组代表 authoritative empty，不再回落到 legacy `PetCareLog`。`QuickFeedStockSnapshotStore` 的 revision hash 纳入 ledger entries，`QuickFeedDetailSheet` 初始化与 `refreshStockSnapshot` 也传入 `observedFeedingLedgerEntries`。新增 `QuickFeedStockSnapshotStoreTests.stockSnapshotUsesFeedingLedgerEntriesForConsumptionWhenProvided` 与 `stockSnapshotTreatsSuppliedEmptyFeedingLedgerEntriesAsAuthoritative`。验证：`git diff --check` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/QuickFeedStockSnapshotStoreTests' '-only-testing:OhanaTests/QuickFeedOverviewSnapshotStoreTests' '-only-testing:OhanaTests/QuickFeedDataControllerTests/feedTodayStateDoesNotFallbackToPetCareLogRelationship()'` PASS（10 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-10-41-+0200.xcresult`）；`scripts/dev-check-changed.sh` PASS；post-format `git diff --check` PASS。TFU-006 保持 Open：typed-metric compatibility、非 AchievementWall caller 与独立 dashboard/writer 的 explicit legacy stock fallback 仍需迁移或明确边界。 |
| 2026-06-25 | Achievement non-wall caller ledger context P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：Oasis bento、PetRetentionHub 和 IslandRetentionDashboard 不再裸调 `AchievementManager.compute(for:)` 并触发 manager 的 relationship-array fallback；`OasisRewardLiveDataStore` 首帧后加载 pet-subject achievement ledger events，`OasisRewardCommandExecutor.makeBentoSnapshot`、`PetRetentionHubScreenModel`、`IslandRetentionDashboardScreenModel` 全部显式传 `AchievementComputationContext(careLedgerEvents:)`。Retention 两个 view 也通过 `OhanaFrameScheduler.runAfterNextFrame` deferred fetch ledger events，避免首帧 live `@Query`。新增 `MemberLifecycleGateTests.nonWallAchievementCallersUseLedgerContext`。验证：首次 targeted run 暴露 `summaries` getter 漏 `return`，第二次暴露 optional `subjectId`，均修正；最终 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementWallRoutesLedgerEventsIntoAchievementContext()' '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementManagerPrefersAuthoritativeLedgerCareSummaryWhenProvided()' '-only-testing:OhanaTests/MemberLifecycleGateTests/nonWallAchievementCallersUseLedgerContext()'` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-16-48-+0200.xcresult`）；`scripts/dev-check-changed.sh` PASS；post-check `git diff --check` PASS，且源码搜索确认 Oasis / DashboardRecords / Achievements caller 中无裸 `AchievementManager.compute(for: pet)` / `compute(for: $0)`。TFU-006 保持 Open：typed-metric compatibility 与独立 dashboard/writer 的 explicit legacy stock fallback 仍需迁移或明确边界。 |
| 2026-06-25 | Today Focus pet-weight ledger-authoritative P1 批处理 | 继续按主题批处理推进 TFU-20260612-006：Home read model 和 Today Focus daily-completion economy gate 的 `TodayFocusCareLedgerEntry` fetch 现在纳入 pet `.weight` ledger rows；`TodayFocusService` 的 `q_weight_*` 完成判断读 `.weight` ledger，不再在 read-model path 读 `pet.weightLogs`；`IslandQuestEngine` 在 ledger snapshot supplied/authoritative empty 时用 ledger 判断 weight task generation 与 weight care-plan completion。新增/更新 `todayFocusServiceCompletesCareQuestsFromLedgerEntries`、`todayFocusServiceTreatsLedgerWeightAsAuthoritative`、`islandQuestEngineTreatsEmptyReadModelLedgerAsAuthoritative`、`islandQuestEngineUsesLedgerWeightForReadModelCompletion`。验证：方法级 Swift Testing filter 编译但选择 0 tests，不计入证据；文件级 `scripts/test-simulator.sh '-only-testing:OhanaTests/OhanaTests'` 初跑暴露 daily-completion fixture 缺 pet weight ledger 和 economy-service weight fetch gap，修正后 PASS（140 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-24-40-+0200.xcresult`）。TFU-006 保持 Open：独立 dashboard/writer 的 explicit legacy stock fallback 与剩余兼容桥仍需迁移或明确边界。 |
| 2026-06-25 | Feeding stock independent dashboard/writer ledger P1 批处理 | 按同类风险面批量推进 TFU-20260612-006：`FeedingPlanWriter.rebuildFoodStockReminders` 不再 fetch stock `PetCareLog`，而是按 pet scoped feeding `CareLedgerEvent` 投影为 `QuickFeedLedgerEntry` 后传给 `FeedStockCalculator`；`IslandFoodDashboardRouteData` / `IslandFoodDashboardSnapshot` / `IslandFoodDashboardContentView` 删除 `legacyStockCareLogs`，dashboard stock overview 改由 stock-specific `QuickFeedLedgerEntry` 纯值驱动；`QuickFeedLedgerEntry.entries` 标成 `nonisolated`，避免纯投影在 nonisolated writer 调用时产生 actor-isolation warning。更新 `QuickFeedDataControllerTests.feedTodayStateDoesNotFallbackToPetCareLogRelationship` 和 `IslandFoodDashboardSnapshotStoreTests` fixture。验证：`git diff --check` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/IslandFoodDashboardSnapshotStoreTests' '-only-testing:OhanaTests/QuickFeedDataControllerTests' '-only-testing:OhanaTests/QuickFeedStockSnapshotStoreTests'` 初跑 PASS（12 tests）但暴露纯投影隔离 warning，修正后复跑 PASS（12 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-32-39-+0200.xcresult`）。TFU-006 保持 Open：typed-metric compatibility 与剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | PetAllFeatures ledger-backed feature metrics P1 批处理 | 继续按同类风险面批量推进 TFU-20260612-006：`PetAllFeaturesActivitySummary` 扩为 feature-hub 纯值 read model，route first-frame 后用 pet-scoped `CareLedgerEvent` 计算 feed/non-feed care、potty、walk、health、weight、expense 和 latest-weight/expense-total；photos、milestones、documents、insurance、medication 用 route-scoped narrow counts/fetches。`PetAllFeaturesSheet` 的 health/weight/expense/moments/archive/timeline 指标不再读 `pet.healthLogs`、`pet.weightLogs`、`pet.expenseLogs`、`pet.photoLogs`、`pet.milestones`，也不再 fetch legacy `PetCareLog`/`PetPottyLog`/`PetWalkLog`。新增行为测试 `MemberLifecycleGateTests.petAllFeaturesActivitySummaryUsesLedgerAndRouteScopedCounts`，并扩展源码守卫。验证：方法级 Swift Testing filter 编译但选择 0 tests，不计入证据；文件级 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests'` PASS（65 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-46-18-+0200.xcresult`）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 与 explicit fallback callers 仍需迁移或明确边界。 |
| 2026-06-25 | Home / Today Focus negative-feedback ledger projection P1 批处理 | 按同类用户可见 read-model 链路批量推进 TFU-20260612-006：`IslandNegativeFeedback` 新增纯值 `IslandNegativeCareLedgerEntry`，no-check-in、食欲/饮水趋势、便便异常和喂食超期不再扫描 `pet.careLogs`、`pet.walkLogs`、`pet.pottyLogs`、`pet.hygieneLogs`；Home `TodayFocusSnapshot` 把已有 `TodayFocusCareLedgerEntry` 映射给负反馈，`TodayFocusCareLedgerEntry` 同步携带 `CareLedgerEvent.amountValue` 以保留 amount-based 趋势语义。新增/更新 `OhanaTests.islandNegativeFeedbackDoesNotWarnAfterTodayCareCheckIn`、`islandNegativeFeedbackIgnoresLegacyRelationshipCheckInsWhenLedgerIsEmpty`、`islandNegativeFeedbackUsesLedgerAmountsForAppetiteTrend` 与 `MemberLifecycleGateTests.islandNegativeFeedbackUsesLedgerEntriesInsteadOfPetRelationshipLogs`。验证：`git diff --check` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/OhanaTests' '-only-testing:OhanaTests/MemberLifecycleGateTests'` PASS（208 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_02-54-22-+0200.xcresult`）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 与 explicit fallback callers 仍需迁移或明确边界。 |
| 2026-06-25 | IslandQuestEngine care fallback removal P1 批处理 | 继续同一 Home / Today Focus read-model 链路批处理：`IslandQuestEngine` 的 care-plan completion、play-equivalent suppression、pet weight task、routine subtitle 和 last-routine actor hint 现在只读 `TodayFocusCareLedgerEntry`，不再回退扫描 `pet.careLogs`、`pet.walkLogs`、`pet.pottyLogs`、`pet.weightLogs`；legacy relationship rows 只有被 read model 投影成 ledger entry 后才会影响首页委托。新增源码守卫 `MemberLifecycleGateTests.islandQuestEngineUsesLedgerEntriesInsteadOfPetRelationshipCareFallbacks`，并更新 `OhanaTests.islandQuestEngineDoesNotCreatePlayTaskForEveryPetAfterInteraction` 让 walk interaction 通过 ledger entry 输入。验证：`git diff --check` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/OhanaTests' '-only-testing:OhanaTests/MemberLifecycleGateTests'` PASS（209 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-01-36-+0200.xcresult`）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 与 explicit fallback callers 仍需迁移或明确边界。 |
| 2026-06-25 | WaterCareCycleStatus snapshot-only boundary P1 批处理 | 继续同类 read-model 边界收口：`WaterCareCycleStatusCalculator` 删除“未传 snapshot 就扫 `pet.careLogs`”的隐式兼容读取，默认按 `.empty` water-cycle snapshot 加设置 anchor 计算；Home expanded quick-action 仍通过 `HomeCareQuickActionEntry` 纯值生成 `WaterCareCycleLogSnapshot`，因此旧 `PetCareLog` 关系数组不能暗中影响水循环逾期。新增 `HomeExpensePreviewStoreTests.waterCareCycleStatusIgnoresLegacyCareLogRelationshipWithoutSnapshot` 与 `MemberLifecycleGateTests.waterCareCycleStatusUsesExplicitSnapshotInsteadOfPetCareLogRelationship`。验证：`git diff --check` PASS；源码搜索 `WaterCareCycleStatus.swift` 无 `pet.careLogs` / `latestCareLogDate`；`scripts/test-simulator.sh '-only-testing:OhanaTests/HomeExpensePreviewStoreTests' '-only-testing:OhanaTests/MemberLifecycleGateTests'` PASS（87 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-07-18-+0200.xcresult`）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 与 explicit fallback callers 仍需迁移或明确边界。 |
| 2026-06-25 | FocusCard walk metric explicit-value boundary P1 批处理 | 继续首页首屏路径收口：`FocusCard.from(_ pet:)` 删除 `weeklyWalkDistanceMeters(for:)` 与 `includeWalkDistance`，不再默认扫描 `pet.walkLogs` 计算狗狗周遛狗距离；需要展示 walk metric 时必须由 read model 或 route 显式传入 `homeWalkDistanceMeters` 纯值。`FocusHomeCardDataSource` 构建首页卡片时也不再携带旧参数。新增 `MemberCreationServiceTests.focusHomeCardUsesExplicitWalkDistanceInsteadOfPetWalkRelationship` 与 `MemberLifecycleGateTests.focusHomeCardModelUsesExplicitWalkDistanceInsteadOfPetWalkRelationship`。验证：`git diff --check` PASS；源码搜索 `FocusHomeModels.swift` / `FocusHomeCardDataSource.swift` 无 `includeWalkDistance`、`weeklyWalkDistanceMeters`、`pet.walkLogs`；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberCreationServiceTests' '-only-testing:OhanaTests/MemberLifecycleGateTests'` PASS（92 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-10-53-+0200.xcresult`）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 与 explicit fallback callers 仍需迁移或明确边界。 |
| 2026-06-25 | DashboardRecords retention archive metrics P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`PetRetentionArchiveMetrics` 复用 `PetAllFeaturesActivitySummary` 并补 latest-memory / expiring-protection route-scoped fetch；`PetRetentionHubView` 与 `IslandRetentionDashboardContentView` 的 archive score、照片/时刻、健康基线、timeline、dashboard capsules 和 retention score 不再从 `pet.photoLogs`、`pet.milestones`、`pet.weightLogs`、`pet.healthLogs`、`pet.expenseLogs`、`pet.careLogs`、`pet.walkLogs`、`pet.documents`、`pet.insurances`、`pet.medications` 渲染。扩展 `MemberLifecycleGateTests.nonWallAchievementCallersUseLedgerContext`，新增 `petRetentionArchiveMetricsUsesRouteScopedCountsAndLatestMemory`。验证：`git diff --check` PASS；源搜索确认四个 DashboardRecords retention 文件无上述直接关系读取；首次 targeted run 红在 `retentionScore` 漏 `return`，修复后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/nonWallAchievementCallersUseLedgerContext()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petRetentionArchiveMetricsUsesRouteScopedCountsAndLatestMemory()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-21-38-+0200.xcresult`）；`scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 0 files；changed-file audits PASS；仍因当前大脏树提示 build 建议，本批 retention compiler surface 已由 targeted simulator test 覆盖）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 与 explicit fallback callers 仍需迁移或明确边界。 |
| 2026-06-25 | IslandWeightDashboard pet ledger metrics P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`IslandUnifiedStatsViewModel` 现在 route-loads pet weight / walk `CareLedgerEvent` rows，并用纯值 metric entries 计算 pet weight deltas、absolute weight series、weekly exploration distance 与 weekly exploration count；`IslandWeightDashboard` 的 total island weight 和 pet sparkline 改读 `vm.weightAbsolutes`，不再排序 `pet.weightLogs`。新增 `MemberLifecycleGateTests.islandWeightDashboardUsesPetLedgerMetricsInsteadOfPetRelationships` 与 `islandUnifiedStatsUsesPetLedgerMetricsInsteadOfLegacyPetRelationships`，后者证明 legacy `PetWeightLog` / `PetWalkLog` 不再影响 pet dashboard metrics。验证：源搜索确认 `IslandUnifiedStatsViewModel.swift` / `IslandWeightDashboard.swift` 无 `pet.weightLogs` 或 `pet.walkLogs`；`git diff --check` PASS；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/islandWeightDashboardUsesPetLedgerMetricsInsteadOfPetRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/islandUnifiedStatsUsesPetLedgerMetricsInsteadOfLegacyPetRelationships()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-27-51-+0200.xcresult`）。TFU-006 保持 Open：quick weight sheets、dormant/legacy chart surfaces 与剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Quick weight sheets latest ledger metric P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`GenericWeightEntrySheet` 的 quick weight chips 和 `QuickWeightSheet` 的 “上次记录” hint 现在首帧后用 `PetWeightLedgerRouteMetrics.latestWeightKg(petID:context:)` 做 pet-id-scoped `CareLedgerEvent(.weight)` 窄查询，并把结果存成 `latestPetWeightKg: Double?`；render path 不再排序 `pet.weightLogs`。新增 `MemberLifecycleGateTests.quickWeightSheetsUseRouteScopedLedgerMetricsInsteadOfPetWeightRelationships` 与 `quickWeightSheetsUseLedgerLatestWeightInsteadOfLegacyPetRelationship`，后者证明 legacy `PetWeightLog` 不再影响 quick-sheet hint。验证：源搜索确认 `GenericWeightEntrySheet.swift` / `QuickWeightSheet.swift` 无 `pet.weightLogs`；首次 targeted run 因 `amountValue` 非 optional 的 helper 写法编译失败，修正后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/quickWeightSheetsUseRouteScopedLedgerMetricsInsteadOfPetWeightRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/quickWeightSheetsUseLedgerLatestWeightInsteadOfLegacyPetRelationship()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-33-57-+0200.xcresult`）；post-check `git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 1 file；changed-file audits PASS；仍因当前大脏树提示 build 建议，本批 quick weight compiler surface 已由 targeted simulator test 覆盖）。TFU-006 保持 Open：dormant/legacy chart surfaces 与剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Dormant PetChartDashboard broad-read removal P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：删除未被活跃 Swift source 或 `Ohana.xcodeproj` 引用的 `PetChartDashboard.swift`，移除其中对 `pet.weightLogs`、`pet.walkLogs`、`pet.pottyLogs`、`pet.expenseLogs` 的 dormant render-helper broad reads；新增 `MemberLifecycleGateTests.dormantPetChartDashboardBroadReadSurfaceStaysRemoved`，防止旧 surface 回潮。验证：`rg PetChartDashboard` 仅剩历史 docs 与新 guard；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/quickWeightSheetsUseRouteScopedLedgerMetricsInsteadOfPetWeightRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/quickWeightSheetsUseLedgerLatestWeightInsteadOfLegacyPetRelationship()' '-only-testing:OhanaTests/MemberLifecycleGateTests/dormantPetChartDashboardBroadReadSurfaceStaysRemoved()'` PASS（3 Swift Testing tests，iPhone 17 simulator，编译日志移除 stale `PetChartDashboard.o`，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-37-41-+0200.xcresult`）；post-check `git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 0 files；changed-file audits PASS）。TFU-006 保持 Open：FunctionMenu legacy aggregate counts 与剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | FunctionMenu aggregate summaries P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`FeatureAggregateView` 的 Documents / Moments / Achievements subtitles 不再读 `pet.documents.count`、`pet.photoLogs.count`、`pet.milestones.count`；`FunctionMenuRouteData` 首帧后加载 `FunctionMenuPetAggregateSummary`，按 pet id 对 `PetDocument` / `PetPhotoLog` / `PetMilestone` 做 route-scoped `fetchCount`，并把纯值 `[UUID: FunctionMenuPetAggregateSummary]` 传给 router / dashboard / aggregate view。新增 `MemberLifecycleGateTests.functionMenuAggregateUsesRouteScopedSummariesInsteadOfPetArchiveRelationships` 与 `functionMenuPetAggregateSummaryCountsRouteScopedArchiveItems`。验证：源码搜索确认 FunctionMenu 渲染面无上述 legacy count 读取；首次 targeted runs 暴露新 helper 漏 `return`，修正后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/functionMenuAggregateUsesRouteScopedSummariesInsteadOfPetArchiveRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/functionMenuPetAggregateSummaryCountsRouteScopedArchiveItems()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-45-16-+0200.xcresult`）；post-check `git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 0 files；changed-file audits PASS；仍因当前大脏树提示 build 建议，本批 FunctionMenu compiler surface 已由 targeted simulator test 覆盖）。TFU-006 保持 Open：剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Archive document/milestone route rows P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`DocumentsListView`、`PetMilestoneListView` 改用 explicit-navigation route-scoped `@Query` 读取 `PetDocument` / `PetInsurance` / `PetMilestone`，不再从 `pet.documents`、`pet.insurances`、`pet.milestones` 渲染；未被生产调用的 `PetDocumentsCard.swift` / `PetMilestonesCard.swift` dormant broad-read surfaces 直接删除；`PetProtectionDashboardState` 改为由调用方传入 document/insurance rows；删除未被生产调用的 `ArchiveMemorySnapshot.init(pet:)` legacy fallback，归档分数测试改为显式传 `PetAllFeaturesActivitySummary`。新增 `MemberLifecycleGateTests.archiveFeatureViewsUseRouteScopedRowsInsteadOfPetRelationships` 与 `protectionDashboardStateUsesSuppliedArchiveRows`。验证：源码搜索确认 touched archive files 无 `pet.documents` / `pet.insurances` / `pet.milestones` 读取、无 `ArchiveMemorySnapshot(pet:)` caller，guard 同时断言 dormant card files 不回潮；首次 targeted run 红在测试 helper 使用，changed smoothness audit 又拦下 dormant card query，中间修正后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/archiveFeatureViewsUseRouteScopedRowsInsteadOfPetRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/protectionDashboardStateUsesSuppliedArchiveRows()' '-only-testing:OhanaTests/OhanaTests/archiveMemorySnapshotRecommendsBasicInfoForEmptyProfile()' '-only-testing:OhanaTests/OhanaTests/archiveMemorySnapshotRecommendsDocumentsAfterBasicInfo()' '-only-testing:OhanaTests/OhanaTests/archiveMemorySnapshotRecommendsMomentsAfterProtection()' '-only-testing:OhanaTests/OhanaTests/archiveMemorySnapshotRecommendsWeightAfterMemory()' '-only-testing:OhanaTests/OhanaTests/archiveMemorySnapshotRecommendsRetentionWhenComplete()'` PASS（7 Swift Testing tests，iPhone 17 simulator，编译日志移除 stale `PetDocumentsCard.o` / `PetMilestonesCard.o`，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_03-55-30-+0200.xcresult`）；post-check `git diff --check` PASS；`scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 0 files；changed-file audits PASS；仍因当前大脏树提示 build 建议，本批 archive compiler surface 已由 targeted simulator test 覆盖）。TFU-006 保持 Open：PDF/export surfaces 与剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Pet moments/timeline deferred route rows P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`PetTimelineItemsBuilder` 改为消费显式 `PetTimelineSourceRows`，care/potty/walk/health/expense/weight/photo/milestone 渲染数据不再从 `Pet` 关系数组读取；`PetMomentsHubRouteContainer` 首帧不再增加 `@Query` 订阅，改用 `OhanaFrameScheduler.runAfterNextFrame` 后的 route-scoped `FetchDescriptor` 批量加载 rows，并标记 `// route-first-frame: allow deferred-fetch`；`PetMomentsHubView` / `PetPhotoAlbumView` 改为消费传入 rows。新增 `MemberLifecycleGateTests.petMomentsTimelineUsesRouteScopedRowsInsteadOfPetRelationships` 与 `petTimelineBuilderUsesSuppliedRowsInsteadOfPetRelationships`，并更新 shared session timeline 测试显式传 source rows。验证：源码搜索确认 Moments/Timeline/Album touched files 无 `pet.careLogs` / `pet.pottyLogs` / `pet.walkLogs` / `pet.healthLogs` / `pet.weightLogs` / `pet.expenseLogs` / `pet.photoLogs` / `pet.milestones`，且 route container 无 `@Query`；首次 `dev-check` 正确拦下新增 route `@Query`，改为 deferred fetch 后 `scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 0 files；UI/accessibility/smoothness/route-first-frame/runtime/economy/member lifecycle/derived-state audits PASS）；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/petMomentsTimelineUsesRouteScopedRowsInsteadOfPetRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petTimelineBuilderUsesSuppliedRowsInsteadOfPetRelationships()' '-only-testing:OhanaTests/SharedPetActionRecorderTests/sharedSessionFactIsMarkedForCloudSyncAndTimelineUsesSessionTotals()'` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-07-30-+0200.xcresult`）；post-check `git diff --check` PASS。TFU-006 保持 Open：PDF/export surfaces 与剩余 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | CrewRoster pet summary route counts P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`CrewRosterRouteData` 首帧后加载 `CrewRosterPetSummary`，按 pet id 对 `PetDocument` 做 route-scoped `fetchCount`，并把纯值 `[UUID: CrewRosterPetSummary]` 传给 `CrewRosterOverlay` / `CrewRosterProfilePanel`；活跃成员面板不再用 `pet.documents.count` 渲染证件数量。未被活跃 Swift source 或 `Ohana.xcodeproj` 引用、且仍包含 `pet.documents` / `pet.weightLogs` broad reads 的 `CrewRosterExpandedMemberOverlay.swift` 直接删除。新增 `MemberLifecycleGateTests.crewRosterUsesRouteScopedPetSummariesInsteadOfPetArchiveRelationships` 与 `crewRosterPetSummaryCountsPetScopedDocuments`。验证：源码搜索确认 CrewRoster 活跃 app source 无 `pet.documents.count` / `pet.weightLogs`；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/crewRosterUsesRouteScopedPetSummariesInsteadOfPetArchiveRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/crewRosterPetSummaryCountsPetScopedDocuments()'` PASS（2 Swift Testing tests，iPhone 17 simulator，编译日志移除 stale `CrewRosterExpandedMemberOverlay` objects，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-12-21-+0200.xcresult`）。TFU-006 保持 Open：PDF/export surfaces、剩余高频 UI/report relationship reads 与 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Pet profile health/sitter route summaries P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`PetBasicInfoDetailView` 首帧后加载 `PetBasicInfoHealthSummary` 纯值快照，健康摘要卡和“给兽医”分享文案改读 vaccine / active medication / symptom / insurance / latest weight summary text；`SitterCardPreviewSheet` 的体重行也改读 pet-id-scoped `.weight` ledger latest helper。两个展示面不再在 render/computed path 读取 `pet.healthLogs`、`pet.medications`、`pet.symptomLogs`、`pet.insurances` 或 `pet.weightLogs`。新增 `MemberLifecycleGateTests.petProfileCardsUseRouteSummariesInsteadOfPetRelationshipHealthReads` 与 `petProfileHealthSummaryUsesPetScopedRowsAndLedgerWeight`，后者证明 legacy `PetWeightLog` 不影响资料摘要最新体重，ledger row 才是来源。验证：源码搜索确认目标 files 无上述旧关系读取；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/petProfileCardsUseRouteSummariesInsteadOfPetRelationshipHealthReads()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petProfileHealthSummaryUsesPetScopedRowsAndLedgerWeight()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-22-44-+0200.xcresult`）。TFU-006 保持 Open：PDF/export surfaces、剩余高频 UI/report relationship reads 与 action-time/delete/claim bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Dormant DogActivityCard broad-read removal P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：删除未被活跃 Swift source 或 `Ohana.xcodeproj` 引用的 `DogActivityCard.swift`，移除其中对 `pet.careLogs` 和 `pet.walkLogs` 的 dormant visible-count broad reads；Walk runtime helpers 保持不动，留给单独 runtime-safe batch。新增 `MemberLifecycleGateTests.dormantDogActivityCardBroadReadSurfaceStaysRemoved`。验证：`rg DogActivityCard` 仅剩新 guard；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/dormantDogActivityCardBroadReadSurfaceStaysRemoved()'` PASS（1 Swift Testing test，iPhone 17 simulator，编译日志移除 stale `DogActivityCard` objects，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-24-31-+0200.xcresult`）。TFU-006 保持 Open：PDF/export surfaces、剩余高频 UI/report relationship reads 与 action-time/delete/claim/runtime bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Pet vet PDF snapshot export P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`PetVetSummaryPDFRenderer.render` 改为显式接收 `ModelContext`，先构建 `PetVetSummaryPDFSnapshot`，通过 pet-id-scoped `FetchDescriptor` 查询 health logs、active medications、symptoms、insurance、documents 和 CareLedger `.weight` rows，再让 `PetVetSummaryPDFView` 只读纯值 snapshot。PDF 渲染面不再读取 `pet.healthLogs`、`pet.medications`、`pet.symptomLogs`、`pet.insurances`、`pet.documents` 或 `pet.weightLogs`；最新体重和 3 个月趋势改由 ledger weight rows 驱动。新增 `MemberLifecycleGateTests.petVetSummaryPDFUsesSnapshotInsteadOfPetRelationshipReads` 与 `petVetSummaryPDFSnapshotUsesPetScopedRowsAndLedgerWeight`，后者证明 legacy `PetWeightLog` 不影响 PDF 体重。验证：源搜索确认 PDF export surface 无旧关系读取且无 stale `render(pet:)` 调用；`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/petVetSummaryPDFUsesSnapshotInsteadOfPetRelationshipReads()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petVetSummaryPDFSnapshotUsesPetScopedRowsAndLedgerWeight()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-33-35-+0200.xcresult`）。TFU-006 保持 Open：剩余高频 UI/report relationship reads 与 action-time/delete/claim/runtime bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Expense/Insurance route-scoped rows P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：`ExpenseHistoryDataContainer` 首帧后通过 `ExpenseHistoryRouteData.load` deferred fetch pet-id-scoped `PetExpenseLog` rows、humans、pets、shared-care sessions，并把 rows 传给 `ExpenseHistoryContentView` / `PetExpenseDashboardContent`；历史筛选、六个月图表、dashboard 最近记录不再读 `pet.expenseLogs`，且不再给该路由新增首帧 `@Query` 订阅。`AddExpenseSheet` 的 quick amounts、active insurance 和 claim expense matching 改用 pet-id-scoped expense/insurance rows；`PetInsuranceView` 显式保险路由也改用 route-scoped `PetInsurance` rows，不再从 `pet.insurances` 渲染。更新 `MemberLifecycleGateTests.expenseHistoryDashboardUsesRouteScopedRowsInsteadOfPetExpenseRelationship`。验证：源码搜索确认 Expense/Insurance touched files 无 `pet.expenseLogs` / `pet.insurances` 关系读取；首次 `dev-check` 拦下 `ExpenseHistoryDataContainer` route `@Query`，改为 deferred fetch 后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/expenseHistoryDashboardUsesRouteScopedRowsInsteadOfPetExpenseRelationship()'` PASS（1 Swift Testing test，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-44-44-+0200.xcresult`）；`scripts/dev-check-changed.sh` PASS（SwiftFormat formatted 0 files；UI/accessibility/smoothness/route-first-frame/runtime/economy/member lifecycle/derived-state audits PASS）。TFU-006 保持 Open：剩余高频 UI/report relationship reads 与 action-time/delete/claim/runtime bridges 仍需迁移或明确边界。 |
| 2026-06-25 | CoHealth snapshot dashboard P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：新增 `CoHealthDashboardSnapshot` / `CoHealthPetSnapshot` / `CoHealthWalkEvent` / `CoHealthWeightPoint`，`CoHealthDashboardDataContainer` 与 `CoHealthDashboardFullDataContainer` 首帧后 deferred load snapshot，不再用 `@Query(sort: \Pet.name)` 在 route 首帧订阅全宠物。compact/full CoHealth render path 不再从 `pet.walkLogs` / `pet.weightLogs` 计算关联宠物、遛狗里程、体重趋势、最新宠物体重或七日柱状图；pet walk/weight 输入来自 pet-subject `CareLedgerEvent(.walk/.weight)`，human weight 也被 route-scoped fetch 成纯值。新增 `MemberLifecycleGateTests.coHealthDashboardsUseSnapshotInsteadOfPetWalkAndWeightRelationships` 与 `coHealthDashboardSnapshotUsesLedgerRowsForPetWalkAndWeight`，后者证明 legacy `PetWalkLog` / `PetWeightLog` 不影响 CoHealth pet metrics。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/coHealthDashboardsUseSnapshotInsteadOfPetWalkAndWeightRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/coHealthDashboardSnapshotUsesLedgerRowsForPetWalkAndWeight()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_04-53-30-+0200.xcresult`）。TFU-006 保持 Open：medication-heavy render/action paths 与剩余 action-time/delete/claim/runtime bridges 仍需迁移或明确边界。 |
| 2026-06-25 | Medication route rows P1 批处理 | 继续按同根因加速推进 TFU-20260612-006：新增 `PetMedicationRouteData` / `IslandMedicationRouteData`，`PetMedicationView`、`IslandMedicationDashboard`、`PetHealthDetailView` 和 `PetMedicationDetailSheet` 不再用容器 `@Query` 或 `pet.medications` 计算 medication render state；药物详情 sheet 的全局 Event query 也改成当前 medication 的 deferred dose-event load。新增 `MemberLifecycleGateTests.medicationSurfacesUseRouteScopedRowsInsteadOfPetMedicationRelationship` 与 `medicationRouteDataScopesMedicationRowsAndDoseEvents`，覆盖 no-relationship/no-query guard、pet scoped medication rows、island pet grouping、detail medication-id dose events。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/medicationSurfacesUseRouteScopedRowsInsteadOfPetMedicationRelationship()' '-only-testing:OhanaTests/MemberLifecycleGateTests/medicationRouteDataScopesMedicationRowsAndDoseEvents()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_05-02-55-+0200.xcresult`）。TFU-006 保持 Open：剩余 action-time/delete/claim/runtime bridges 与任何未迁移高频/report relationship reads 仍需继续收口。 |
| 2026-06-25 | Health route rows and alert-source read model P1 批处理 | 继续收口 TFU-20260612-006 的健康高频/报告面：`PetHealthRouteData` / `IslandHealthRouteData` 负责首帧后 route-scoped health/symptom/heat-cycle rows；新增 `PetHealthAlertSourceRouteData` 批量按 pet id 分组加载 health、weight、care、potty、walk、document、symptom、heat-cycle rows，供健康提醒聚合使用，避免活跃 Home/report 调用通过 `PetHealthAlertSource.init(pet:)` 回扫 `pet.activeHealthLogs`、`pet.weightLogs`、`pet.careLogs` 等关系数组。新增/更新 `MemberLifecycleGateTests.healthSurfacesUseRouteScopedRowsInsteadOfPetHealthRelationships` 与 `petHealthRouteDataScopesHealthRowsAndAlertSource`，后者验证 bulk alert source 不串宠物数据。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/healthSurfacesUseRouteScopedRowsInsteadOfPetHealthRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petHealthRouteDataScopesHealthRowsAndAlertSource()'` PASS（2 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_05-24-44-+0200.xcresult`）；最终 closure rerun 同 health tests 也 PASS（xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_05-51-33-+0200.xcresult`）。 |
| 2026-06-25 | Achievement pet activity summary authoritative context P1 批处理 | 继续收口 TFU-20260612-006 的 achievement/retention/oasis 非 wall caller：新增 `AchievementPetActivityRouteData`，按 pet id route-scoped fetch food records、photo logs、milestones、documents、insurance、medications、symptoms，并投影成 `AchievementPetActivitySummary`；`AchievementManager` 在 `AchievementComputationContext` 有 ledger/activity summary 时不再 fallback 到 `pet.walkLogs`、`pet.careLogs`、`pet.healthLogs`、`pet.weightLogs`、`pet.expenseLogs`、`pet.photoLogs`、`pet.documents`、`pet.insurances`、`pet.medications`、`pet.symptomLogs` 等关系数组。Achievement wall、Oasis bento、PetRetentionHub、IslandRetentionDashboard 均显式传入 ledger + activity summaries。新增/更新 `MemberLifecycleGateTests.achievementWallRoutesLedgerEventsIntoAchievementContext`、`achievementManagerPrefersAuthoritativeLedgerCareSummaryWhenProvided`、`achievementPetActivityRouteDataScopesActivitySummaries`、`nonWallAchievementCallersUseLedgerContext`、`petRetentionArchiveMetricsUsesRouteScopedCountsAndLatestMemory`。验证：首次 targeted run 暴露新 route helper 漏 `Foundation` import，修复后 `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementWallRoutesLedgerEventsIntoAchievementContext()' '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementManagerPrefersAuthoritativeLedgerCareSummaryWhenProvided()' '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementPetActivityRouteDataScopesActivitySummaries()' '-only-testing:OhanaTests/MemberLifecycleGateTests/nonWallAchievementCallersUseLedgerContext()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petRetentionArchiveMetricsUsesRouteScopedCountsAndLatestMemory()'` PASS（5 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_05-39-05-+0200.xcresult`）。 |
| 2026-06-25 | TFU-006 final Home/report health-alert closure | 最终关闭 TFU-20260612-006：FamilyWeeklyReport 的照片回忆和健康提醒均改为 route data 纯输入，`FamilyWeeklyReportDashboardContentView` 不再从 `pet.photoLogs` 或 `appServices.healthAlerts.scanAlerts(pets: activePets)` 计算报告内容；Home read-model actor 现在加载 `PetHealthAlertSourceRouteData` 并把 `healthAlertSources` 放入 `VerticalSolidHomeSourceState`，`VerticalSolidHomeSnapshotBuilder.buildForReadModelActor` 只调用 `scanAlerts(sources: source.healthAlertSources)`。`HomeReadModelStoreTests.storeSourceDoesNotReintroduceMainContextCompatibilityFetch` 加 guard，锁住 Home actor 不再回退 main-context compatibility 或 pet relationship alert source。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/familyWeeklyReportsUseDeferredLedgerReadModelInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/achievementWallRoutesLedgerEventsIntoAchievementContext()' '-only-testing:OhanaTests/MemberLifecycleGateTests/nonWallAchievementCallersUseLedgerContext()'` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_05-44-42-+0200.xcresult`）；final rerun `scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/familyWeeklyReportsUseDeferredLedgerReadModelInsteadOfPetRelationshipLogs()' '-only-testing:OhanaTests/MemberLifecycleGateTests/healthSurfacesUseRouteScopedRowsInsteadOfPetHealthRelationships()' '-only-testing:OhanaTests/MemberLifecycleGateTests/petHealthRouteDataScopesHealthRowsAndAlertSource()' '-only-testing:OhanaTests/HomeReadModelStoreTests/storeSourceDoesNotReintroduceMainContextCompatibilityFetch()'` PASS（4 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_05-57-00-+0200.xcresult`）。全局残留扫描仍能看到删除/清理、后台日历/通知兼容、action-time command、legacy `scanAlerts(pets:)` API 和非 CareLedger medication/deeplink helper；这些不再属于 TFU-006 的用户可见 CareLedger read-model debt。 |
| 2026-06-25 | TFU-019 human memorial read-only closure review | P1 closure review pass：核当前 `main` 上 human memorial command/UI 边界，`MemberLifecycleGateTests` 覆盖 deceased human profile edit deny、memorial note with reminder 只写 memorial content、presentation/security/economy/settings/family-task writes deny；destructive delete 保持显式 lifecycle/data-removal action，不属于 profile/privacy mutation。验证：`scripts/test-simulator.sh '-only-testing:OhanaTests/MemberLifecycleGateTests/deceasedCareAndProfileEditDenyWrites()' '-only-testing:OhanaTests/MemberLifecycleGateTests/deceasedHumanNoteWithReminderWritesMemorialContentOnly()' '-only-testing:OhanaTests/MemberLifecycleGateTests/deceasedMembersRejectPresentationSecurityEconomyAndSettingsWrites()'` PASS（3 Swift Testing tests，iPhone 17 simulator，xcresult `/var/folders/9j/7ldcxzn91d947mg4p_7wxmz40000gn/T/OhanaDerivedData/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.06.25_06-17-15-+0200.xcresult`）。已将 TFU-20260612-019 标记 Done，并更新 P1 triage：当前已知 active repository-code P1 为 0，剩余 Open P1 主要是 review-gate、CloudKit 1.x deferred 和真机验证。 |
