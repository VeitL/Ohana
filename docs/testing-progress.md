# Testing and Release Status

> Active validation dashboard only. The verbose pre-audit-sync ledger is
> archived at
> [`docs/archive/testing-progress-2026-07-10-pre-audit-sync.md`](archive/testing-progress-2026-07-10-pre-audit-sync.md).
> Older history is at
> [`docs/archive/testing-progress-full-2026-06-25.md`](archive/testing-progress-full-2026-06-25.md).
>
> Workflow: [`docs/ai-module-test-playbook.md`](ai-module-test-playbook.md).
>
> Status ownership: [`docs/status-ledger-map.md`](status-ledger-map.md).
>
> True-device plan: [`docs/release-true-device-test-plan.md`](release-true-device-test-plan.md).

## Current Release Read

- Last compacted: 2026-07-11.
- Release bar: **Open P0 = 0; first-release-reachable repo-code P1 = 0**.
- Active phase: **continue signed-device acceptance before RC**.
- Open follow-ups: 9 total in `docs/task-follow-ups.md`.
- Open P1: 5 total: 1 deferred CloudKit 1.x item and 4 physical-device
  validation items.
- Current decision: do not submit to App Store or call the app RC-ready until
  the physical-device P1 items are dispositioned.

## Evidence Reconciliation

Dated results describe different surfaces and must not be collapsed into a
single “all tests pass” claim:

| Evidence date | Surface | Result | Current meaning |
| --- | --- | --- | --- |
| 2026-07-11 HealthKit physical partial and local repair | Signed Human Workout path on the current physical device, followed by targeted simulator Unit/Integration and incremental `-O` Release compilation | The user confirmed authorization and matching values except Exercise, Stand, and the activity rings. A selected import persisted (the control changed to delete) but gave no visible confirmation. The local repair passed 7/7 targeted tests and Release compilation. | The app now requests and directly reads Exercise Time and Stand Hour as partial-summary fallbacks, does not invent missing activity goals, and announces localized import success/failure. A newly signed physical-device retest plus relaunch, denial, and revocation remain open under TFU-20260706-001; this is not a full HealthKit closure. |
| 2026-07-11 Plant watering-detail decomposition | Water mode/primary/trend/discovery rendering, watering schedule form, stale Plant source contracts | Plant detail experience tests passed 16/16 and long-language layout tests passed 10/10 on the iPhone 17 simulator; Debug compilation occurred in the test lane; incremental `-O` Release succeeded in 31.46 seconds and the unchanged warm rerun in 13.08 seconds; changed-file, full complexity, architecture, UI, accessibility, localization, runtime, and data-safety gates passed | `PlantCareFeatureDetailView.swift` fell from 2,065 to 1,403 lines. Pure value-driven watering render components and a binding-only schedule form now own presentation while persistence, reminder synchronization, and failure recovery remain in the parent command boundary. Five unreachable legacy render branches were removed, the 191-line function hotspot disappeared, and the production complexity baseline shrank from 97 to 96 declarations. These are compile/static/source-contract results, not a claim of manual VoiceOver or physical-device interaction proof. |
| 2026-07-11 TFU-20260711-002 closure and complexity reduction | Wallet bootstrap boundary, Home quick actions, Plant side effects, Calendar/Care/Member command payloads, Release compile surface | Wallet tests passed 20/20; Plant selections passed 9/9; the final calendar/backdate selection passed 4/4 after removing a global-registry test-order dependency; Debug and incremental `-O` Release simulator builds succeeded; the complete static release-hardening lane passed | `CoconutWalletService.swift` is 1,000 lines and back under its architecture ratchet. The production complexity baseline shrank from 120 to 97 declarations with no new/growing hotspot. Release excludes 9,747 lines of Settings DesignLab source and verifies that exclusion from the generated Swift compile list. TFU-20260711-002 is closed; CloudSync remains compiled but runtime-disabled in Solo because safely excluding it requires a later explicit module/stub boundary. |
| 2026-07-11 Oasis island-funding regression repair | D17 gift ownership, legacy gift migration, island-total injection, no-Human Pet-first UI | Targeted Unit/Integration passed 32/32; the Pet-first flow completed five injections from Lv0 to Lv1 in 1/1 UI test (41.138 seconds); a newly signed Release overlaid and launched on the current iPhone without clearing data; the user then completed the physical injection check on that overlaid Release | The 50-coconut starter gift is now an unowned `system:island` reserve. Tree injection can atomically spend the formal island total while excluding `system:legacy`. A read-only copy of the preserved device store proves the old Pet v2 gift was reclassified exactly once: island reserve 50, Pet reward 9, legacy 0, total still 59, with paired -50/+50 transfer facts and an idempotent marker. The user-observed physical tap confirmed the expected coconut deduction and energy increase, so this preserved-data Oasis regression is device-verified. |
| 2026-07-11 TFU-20260710-007/008 closure | Expense command/restore invariants and Reduce Motion runtime semantics | Expense/policy/insurance/atomic-restore tests passed 20/20; the Reduce Motion Pet-first care/reward/Oasis UI path passed 1/1 on the iPhone 17 simulator | User-entered expenses now require a finite positive amount; only domain-generated, marked insurance reimbursements may persist as negative expenses. Restore rejects invalid expense facts before mutation. Reduce Motion now selects `.minimal` motion without throttling refresh/background work or disabling interaction, while Low Power and thermal constraints remain independent. Both follow-ups are closed locally; physical assistive-technology acceptance remains external. |
| 2026-07-11 TFU-20260710-006 closure | SwiftData primary-store identity, startup failure/retry UI, and risk-based test routing | Store recovery Unit/Integration tests passed 9/9; the fail-closed/retry UI path passed 1/1; the dedicated Unit and UI schemes each resolve to App plus one test target | Production no longer opens a second writable disk or memory store. Migration-plan fallback retries the same primary identity; total open failure mounts a non-writable recovery shell. TFU-20260710-006 is closed locally. |
| 2026-07-11 D17 Pet-first closure | Clean-install Pet-first journey, optional-Human compatibility, interruption/idempotency units, and relaunch-isolated timing | D17 stability passed 10/10; median 29.97 seconds, mean 30.61, range 29.03–32.93; later optional-Human UI flow and legacy Human-fixture smoke passed | TFU-20260710-005 is closed for repository-local acceptance. Signed-device touch latency, VoiceOver traversal, energy, and permission dialogs remain physical-device evidence, not part of this closure. |
| 2026-07-11 UI smoke closure | Fast release smoke, relaunch-isolated first-pet stability, and 47-failure root-cause disposition | Fast smoke passed 3/3; first-pet path passed 10/10 repetitions; all 47 historical failures map to 14 named clusters with representative reruns | Repository-local UI smoke gate is restored. The full 81-test suite was not rerun, so this is not an 81/81 claim; physical VoiceOver traversal remains true-device evidence. |
| 2026-07-10 pre-audit baseline | CI-equivalent unit scheme and static/local gates | Archived ledger records 1,509 unit tests passing, lint/audits passing, and a Release simulator build passing | Strong unit/static baseline for that commit; not proof of current uncommitted changes or device behavior |
| 2026-07-10 independent audit | Full UI suite | 8/80 passed; 68 failures shared the first-pet bootstrap/accessibility identity | Historical pre-repair evidence; it proves the old shared bootstrap failure, not that the visible first-pet entry was absent or that the current fast gate is red |
| 2026-07-10 UI gate recovery | First-pet stability plus a post-repair full UI run | First-pet smoke passed 10/10; the full run executed 81 tests, with 34 passed and 47 failed across roughly 13 root-cause clusters | Historical full-suite checkpoint, now triaged by the 2026-07-11 closure. It remains the newest full-suite run and is not a green 81/81 result. |
| 2026-07-10 privacy/document repair | OS backup exclusion, delete-all semantics, public identity, permission localization, and rules/status consolidation | Changed-file/static audits passed; 14 targeted tests passed with 0 failures; Debug simulator build succeeded | Local implementation evidence only; OS backup behavior, signed capability identity, and release-device paths still require TFU-20260709-001 |
| 2026-07-10 atomic restore repair | Strict manifest/media preflight and one SwiftData transaction without nested saves | 24 unique targeted unit regressions passed; final post-change atomic/plant run passed 7/7; release-data-safety audit and Debug simulator build passed | Repository P0 is closed locally; process termination, low-storage, and signed-device behavior remain separate device evidence |

## Validation Ladder

1. `git diff --check` and syntax/manifest checks.
2. `scripts/dev-check-changed.sh` for the changed-file recommendation.
3. Risk-specific audits and targeted unit tests.
4. `scripts/build-debug-fast.sh` for compiler/runtime/persistence/privacy changes.
5. Full module/release gates only at an appropriate handoff.
6. Physical-device, signed capability, backup/restore, energy, location, Health,
   notification, and accessibility checks remain separate evidence.

## Phase Overview

| Area | Status | Current note |
| --- | --- | --- |
| Product and documentation authority | 🟢 | Authority is consolidated and the D17 Pet-first implementation now matches the product source of truth. |
| Privacy and delete-all contract | 🟡 | OS backup exclusion, automatic-backup/Reset generation fencing, and post-commit Human Note attachment deletion are implemented and locally verified; signed-device backup evidence remains external. |
| Backup restore | 🟢 | Strict bounded preflight rejects malformed identities, dates, duplicates, required relations, and media; one SwiftData transaction rolls back every injected phase/save/cancellation failure before publishing defaults or notifications. |
| Unit/static baseline | 🟡 | Current wallet/Plant/Calendar and cross-domain selections pass, Debug and incremental Release compile, and the complete static release-hardening lane is green; this does not replace the broader dated full-unit baseline or nightly full-suite depth. |
| UI automation | 🟢 | D17 Pet-first smoke covers no-Human care/reward/Oasis, production overlays, accessibility semantics, and a Reduce Motion runtime-policy path; the clean-install path passed 10/10 relaunch-isolated repetitions with a 29.97-second median. The historical 47 failures remain mapped to 14 root-cause clusters. The full 84-test suite was not rerun and is not claimed green. |
| Domain data validation | 🟢 | User-entered expense commands reject zero, negative, NaN, and infinity; domain-generated marked insurance reimbursements remain valid negative facts; backup preflight and rehydrate enforce the same persisted-value contract before mutation. |
| Store recovery identity | 🟢 | All automatic production opens target one primary identity; migration/default-open failure, disk-full recovery, repeated operation, relaunch persistence, fail-closed startup, and retry are covered locally. |
| Accessibility/runtime policy | 🟡 | Reduce Motion is locally separated from Low Power/thermal work throttling and the core interactive path passes; VoiceOver, Voice Control, Switch Control, and signed-device motion/touch acceptance remain external. |
| Device/OS product scope | 🟢 | D24 approves iPhone-only / iOS 26.2+; all target configurations and Debug/Release simulator products report device family `[1]`, with native iPad/watchOS deferred. |
| Architecture/CI gate | 🟢 | Wallet bootstrap ownership is split, `CoconutWalletService.swift` is 1,000 lines, the Plant watering-detail parent is 1,403 lines, and strict architecture plus the complete release-static lane pass. The production complexity ratchet now tracks 96 existing declarations, down from 120. |
| Simulator manual coverage | 🟡 | Broad historical coverage exists; remaining depth is non-blocking P2. |
| Physical-device acceptance | 🟡 | A development-signed Release Archive passes strict signing checks and installs/launches on the current iPhone 17 Pro Max. The preserved-data Oasis injection passes. HealthKit authorization and most displayed values pass, while the locally repaired Exercise/Stand/ring and visible-import-feedback behavior awaits a new signed build; App Store distribution/storefront, smallest-device smoke, notification, Memorial, HealthKit relaunch/denial/revocation, backup, location, energy, and accessibility checks remain open. |
| CloudKit/Family | ⚪ | Future-only; Solo capability profile remains closed. |

## Active Module Pointers

| Surface | Follow-up | Required evidence |
| --- | --- | --- |
| Future CloudKit live apply | TFU-20260614-014 | Two-device shared-zone conflict/delete proof after capability enablement |
| Memorial | TFU-20260612-017 | Real-device UI and notification acceptance |
| Notifications | TFU-20260612-016 | Real-device delivery/action/privacy acceptance |
| Human Workout | TFU-20260706-001 | Permission passed by user observation; real summary/read/import/relaunch/denial/revocation evidence remains |
| Solo signed Release/public identity | TFU-20260709-001 | R0-R6, iPhone-only signed Archive, minimum-hardware smoke, verified App Store Apple ID/storefront, OS backup exclusion, and energy/location paths |
| Pet GUI depth | TFU-20260629-004 | Remaining release-relevant negative/stale-route paths |
| Long-language layouts | TFU-20260612-020 | German/max-Dynamic-Type dense-screen sweep |
| Concurrency/global lifecycle | TFU-20260710-010 | Incremental strict-concurrency, task cancellation, and DI proof |
| Maintenance/Home invalidation | TFU-20260710-011 | Bounded work and measured scoped-invalidation migration |

## Recent Validation Snapshots

| Date | Scope | Result |
| --- | --- | --- |
| 2026-07-11 | HealthKit physical partial and Exercise/Stand/import-feedback repair | The user confirmed successful authorization and matching displayed Health values except Exercise, Stand, and the concentric activity rings. Importing a selected workout persisted it—the control changed to delete—but supplied no visible result. Static tracing found that the read set omitted Exercise Time and Stand Hour and that all three rings depended exclusively on a non-nil Activity Summary. The repair requests and directly queries both missing types, merges measured values with a partial Activity Summary, preserves only real Health goal values, replaces misleading empty rings with an explicit goal-unavailable state, and presents localized success/failure toast, haptic, and VoiceOver announcement after import. Resolver, source-contract, import, duplicate-import, and overlapping-source tests passed 7/7; xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUnitTests-2026.07.11_20-59-34-+0200.xcresult`. Strict targeted SwiftLint, formatting, UI, accessibility, changed-code complexity, changed-file, status-ledger, and incremental `-O` Release gates passed. A new signed overlay and real-data retest are still required; no simulator evidence is being substituted for those device behaviors. |
| 2026-07-11 | Plant watering-detail decomposition and stale-test repair | Removed five unreachable legacy water render branches; extracted value-driven mode, primary-task, chart, discovery, and schedule-form components without moving persistence or reminder side effects into render code. `PlantCareFeatureDetailView.swift` fell from 2,065 to 1,403 lines; the 191-line schedule function hotspot was removed; full production complexity now passes across 947 files with 96 grandfathered declarations. Three pre-existing source-string assertions were updated to the already-refactored Plant request facade and current Plant feature-collection UI path instead of restoring deleted APIs. Plant detail experience passed 16/16 and long-language layout passed 10/10; changed-file, architecture, V4 UI, accessibility, and Release checks passed. Incremental Release took 31.46 seconds after the source change and 13.08 seconds unchanged/warm. |
| 2026-07-11 | TFU-20260711-002 architecture and complexity closure | Wallet bootstrap moved to `CoconutEconomyBootstrapService`; Home status/render work and Plant schedule side effects gained narrower boundaries; Calendar/Care/Member long-parameter write APIs now use compact request/value payloads. The complexity fixture self-tests passed, the all-source audit scanned 945 production files and passed with 97 grandfathered declarations, and full architecture passed. The complete 44.19-second static release-hardening lane passed all 964-file runtime, economy, lifecycle, data-safety, UI, accessibility, smoothness, and route audits. Wallet tests passed 20/20, Plant selections 9/9, and the final deterministic Calendar/backdate selection 4/4. Debug and incremental `-O` Release builds passed; the final unchanged warm Release took 14.08 seconds and compile-list verification confirms Settings DesignLab is excluded. TFU-20260711-002 is closed. |
| 2026-07-11 | Rule and validation-lane simplification | `AGENTS.md` was reduced from 520 to 271 lines while retaining authority, product/data invariants, authorization, and risk routing. `dev-check-changed.sh` is read-only by default and refuses unscoped `--fix-format`; module exit no longer reruns changed audits; release hardening now aligns its 17 static audit families with CI and adds full Unit/optional UI lanes. Validation routing tests, governance/skill checks, data-safety bad/good fixtures, and the complete 959-file audit fixture suite passed. At that checkpoint the canonical release static run stopped at the wallet oversized ratchet; the TFU-20260711-002 closure row supersedes that blocker. |
| 2026-07-11 | Oasis island reserve and aggregate injection repair | A current-device R0 smoke with one Pet, no Human, and 59🥥 reproduced an injection that only vibrated. Static tracing found that the old D17 gift was member-owned while the injection command required a current Human. New gifts now credit `system:island`; an idempotent legacy transfer preserves the total while moving the still-available old gift amount out of the member wallet; tree injection atomically spends reserve then active member wallets and excludes `system:legacy`. Targeted Unit/Integration passed 32/32 with xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUnitTests-2026.07.11_14-25-42-+0200.xcresult`. The no-Human Pet-first UI path performed five successful injections to Lv1, 1/1 in 41.138 seconds, xcresult `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-OhanaUITests-2026.07.11_14-30-00-+0200.xcresult`. An incremental `-O` Release device build succeeded at `/tmp/OhanaDeviceReleaseFast-20260711`, passed strict codesign verification, overlaid without uninstalling, and launched on the current iPhone. A read-only store copy then showed `system:island=50`, Pet=9, `system:legacy=0` plus the paired migration transfer and marker. The user subsequently performed the physical injection on that overlaid Release and confirmed the expected coconut deduction and energy increase; this device regression is closed. |
| 2026-07-11 | Release build-time routing | The 959-file / 313,518-line App target made every fresh Release Archive run one full WMO frontend; the signed baseline took about 8 minutes 47 seconds, and reusing its DerivedData still started another complete WMO compile. `scripts/build-release-fast.sh` now provides an explicit `-O` incremental iPhone 17 simulator lane with an external stable cache, while `scripts/archive-release-local.sh` preserves `-O` WMO for signed RC/device gates, rejects File Provider-backed output, disables the non-product index store, and verifies signing/xattrs without export or upload. The simulator fast lane succeeded from a cold cache in 178.14 seconds and from the warm cache in 13.22 seconds; an additional warm run before making `-O` explicit succeeded in 18.77 seconds. The same incremental `-O` strategy was used for the signed current-device repair package; after its cold device cache was established, an unchanged signed rebuild succeeded in about 15.7 seconds and still passed strict codesign verification. The Archive tool's read-only `--verify` path passed against the signed R0 Archive, confirming code-sign validity, iPhone-only family `[1]`, and iOS 26.2 minimum without triggering another WMO build. Shell syntax, invalid-mode fail-fast, repository-output rejection, and `git diff --check` passed. The final distribution Archive remains WMO by design; this optimization reduces its frequency rather than claiming WMO itself became incremental. |
| 2026-07-11 | TFU-20260709-001 R0 signed Release partial | A Release 1.0 (1) Archive from commit `eece7d642` plus the current dirty worktree succeeded when DerivedData and the archive were placed outside the File Provider-managed project directory. Strict `codesign` verification and Xcode store validation passed. The arm64 product reports `UIDeviceFamily = [1]` and iOS 26.2 minimum, contains no app extension/watchOS content, and signs HealthKit plus CloudDocuments without CloudKit, APNs, or App Group. The development profile includes the connected iPhone 17 Pro Max; installation and launch succeeded on iOS 26.5.2 (23F84), and the process remained present after launch. The public privacy-policy URL is anonymously reachable. The repaired 59🥥 no-Human Oasis path passed its newly signed overlay retest by user observation. R0 remains open because this machine has only Apple Development signing and App Store Connect/storefront plus minimum-device, backup, energy/location, permission-dialog, and iCloud Drive failure proof are absent. Two earlier `.build` Archive attempts failed when File Provider reattached `com.apple.FinderInfo` after the xattr build phase; the external build location removed that signing blocker without changing app code. |
| 2026-07-11 | TFU-20260711-001 architecture-gate closure | Closed locally. AppState and plant-reminder preference backup mapping moved without behavior changes into `DataBackupManager+AppState.swift`; `DataBackupManager.swift` fell from 1,274 to 1,103 lines without adding an oversized-file baseline. Paid-consumable AppState, plant-reminder preference/rebuild, and repeated idempotent restore tests passed 3/3 under `OhanaUnitTests`; artifact `.build/TestResults/2026-07-11/tfu-20260711-001-backup-app-state.xcresult`. Strict full architecture, release-data-safety, audit fixtures, runtime, localization, governance, resources, full UI/accessibility/smoothness/route static gates, gitleaks, git-size, and the complete `scripts/release-hardening-check.sh --skip-build` passed. The test run compiled the changed App and Unit targets; the release-hardening command intentionally skipped a duplicate build. |
| 2026-07-11 | Post-device-matrix full release-hardening checkpoint | Historical checkpoint superseded by the TFU-20260711-001 closure row. Debug/Release builds, device-family product inspection, governance, status, changed-file and audit-fixture gates passed, but the first complete no-build release-hardening attempt stopped at the then-oversized `DataBackupManager.swift`; later sections were unexecuted in that attempt and were subsequently run successfully during closure. |
| 2026-07-11 | TFU-20260710-009 supported-device decision and repository configuration | Closed locally and remaining physical proof merged into TFU-20260709-001 R0. Product D24 now fixes first release at iPhone-only / iOS 26.2+, deferring native iPad and watchOS. All nine App/Unit/UI target configurations use device family `1`; Debug and Release `iPhone 17` simulator builds succeeded, and both built products report `UIDeviceFamily = [1]` plus `MinimumOSVersion = 26.2`. A machine-readable release-device manifest and governance audit reject family/deployment/watch-target drift. Signed Archive, App Store Connect/storefront, paired-Watch notification forwarding, and smallest physical iPhone smoke remain external R0/GAP-6 evidence rather than being falsely marked complete. |
| 2026-07-11 | TFU-20260710-007/008 expense and Reduce Motion hardening | Closed locally. User, shared, and Human expense commands reject non-positive or non-finite amounts before persistence and expose localized retryable errors; backup preflight and rehydrate apply the same rule. The sole negative exception is a finite insurance reimbursement produced by the insurance domain command with the audited category/marker. Reduce Motion now maps to `.minimal`: spatial/repeating motion stops, essential state feedback and interaction remain available, and refresh/background maintenance are not treated as Low Power work. AppWorkloadPolicy passed 6/6; expense plus insurance passed 7/7; atomic restore passed 7/7; the Reduce Motion Pet-first care/reward/Oasis path passed 1/1 in 34.623 seconds on iPhone 17. Debug simulator build succeeded. Artifacts: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.11_11-30-42-+0200.xcresult`, `Test-Ohana-2026.07.11_11-45-31-+0200.xcresult`, and `Test-Ohana-2026.07.11_11-33-46-+0200.xcresult`. The full Unit and 84-test UI suites were not rerun; physical assistive-technology and energy acceptance remain open. |
| 2026-07-11 | TFU-20260710-006 primary-store identity and test-lane repair | Closed locally. Every automatic production attempt now uses the same primary SwiftData store identity: first with the migration plan, then without it. If both opens fail, startup mounts a non-writable recovery shell with Retry and Support instead of silently creating a second disk store or an in-memory writable session. Fault-injected failure, recovery/retry, repeated disk-full behavior, and real cross-launch persistence tests passed 9/9 under `OhanaUnitTests`; the startup fail-closed/retry user path passed 1/1 under `OhanaUITests`. UI shard completeness passed with 83 tests assigned exactly once. Final artifacts: `.build/TestResults/2026-07-11/tfu-006-store-identity-unit-postformat.xcresult` and `.build/TestResults/2026-07-11/tfu-006-store-recovery-ui-final.xcresult`. The full Unit and 83-test UI suites were not rerun because this closure uses risk-targeted lanes. |
| 2026-07-11 | TFU-20260710-005 D17 Pet-first implementation | Closed locally. Onboarding requires the first Pet, then opens the Pet quick-weight entry for one persisted care fact, grants the deterministic one-time starter gift to the unowned `system:island` reserve, and exposes Oasis Lv0 without requiring or silently creating a Human. The formal island total combines that reserve with all active member wallets and can fund tree injection; member-specific balances remain distinct. Human name is required only when a Human is later added; gender and birthday default to absent. The original targeted onboarding, member-lifecycle, reset, route and 10/10 clean-install stability evidence remains valid; the 2026-07-11 ownership repair additionally passed 32 targeted Unit/Integration tests and a 1/1 five-injection no-Human UI path. The preserved-data injection retest also passed on the overlaid physical-device Release by user observation. Primary original artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.11_10-03-07-+0200.xcresult`; ownership-repair artifacts are recorded above. Broader signed-device touch latency, VoiceOver traversal, battery/thermal energy, and permission dialogs remain physical-device work. |
| 2026-07-11 | TFU-20260710-004 UI smoke closure | Closed for the repository-local gate. The current fast release smoke passed 3/3, covering first-pet creation without and with production overlays plus the first-pet accessibility action contract. The first-pet path then passed 10/10 repetitions with App relaunch enabled; the xcresult reports ten passed repetitions with an average duration of 39 seconds. The historical 47 failures are individually mapped into 14 root-cause clusters, with representative Human, Pet, Calendar, walk, litter, health, hygiene, feed, Plant, Settings, Daily Streak, profile, and quick-menu reruns recorded in `docs/audits/2026-07-11/UI_SMOKE_GATE_CLOSURE.md`. A confirmed Settings `LazyVStack` layout loop was removed; Plant reminder and bulk-edit routes passed afterward. Artifacts: `.build/TestResults/2026-07-11/ui-release-smoke-v3.xcresult`, `.build/TestResults/2026-07-11/first-pet-stability-10x.xcresult`, and the focused bundles listed in the closure report. The full 81-test suite was not rerun, so no 81/81 claim is made. Physical VoiceOver traversal remains true-device acceptance. |
| 2026-07-10 | TFU-20260710-004 UI smoke recovery | Historical checkpoint, superseded by the 2026-07-11 closure row. First-pet smoke passed 10 consecutive runs. The next full UI run executed 81 tests: 34 passed and 47 failed, clustered into roughly 13 root causes rather than one shared bootstrap failure. Recording inspection confirmed Daily Streak visibly opened, so that failure was a stale selector/assertion rather than a route failure; its revised selector had not yet been rerun at this checkpoint. The Calendar flow progressed past the former duplicate `Done` ambiguity, but its long CRUD rerun was interrupted after exceeding the short validation budget and was not counted as pass/fail. Three targeted route/rail policy unit tests passed; one selector was initially mistyped and executed zero tests, then was corrected and passed in a separate run. VoiceOver order was unverified at this checkpoint because the available runtime inspection exposed no usable accessibility tree. Full-suite artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_21-35-04-+0200.xcresult`; 10-run first-pet artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_18-54-32-+0200.xcresult`; policy artifacts: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_23-58-26-+0200.xcresult` and `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_23-59-40-+0200.xcresult`. |
| 2026-07-10 | TFU-20260710-003 strict and atomic backup restore | Closed locally. Required versions, identities, dates, duplicates, relations, media metadata/content, and bounded sizes are validated before live mutation. Restore now requires a clean context and uses one SwiftData transaction with autosave disabled and no nested saves; defaults, notifications, projections, and plant reminder side effects publish only after commit. Fault injection at all six phases, transaction-save failure, cancellation, malformed/oversized/tampered input, repeated idempotent restore, localization, and 17 existing backup compatibility regressions passed 24/24 unique targeted tests. The final post-overlay atomic/plant run passed 7/7. `git diff --check`, `scripts/dev-check-changed.sh`, release-data-safety, and `scripts/build-debug-fast.sh` passed. Artifacts: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_16-58-14-+0200.xcresult`, `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_17-03-22-+0200.xcresult`, and `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_17-08-37-+0200.xcresult`. Process termination, low-storage, and signed-device behavior are not simulator-proven and remain part of device/reliability acceptance rather than this closed repository P0. |
| 2026-07-10 | TFU-20260710-002 Human Note attachment lifecycle | Closed locally. Note deletion now removes only post-commit unreferenced files; Human deletion clears owned/orphan files while preserving surviving shared references; Reset removes the entire managed root only after the SwiftData commit. Injected-save rollback, repeated-delete, shared-reference, Note/Human/Reset, original command, and automatic-backup/Reset regressions passed 18/18 with 0 failures. `scripts/dev-check-changed.sh`, release-data-safety, UI V4, accessibility, localization, smoothness, route-first-frame, runtime, lifecycle, and status-ledger gates passed; `scripts/build-debug-fast.sh` succeeded. Artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_16-33-30-+0200.xcresult`. Filesystem behavior is simulator-local; signed-device backup inspection remains TFU-20260709-001. |
| 2026-07-10 | TFU-20260710-001 automatic backup + Delete-All Reset | Closed locally. One shared service now owns the run task, generation checkpoints and managed-file cleanup; Settings awaits the coordinated Reset. Deterministic non-cooperative-export, in-flight-write, cleanup-failure and new-generation tests passed. The combined targeted run passed 25 tests with 0 failures; `scripts/dev-check-changed.sh`, release-data-safety, architecture and runtime audits passed; `scripts/build-debug-fast.sh` succeeded. Artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_16-04-06-+0200.xcresult`. Real iCloud Drive timing remains part of TFU-20260709-001, not this repository P0. |
| 2026-07-10 | Current documentation/privacy repair | `git diff --check`, manifest/plist syntax, status-ledger, governance, agent/skill, release-data-safety, strict localization, audit-fixture, and `scripts/dev-check-changed.sh` gates passed. `LocalBackupExclusionPolicyTests`, `AppResetServiceTests`, and `SettingsRouteContainerTests` passed 14/14 with 0 failures. `scripts/build-debug-fast.sh` succeeded for the iPhone 17 simulator. Test artifact: `.build/DerivedData/tests/main-b6cf423d5931-tests/Logs/Test/Test-Ohana-2026.07.10_15-25-45-+0200.xcresult`. |
| 2026-07-10 | Independent Phase A-D audit | Confirmed 3 release-blocking code findings, one broken UI gate, and the D17 implementation conflict; generated the dated package under `docs/audits/2026-07-10/`. |
| 2026-07-10 | Pre-audit unit/static baseline | See the archived pre-audit ledger for exact commands and artifact paths; do not reuse it as proof for this worktree. |

## Update Rules

- This file answers only: current release gate, current phase, latest high-signal
  validation, and where the active work lives.
- Open work belongs in `docs/task-follow-ups.md`; this file points to it.
- Keep detailed command output and old snapshots in dated audit/archive files.
- Never turn a static/build result into a runtime, device, cloud, energy, or App
  Store claim.
- Update this dashboard only after durable status changes, then run
  `scripts/audit-doc-status-ledgers.sh`.
