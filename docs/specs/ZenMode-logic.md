# 佛系模式规则书

> 状态：当前产品合同。  
> 所有者：`product-foundation.md` D17、D19、D26、D29–D31。  
> 最近确认：2026-07-18。

## 目标与边界

佛系模式用一次轻触回答“今天大家怎么样”。它与普通照护系统是两个 App Shell，
但复用同一组 Human、Pet、Plant、钱包、Oasis、商店、扭蛋、收藏、电子宠物和
StoreKit 权益。模式不是套餐：Free、Personal 与未来 Family 都可选择任一模式。

佛系 Shell 固定为 Home、Streak、Oasis 三个 Tab，并使用独立 `ZenTab`。不得挂载
普通模式的 Task Center、植物总览、QuickCare、Crew、Function Menu、卡片展开、
成长弹窗、全局散步条或重型首页查询。未选中的 Streak 与 Oasis 不启动重任务。

## 首次使用与模式切换

- 新安装先选择普通或佛系，再输入 Human 名称。该 Human 是佛系的“本人”。
- 普通模式继续现有的立即/稍后建 Pet 流程；佛系不要求 Pet，直接进入 Home。
- 旧安装默认普通模式，保留全部可达数据，并显示一次可关闭的佛系介绍。
- 设置可切换模式。切换先关闭 route/sheet/cover、选择目标 Shell 的 Home，再结束或
  开启 `PresenceParticipationPeriod`。不得复制或迁移业务数据。
- 进入佛系时只有一位在世 Human 则自动绑定；多位时必须显式选本人。
- 本人删除或进入纪念状态后，自动打卡与佛系提醒立即停止，直到重新绑定。
- 切回普通模式结束参与区间并取消佛系提醒/未来守护；历史保留。再次开启从当天
  开始，不补签，也不把普通模式日历空白解释为漏签。

## Home

App 在完成引导后由用户可见的前台激活事件调用 `autoCheckInOwner(now:)`。冷启动
重复事件幂等；后台刷新、同步、静默唤醒或仅构建 View 不构成打卡。

首页只读取在世 Human、在世 Pet、未归档 Plant 与当天 Presence 事实。本人置顶，
其余按 Human、Pet、Plant 和各领域稳定排序展示。佛系从第一天展示植物，不读取
普通模式 Lv.4 可见门；新增仍使用统一 Free/Personal 额度命令。

- 未打卡卡片整卡点击调用 `checkIn(subject:status:nil:source:.card,now:)`。
- 已打卡卡片点击打开固定状态选择，不重复奖励基础打卡。
- `…` 仅提供编辑、纪念/归档和删除。
- “一键全部”提交时重新读取全部有效对象，并在一个批次中幂等写入。全部完成后
  显示只读完成态。
- `+` 先选 Human/Pet/Plant，再复用现有创建命令与表单；保存回 Home。

状态固定为：很好（绿）、还好（蓝）、需要关注（橙）、状态较差（红）。无状态是
中性色。每处颜色同时显示文字或可访问性标签，不能只靠颜色传达。

## Streak

只有绑定本人当天的 Presence 事实推进连续。其他对象的事实只供月历和分析。连续
按事实写入时保存的本地自然日与时区计算；后来改变时区不重写历史。参与区间外
显示“未参与”，区间内缺失才中断当前连续，最长连续保留。

Free 永久可查看所有原始月份、切换对象、当前/最长连续、每日是否打卡和状态标签，
不得按时间隐藏原始历史。Personal 增加 90 天、1 年、全部时间趋势、状态分布、
完成率、跨对象比较与导出。未来 Family 在 Personal 上增加操作者归属和审计。

佛系 Streak 不提供排行榜、补签商店、手动领奖或 Coconut Ledger 快捷入口。旧版
补签日期只作为本人 legacy 历史，不发奖。

## Oasis

`ZenOasisPage` 只展示椰子余额、生命树、能量注入，以及商店、扭蛋、电子宠物三个
紧凑入口。基础 Oasis 对 Free 开放，既有成长等级门继续生效；Personal 不改变椰子
倍率、消费规则、商品价格或扭蛋概率。

佛系首次新增 Pet 或 Plant 后显示一次 50 椰子起航礼领取卡。它与普通模式首 Pet
赠礼使用同一家庭级、一次性、幂等 grant key，进入 `system:island`。任一入口领取
后另一入口不得再次出现或发放。

## 事实与事务

V93 新增：

- `PresenceCheckIn`：主体/自然日唯一键、主体类型/ID、本人标记、时区、时间、来源、
  可选固定状态、批次和操作者。
- `PresenceParticipationPeriod`：本人进入和退出佛系的区间。
- `PresenceRewardReceipt`：奖励种类、稳定幂等键、计划值与实际发放值。
- `SafetyContact`：本机手填姓名和电话号码；不等同 Human、兽医或未来 App 成员。

唯一写入口为 `autoCheckInOwner`、`checkIn`、`checkInAll`、`updateTodayStatus`。同一主体
同一自然日只有一条事实。对象在写入前重新验证；离世、归档、删除对象拒绝新增但
保留历史。打卡、receipt、预算占用与钱包流水在同一事务保存；失败整体回滚。

奖励规则：

| 事件 | 椰子 | 预算 |
| --- | ---: | --- |
| 本人当日首次打卡 | 1 | 日预算 |
| 当天第一次明确状态 | 1 | 日预算 |
| 全部有效对象完成，且至少一个非本人 | 2 | 日预算 |
| 3/7/14/30/60/100/365 天 | 3/10/25/60/150/300/1000 | 家庭一次性，非预算 |
| Pet/Plant 起航礼 | 50 | 家庭一次性，非预算 |

单张非本人卡片不单独奖励。预算触顶仍写事实和实际值为零的 receipt，之后不补发。
更换本人不重置家庭里程碑。所有套餐使用同一数值。

## 数据迁移、备份与隐私

容器开启后一次性、幂等读取旧 `CheckInStreakStore` 日期、补签日期及已领奖里程碑，
只迁移到对应 Human 并标记 legacy；不推测 Pet/Plant，不补发奖励。当天已有 legacy
事实时自动打卡不能再次发奖。旧 defaults 保留一个兼容版本但停止写入。

受限备份 v32 包含 Presence 事实、固定状态、参与区间和 reward receipts。v31 仍可
解码。恢复只 upsert 事实，不运行奖励命令。模式选择、提醒配置、联系人、号码、
短信模板不进入备份、导出、CloudSync 或日志。

## 套餐与安全联系

Free：完整基础打卡/状态/全部打卡/原始月历；一个每日截止时间（建议 20:00）；一位
本机联系人；固定模板；由用户确认系统短信。

Personal：Free 全部能力；按星期的截止设置；15–180 分钟宽限；第二次本机提醒；
最多三位联系人与可编辑模板；长周期分析、比较和导出。降级不删除联系人、模板或
历史，并允许既有调度继续运行，只阻止新增超出 Free 的配置。

Family 当前阶段只有组合能力语义：`hasPersonal = Personal || Family`，`hasFamily =
Family`，以及未来同步所需的主体/操作者字段。当前 shipping profile 的 Family 守护
实现永远禁用，不新增 SKU、购买入口、宣传、CloudKit/APNs entitlement 或后台服务。

通知权限仅在用户主动开启佛系提醒时请求。本机通知可提供“我没事”动作，该明确
动作允许调用打卡命令；后台任务本身绝不打卡。系统短信编辑器只预填号码和“未收
到今天的打卡，请主动确认”语义，用户必须确认。只有系统报告已发送才显示成功；
不可发送时提供复制，不伪称送达、急救、死亡检测或保证。

## 验证门槛

- 单元/集成：模式状态机与本人绑定；命令/并发幂等；状态/全员/七档里程碑；预算
  触顶；事务回滚；纪念/归档拒绝；Free/Personal/Family 能力矩阵。
- 迁移/恢复：V92→V93；defaults 重复迁移；v31/v32 往返；联系人排除；恢复不发奖；
  成员物理删除后历史引用策略。
- UI：两种 clean install、旧用户、三 Tab、卡片/全部/状态、Oasis 礼物、设置切换、
  付费门和降级保护；九语言、Dynamic Type、VoiceOver、深色、RTL。
- 性能：Home 有界读取活跃对象和当天事实；切换 Shell 不重复启动全局 runtime。
- 环境：clean `iPhone 17 Tests` 验证引导/重置/恢复；稳定后一次 Release Dogfood 验证
  V92 既有数据、钱包和模式切换。本地通知动作、短信、权限与后台行为需真机。
