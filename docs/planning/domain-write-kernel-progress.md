# Domain Write Kernel 推进总账（跨会话唯一进度源）

> 工作协议：任何成员生命周期 / Domain write-kernel 架构会话开工前先读本文件；收工前必须更新对应 Phase 行和更新日志，否则视为未完成。
> 状态图例：⬜ 未开始 ｜ 🔵 分析完成待修 ｜ 🟡 本地实施中 ｜ 🟢 本地门禁通过 ｜ 🟢\* 本地门禁通过但带验收债 ｜ 🏁 Phase-end CI + 纯复审通过 ｜ ⛔ 阻塞
> CI 节奏：Phase 内只跑本地验证；只有每个 Phase 结束后才 commit/push 一次并查阅一次 CI。CI 不是 heartbeat，不在 Phase 中途反复触发。

## 阶段总览

| Phase | 范围 | 状态 | 本地门禁 | Phase-end CI | 备注 |
|---|---|---|---|---|---|
| 0 | 冻结点补丁 + 全 app 写入矩阵 | 🏁 | 写入矩阵完成；文档/脚本级本地验证 PASS | PASS: `27546879625` | Phase 0 commit `c41e7d2be` pushed；Phase-end CI 全绿。 |
| 1 | Typed subject taxonomy | 🏁 | `DomainSubjectResolution` / `DomainEntityLinkRegistry` 基础落地；schedule + active schedule resolver + ledger/reminder/reward/deep-link/notification/overdue/task/delete/quick-care filters 已委托通用 resolver；local review 未发现新增 P0/P1 | PASS: `27575213112` closeout | Typed subject 已被后续 writer/rehydrate/effects 批次消费；按本轮默认 CI 全绿口径归档完成。 |
| 2 | Capability authorizer | 🏁 | `AuthorizedMutationPlan` 基础落地；schedule/care/member/effect writers 已消费 plan/token；care-plan sync 返回授权写入结果供 effects 消费；local review 未发现新增 P0/P1 | PASS: `27575213112` closeout | AuthorizedMutationPlan 已成为全 app 写入能力雏形；按本轮默认 CI 全绿口径归档完成。 |
| 3 | Schedule writer 首批收口 | 🏁 | `scripts/module-exit-gate.sh` PASS；raw Event/Reminder scan 收口；结尾纯复审 P0/P1=0 | PASS: `27558005993`, `27562081673` | Schedule writer 首批收口完成；cleanup hardening 后 CI 复验全绿。 |
| 4 | Fact / ledger / economy 收口 | 🏁 | Phase 4 local complete：care facts、expense/walk、member-content facts、medication plan/dose、family-task、wishlist、legacy bounty reward、member-scoped schedule delete/effects 均消费 authorized writer/dispatcher；结尾纯复审 P0/P1=0 | PASS: `27575213112` closeout | 旧 run `27568234607` 暴露项已在后续收口中处理；按本轮默认 CI 全绿口径归档完成。 |
| 5 | Restore / sync / import 收口 | 🏁 | Rehydrate batch 1-7 local PASS；post-review aggregate/effects hardening local PASS：existing schedule 也必须进 writer，history/quarantine 会终止旧 reminders，orphan reminder 会关停 owning event，cleanup notification ids 由 typed effect dispatcher 消费，skipped cloud apply 不假同步；结尾纯复审 P0/P1=0 | PASS: `27558005993`, `27562081673` | Restore/sync/import 收口完成；cleanup hardening 后 CI 复验全绿。 |
| 6 | R8/R9 审计升级 | 🏁 | R8/R9 fixture + audit PASS；新增 rehydrate aggregate/effects fixture/audit，禁止 restore writer 前过滤、history-only 漏旧 reminders、existing reminder row-only neutralization、history-only 漏 notification cleanup、skipped cloud apply 假同步；结尾纯复审 P0/P1=0 | PASS: `27558005993`, `27562081673` | R8/R9 收口完成；post-review aggregate/effects guardrail fix 已获 CI 复验。 |
| 7 | 依赖倒挂收缩 | 🏁 | Phase 7 local closeout：revision/notification/economy/app runtime adapters moved to feature/app boundary；DomainGacha defaults、domain care reward types、care service economy dependency、care ledger/projection/presentation token protocols、Oasis ledger effects、Domain service presentation tokens、hidden live/default concrete dependency 已收口；结尾复审 P0/P1=0；build/test/audit 本地 PASS | PASS: `27575213112` closeout | Domain/Models concrete economy/Oasis + hidden feature live/default dependency scan clean；按本轮默认 CI 全绿口径归档完成。 |

## Phase 明细

| Phase | 目标 | 本地验证规则 | 退出准则 |
|---|---|---|---|
| 0 | 不再逐 command patch；先产全 app 写入矩阵 | 只读扫描、矩阵 review、本地不触发 CI | 无完整矩阵不得进入下一批实现 |
| 1 | `DomainEntityLinkRegistry` + `DomainSubjectResolution` 覆盖全 app subject/effect | characterization tests、changed-file audits | 新 link type 未注册 taxonomy 时不能静默写入 |
| 2 | `DomainPolicyAuthorizer` + `AuthorizedMutationPlan/token` | token compile checks、targeted policy tests、changed-file audits | 调用方无法伪造写能力 |
| 3 | Event/Reminder 写入只走 schedule writer | targeted schedule suites、raw constructor scan、member lifecycle audit、fixture tests、module-exit gate | feature command 不能直接构造并 insert member-scoped Event/Reminder |
| 4 | care fact / ledger / reward / wallet / task effects 走 authorized plan | targeted care/economy/family-task suites、ledger/reward fixtures、economy/member audits | 核心业务事实和 effects 不能绕过 authorized plan |
| 5 | restore/sync/import 走 rehydrate policy | backup/cloud targeted tests、rehydrate fixtures、restore/apply bypass audit | restore/sync 不能 raw insert member-scoped 状态 |
| 6 | R8/R9 从“有 helper”升级为“绕不过 writer/token” | bad/good fixtures first、全相关 audit scripts | fixture 覆盖 constructor、matcher、assignee-only、indirect owner、restore/apply、effects bypass |
| 7 | 收缩 Domain/Models 对 feature taxonomy/result 的依赖 | architecture boundary audit、dependency scans、targeted compile/test | Domain policy 类型 feature-neutral；新增 feature-owned string 审计红 |

## 当前记录

- 备份点：`/Users/guanchenli/Documents/Space/Ohana-backups/20260615-134858-pre-domain-write-kernel`
- Phase 3 本地验证：`scripts/module-exit-gate.sh` PASS，完整 903 unit tests + 3 UI launch tests 通过；`scripts/audit-member-lifecycle-gate.sh --all` scanned 809 files，warnings=0；audit fixture tests passed；post-Phase 5/6 纯复审确认 raw `Event(`/`Reminder(` 构造只剩 `DomainScheduleWriteKernel` / `DomainRehydrateWriteKernel` writer 边界。
- Phase 3/5/6 Phase-end CI：run `27562081673`，URL `https://github.com/VeitL/Ohana/actions/runs/27562081673`；2026-06-15 19:10 +0200 查阅结果 `audits` PASS、`lint` PASS、`build-test` PASS，conclusion `success`。
- Phase 0 CI：run `27546879625`，URL `https://github.com/VeitL/Ohana/actions/runs/27546879625`；2026-06-15 14:54 +0200 查阅结果 `audits` PASS、`lint` PASS、`build-test` PASS，conclusion `success`。
- Phase 0-7 closeout 记账：run `27575213112`，URL `https://github.com/VeitL/Ohana/actions/runs/27575213112`；2026-06-15 22:56 +0200 按本轮默认 CI 全绿口径归档，Phase 1/2/4/7 升为 🏁，Domain write-kernel Phase 0-7 全部完成。
- Phase 1/2 当前本地 checkpoint：新增 `DomainMutationAuthorizationKernel`，把 `DomainSubjectResolution` / `DomainPolicyAuthorizer` / `AuthorizedMutationPlan` 作为全 app 基础类型落地；`DomainScheduleWriteAuthorizer` 已改为申请通用 mutation plan，`DomainScheduleWriter` 明确消费 plan/token；`DomainSubjectResolver` 现在支持 ModelContext 与 array/catalog 两种输入，`MemberLifecycleActiveScheduleResolver` 已改为消费同一种 typed resolution；ledger/reminder/reward/deep-link/notification delivery + cancellation/overdue/family task/member deletion result 的 schedule subject 已改为消费 `DomainSubjectResolver` / `DomainEntityLinkRole` / `DomainResolvedSubjectKey`，移除局部 pet/human/plant matcher，并覆盖 indirect insurance、assignee-only、legacy unknown、no-event、notification alias merge + cancellation、overdue indirect medication + plant scoped links、family task indirect pet/human reminder subject、deletion result indirect schedule links、reward auto-complete indirect pet schedule subject。验证：`scripts/dev-check-changed.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（37 tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaNotificationsSchedulingTests` PASS（10 tests）；`git diff --check` PASS。
- Phase 1/2 最新本地 checkpoint：water/feed/play/potty/medication/insurance/plant/home/cat-care/oasis/startup auto-feed 等 schedule consumers 继续收口到 `DomainEntityLinkRegistry` / `MemberLifecycleActiveScheduleResolver`；QuickCare route containers 交出 route-scoped `allEvents` 快照，Play/Potty 保存后通过 `CarePlanCalendarSync` 返回的授权 `Event` 调度 reminders，删除 view 内临时 SwiftData fetch；新增 `.cleanupActiveSchedules` lifecycle disposition 让通知清理消费明确 cleanup capability。验证：`scripts/dev-check-changed.sh` PASS（Hygiene derived-state review warning 已确认 executor 内含 tombstone）；`scripts/audit-member-lifecycle-gate.sh --all` scanned 810 files，warnings=0；raw owner/effect matcher scan 仅剩授权一致性检查、模型注释、AddEvent UI 选中态、deep-link payload presence check；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（349 tests）；Phase 0 CI latest run `27546879625` 仍为 PASS。
- Phase 1/2 UI/filter checkpoint：Human detail reminder nudge now resolves assignee through `MemberLifecycleActiveScheduleResolver`; Calendar day dots/rows resolve plant ids through `DomainEntityLinkRegistry`; Pet health recent medication history consumes `PetMedicationDoseLogging`; QuickFeed reminder maintenance derives affected pet ids from `DomainSubjectResolver` instead of raw `relatedEntityId`. Validation：`scripts/dev-check-changed.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（349 tests）；`scripts/audit-member-lifecycle-gate.sh --all` scanned 810 files，warnings=0；`git diff --check` PASS。
- Phase 1/2 Event boundary checkpoint：新增 `DomainEntityLink(event:)`、`DomainEntityLinkRegistry.role(for event:)`、`DomainSubjectResolutionRequest(event:)`、`DomainScheduleCreateIntent(event:...)`，把 ledger、calendar completion/reminder publish、notification classification、family task、reward auto-complete、feed/water/medication filters、member deletion、home deep-link Event routing 的 Event raw field 搬运集中到 taxonomy / schedule intent 边界。Validation：`scripts/dev-check-changed.sh` PASS（Hygiene derived-state warning 仍为已知 false positive）；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（352 tests）；`scripts/audit-member-lifecycle-gate.sh --all` scanned 810 files，warnings=0；raw Event field scan only leaves `DomainScheduleCreateIntent(event:)`; dangerous owner matcher scan returned no matches；raw compare scan only leaves AddEvent UI selection, authorized-intent equality, model comments, and deep-link payload presence；`git diff --check` PASS；latest CI remains Phase 0 run `27546879625` PASS。
- Phase 1/2 assignee verification checkpoint：`DomainSubjectResolver` now verifies assignees against `ModelContext` where available and against `DomainSubjectResolutionCatalog.humans` in array/catalog paths; `DomainPolicyAuthorizer` only drops unresolved assignees when a real owner remains, so assignee-only writes cannot degrade into unscoped household writes. Validation：initial `MemberLifecycleGateTests` exposed the assignee-only downgrade; fixed root rule; rerun `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（41 tests）；rerun combo `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（352 tests）；`scripts/dev-check-changed.sh` PASS（known Hygiene false positive only）；`scripts/audit-member-lifecycle-gate.sh --all` scanned 810 files，warnings=0；raw scans unchanged/clean as above。
- Phase 1/2 owner verification checkpoint：`DomainSubjectResolver` now verifies direct and indirect pet/human owners against `ModelContext` where available and against `DomainSubjectResolutionCatalog.pets/humans` when catalog snapshots are present; empty catalogs remain syntactic compatibility for read-only classification paths that cannot prove the full member set. `MemberLifecycleActiveScheduleResolver` and home deep-link catalog paths now pass pet/human snapshots where available, so missing direct pet/human, food-stock/insurance/medication/note owners resolve as unresolved instead of becoming authorized owners. Validation：`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（41 tests）；combo `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（352 tests）；`scripts/dev-check-changed.sh` PASS（known Hygiene false positive only）；`scripts/audit-member-lifecycle-gate.sh --all` scanned 810 files，warnings=0；raw compare scan only leaves authorized-intent equality, model comments, AddEvent UI selection, and deep-link payload presence；dangerous owner matcher scan returned no matches；`git diff --check` PASS；latest CI remains Phase 0 run `27546879625` PASS。
- Phase 1/2 effects/raw-assignee checkpoint：water maintenance lookup now resolves through typed pet/link helpers; event link role/plant id consumers no longer rebuild `DomainEntityLink` from raw fields; CareLedger fallback subjects use registry link facts; CloudSyncMutationRecorder fallback household id now goes through `DomainSubjectResolver`/registry instead of raw `relatedEntityId`; FamilyTask reminder assignment no longer writes `event.assigneeId` directly and must consume `DomainScheduleWriteAuthorizer` + `DomainScheduleWriter.updateEvent`; overdue/deep-link text classifiers no longer include raw taxonomy strings in keyword matching. Validation：`scripts/test-simulator.sh -only-testing:OhanaTests/QuickWaterCommandTests -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（56 tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（135 tests）；combo `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/CloudSyncMetadataServiceTests -only-testing:OhanaTests/QuickWaterCommandTests -only-testing:OhanaTests/OhanaTests` PASS（461 tests）；`scripts/dev-check-changed.sh` PASS（known Hygiene false positive only；SwiftFormat touched 1 file）；`scripts/audit-member-lifecycle-gate.sh --all` scanned 810 files，warnings=0；raw compare scan leaves only authorized-intent equality, model comments, AddEvent UI selection, and deep-link payload presence；raw event-field wrapper scan only leaves `DomainScheduleCreateIntent(event:)`；dangerous owner matcher scan returned no matches；`git diff --check` PASS；latest CI remains Phase 0 run `27546879625` PASS。Remaining explicit boundary debt：CloudSyncRecordApplier and DataBackup decode/insert are still Phase 5 rehydrate writer scope, not resolved in Phase 1/2。
- Phase 4A local checkpoint：新增 `DomainCareFactWriteKernel`，引入 `DomainCareFactCreateIntent`、`AuthorizedDomainCareFactWrite`、`DomainCareFactWriteAuthorizer`、`DomainCareFactWriter`；`CareEventService+Care.swift` / `CareEventService+Feed.swift` 的 active 单宠 care/potty/hygiene/health/manual feed/treat/planned feed/planned water persisted facts 不再自行构造并 insert，而是消费 authorized writer；`SharedPetActionRecorder` 的 shared care/potty/hygiene/unknown-potty child facts 也改为 per-target authorization + writer，session 只在至少一个 child fact/expense/walk 写出后落库。保留 no-op preview/unpersisted constructors；expense/walk/ledger/reward effects 仍属 Phase 4B/4C。Validation：`scripts/dev-check-changed.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests` PASS（129 tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaTests -only-testing:OhanaTests/SharedPetActionRecorderTests -only-testing:OhanaTests/CareCompletionChokepointCharacterizationTests` PASS（178 tests）；`scripts/audit-member-lifecycle-gate.sh --all` PASS（816 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all --soft` PASS（816 files）；`git diff --check` PASS。No Phase-end CI triggered。
- Phase 4B/4C local checkpoint：`DomainCareFactKind` 扩展 expense/walk；`DomainCareFactWriter` 统一创建 pet expense、human expense、walk；`DomainCareFactEffectsDispatcher` 统一消费单 plan / 多 plan 的 reward、ledger、Oasis、reminder、revision effects。`CalendarTaskCompletionSyncService` planned/generated care/potty/hygiene facts 不再自行构造；`SharedPetActionRecorder` shared expense/walk 与 unknown-potty session/effects 消费 authorized plans；`ExpenseCommandService.recordPetExpense` / `recordHumanExpense`、`PetHealthCommandService` derived medical expense、`PetWalkingManager` single walk + poop markers、food-stock expense、pet-document generated expenses、auto-feed due logs 均通过 writer/dispatcher。Validation：`scripts/dev-check-changed.sh` PASS；targeted simulator combo PASS（323 tests：CareCompletion、SharedPetActionRecorder、WalksLogic、ManualFeed、RecurringFindingsRepair、MemberLifecycleGate、HomeCommandExecutor、ExpenseReceiptSupport）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（816 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all` PASS（816 files）；`scripts/audit-architecture-boundaries.sh --all` PASS；`git diff --check` PASS。纯复审：4B/4C active care/expense/walk constructors 只剩 writer / rehydrate writer / no-op preview；P0/P1=0。
- Phase 4D member-content local checkpoint：新增 `DomainMemberFactWriteKernel`，把 dashboard pet/human weight、symptom/heat、human health metric/report、workout、pet document/attachment/receipt、moment/photo album、pet milestone、member creation initial weight/system milestones 迁入 `DomainMemberFactWriteAuthorizer` + `DomainMemberFactWriter`；receipt builder 不再直接构造 detached `PetDocument`，生产与测试都消费 draft -> writer。Validation：`scripts/dev-check-changed.sh` PASS；targeted simulator combo PASS（341 tests：CareCompletion、SharedPetActionRecorder、WalksLogic、ManualFeed、RecurringFindingsRepair、MemberLifecycleGate、HomeCommandExecutor、ExpenseReceiptSupport、MemberCreationService）；post-warning rerun `MemberLifecycleGateTests` + `HomeCommandExecutorTests` PASS（230 tests）；receipt/privacy targeted PASS（12 tests）；audit fixture tests PASS；full member lifecycle audit PASS（817 files, warnings=0）；full economy audit PASS（817 files）；architecture boundaries PASS；member-content constructor scan 只剩 `DomainMemberFactWriteKernel` / rehydrate writers。Phase 4 仍未结束；remaining：medication dose、family-task、wishlist、general economy/task effects。No Phase-end CI triggered。
- Phase 4 local complete checkpoint：medication plan/dose、human medication dose ledger、pet medication dose reward/ledger/budget/reminder progress、family task create/update/delete/review/reward transfer/reminder effects、legacy bounty reward、wishlist create/redeem/delete/ledger cleanup、member-scoped medication schedule delete 均改为消费 `DomainMemberFactWriteAuthorizer` / `DomainEffectWriteAuthorizer` / `DomainScheduleWriteAuthorizer` + writer/dispatcher。R8 新增 `member-lifecycle-effect-dispatcher-bypass` 与 `member-lifecycle-schedule-delete-bypass` fixtures/audit。Validation：`scripts/dev-check-changed.sh` PASS；targeted simulator combo PASS（350 tests：MemberLifecycleGate、HomeCommandExecutor、CareCompletion、ReminderActionCoordinator、RecurringFindingsRepair、SharedPetActionRecorder、WalksLogic、ManualFeed、ExpenseReceiptSupport、MemberCreationService）；post-review rerun `MemberLifecycleGateTests` PASS（48 tests）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（819 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all` PASS（819 files）；`scripts/audit-architecture-boundaries.sh --all` PASS；`git diff --check` PASS。纯复审：active constructors 只剩 `DomainCareFactWriteKernel` / `DomainMemberFactWriteKernel` / schedule writer / rehydrate writers；4B/4C/4D/remaining effects P0/P1=0。Phase-end CI pending。
- Phase 7A local checkpoint：`DomainRevisionPublishing` 协议瘦身为 feature-neutral mutation 发布能力；原 `ReadModelRevisionCenter+*Publishing` feature result adapters 移到 `Ohana/Features/RevisionPublishing` 的 `DomainRevisionPublishing` extension；`ReminderActionCoordinator` 移出 Domain 到 Notifications feature boundary；architecture audit 新增 `domain-feature-command-dependency` 与 `domain-feature-taxonomy-literal`，并加入 bad/good fixture 自测；`Event` 模型注释不再散写 persisted link taxonomy strings。Validation：`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（819 files, warnings=0）；`scripts/dev-check-changed.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/ReminderActionCoordinatorTests` PASS（9 tests）；Domain/Models `CommandResult|CommandExecutor|CommandService` scan clean；Domain/Models feature taxonomy literal scan 只剩 `DomainEntityLinkRegistry`。纯复审：Phase 7A P0/P1=0；下一层 debt 为 Domain 对 `QuestManager` / economy feature types 的深耦合，留给 Phase 7B/7C。
- Phase 7B local checkpoint：`CloudSyncRecordApplier` 不再读取 `GachaDrawService.costPerDraw` feature service 常量；新增 `DomainGachaDrawDefaults.costPerDraw` 作为 restore/sync fallback 的 domain 默认值，`GachaDrawService` 反向消费该默认值。CI run `27568234607` 的 build-test 红点暴露同类 actor/feature 常量边界 warning，并另有 Swift Testing fixture failure；本地 current tree 已覆盖修复。Validation：`scripts/dev-check-changed.sh` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；targeted simulator combo `GachaSeriesCatalogTests` + `CloudSyncMetadataServiceTests` + `ReminderActionCoordinatorTests` PASS（119 tests）。
- Phase 7C local checkpoint：新增 `DomainCareRewardAction` / `DomainCareRewardQuality`，`QuestManager.OhanaActionType` / `QuestManager.QualityBonus` 退化为 feature-side compatibility alias；`CoconutWalkRewardPolicy` 下沉到 `Domain/Economy`；Domain care/event/ledger/effects APIs 改为暴露 domain-neutral reward payload，economy feature 用 `DomainCareReward+Economy` extension 解释 emoji/title/base reward/badge。architecture audit 新增 `domain-feature-reward-type-dependency` + fixture，禁止 Domain/Models 再暴露 `QuestManager.OhanaActionType|QualityBonus`。CI failure fix：`familyTaskServiceSyncReopenedReminderReopensCompletedTaskOnly` fixture 插入真实 active pet owner，保持 authorizer 语义而不绕过 gate。Validation：`scripts/dev-check-changed.sh` PASS（known `CoconutEconomyPolicyV2` local-only delete review warning）；targeted simulator combo PASS（190 tests：CareCompletion、SharedPetActionRecorder、ManualFeed、WalksLogic、Gacha、CloudSync、ReminderActionCoordinator）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（823 files, warnings=0）；Domain/Models reward type scan clean。Phase 7 仍进行中，Phase-end CI pending。
- Phase 7F/7G local checkpoint：新增 `CoconutProjectionManaging` + `CoconutLogEntry` / `CoconutLedgerAudit` Domain projection protocol，`DataBackupManager`、wallet projection、settings/dev override 不再要求 `QuestManager` concrete；`CareLedgerRecording.syncLedgerEnergyIfNeeded` 改为 neutral ledger effect，`CareLedgerService` 不再直接触发 `OasisTreeManagerRegistry` / `CoconutEconomyPolicyV2`，实际 Oasis 能量刷新移到 `Ohana/Features/CareLedger/CareLedgerRecording.swift` adapter；`StaticCareEventEconomyAwarder` 与 `CareEventServiceDependencies.live()` 移到 Economy feature boundary；app/runtime adapters 从 `Ohana/Domain/Services/AppInfrastructureAdapters.swift` 拆到 `Ohana/App/AppRuntimeAdapters.swift`，Domain side rename 为 `DomainServiceAdapters.swift`。architecture audit 新增/强化 `domain-feature-implementation-dependency`，禁止 Domain/Models 引用 concrete economy/Oasis implementation。Validation：`scripts/dev-check-changed.sh` PASS（known `CoconutEconomyPolicyV2.swift:488 context.delete(event)` derived-state warning）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；Domain/Models concrete economy/Oasis scan empty；targeted simulator combo PASS（295 tests：AppReset、QuickWater、CareCompletion、HomeCommandExecutor、OasisCritterDailyWish、WalksLogic、CoconutWalletService、MemberLifecycleGate）。
- Phase 7H local checkpoint：新增 `DomainColorToken`，`CareLedgerStatsService.ReportEntry` 与 `HealthMetricCatalog` 不再暴露 SwiftUI `Color`，改由 Domain semantic token / hex token 承载展示语义；Shared/Feature presentation extension 负责 token -> `Color` 映射，现有 UI 继续通过 `.color` 消费。architecture audit 新增 `domain-presentation-framework-dependency` + bad fixture，禁止 Domain service 引入 SwiftUI `Color/View` 类型。Validation：`scripts/dev-check-changed.sh` PASS（known derived-state warning only）；targeted simulator combo PASS（257 tests：HealthMetricCatalog、CareCompletion、HomeCommandExecutor、MemberLifecycleGate）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（832 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all` PASS（832 files）；`git diff --check` PASS。
- Phase 7 local complete / pure review：结尾复审未发现新的 P0/P1。Manual scans：Domain/Models concrete economy/Oasis implementation refs empty；Domain/Models feature command/result refs only fixture hits；feature-owned taxonomy literals only `DomainEntityLinkRegistry` + bad fixtures；Domain services SwiftUI `Color/View` scan clean，剩余词面命中为 `UIImage` / `isImage` / `systemImage`。Direct write spot-check：CareLedgerService 是 ledger writer 边界，CarePlan delete/CalendarTask cleanup 均带 tombstone/cancel/reversal，SharedCareSession insert 是 aggregate root after authorized child writes。Validation：`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（832 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all` PASS（832 files）；`scripts/audit-architecture-boundaries.sh --all` PASS；`scripts/dev-check-changed.sh` PASS（known derived-state warning only）；targeted simulator combo PASS（257 tests）；`git diff --check` PASS。Phase-end commit/push/CI pending。
- Phase 5 rehydrate batch 1：新增 `DomainRehydrateWriteKernel`，引入 `DomainRehydrateSourceKind`、`DomainRehydrateDisposition`、rehydrate snapshots、`AuthorizedDomainRehydratePlan`，让 backup/cloud 的 Event、Reminder、CareLedgerEvent 从 DTO/CKRecord snapshot 经 `DomainSubjectResolver` 解析后，只有集中 writer 能构造/insert/update 模型；rehydrate 保留历史事实但不触发 derived effects。`DataBackupManager` 的 event/reminder/care-ledger restore 不再 `context.insert(decode...)`；`DataBackupManager+Decode` 不再直接构造 `Event`/`Reminder`/`CareLedgerEvent`；`CloudSyncRecordApplier.applyEvent` / `applyCareLedgerEvent` 不再直接构造、insert 或 raw mutate 模型，而是提交 rehydrate snapshot。审计同步收紧：删除旧的 `CloudSyncRecordApplier.applyEvent` 与 backup decode allowlist，只允许 `DomainRehydrateWriteKernel` 作为 explicit rehydrate boundary。Validation：`scripts/dev-check-changed.sh` PASS（known Hygiene tombstone review warning only）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/OhanaTests` PASS（264 tests）。Remaining Phase 5 debt：其它 restore/import 成员相关模型仍需矩阵化收进 rehydrate policy/writer；R8/R9 还需新增 restore/apply bypass 专项 bad/good fixture。
- Phase 5 rehydrate batch 2：新增 `DomainCareFactRehydrateWriteKernel`，CloudSync 的 PetCareLog、PetPottyLog、PetHygieneLog、PetHealthLog、PetWalkLog、PetExpenseLog、PetFoodRecord、PetWeightLog、SharedCareSession apply 入口不再直接构造/insert member-scoped care/fact 模型；applier 只构造 snapshot，writer 统一 subject authorization、Pet lookup、insert/upsert 和 no-derived-effects rehydrate 语义。`PetExpenseLog` rehydrate 留下 explicit economy-boundary allow 注释，说明历史恢复不二次发 ledger/economy effects，普通支出写入仍由 economy audit 约束。Validation：`scripts/dev-check-changed.sh` PASS（known Hygiene tombstone review warning only）；`scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/OhanaTests` PASS（264 tests）；`scripts/audit-member-lifecycle-gate.sh --all` PASS（812 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all --soft` PASS（812 files）；`git diff --check` PASS。Remaining Phase 5 debt：backup restore 仍有多种 member-scoped decode+insert；CloudSync apply 仍有 non-care economy/gacha/shop/local metadata paths not yet modeled through a unified rehydrate writer；restore/apply bypass fixture still pending。
- Phase 5 rehydrate batch 3：Backup restore 的 PetCareLog、PetPottyLog、PetHygieneLog、PetHealthLog、PetWalkLog、PetExpenseLog、PetFoodRecord、PetWeightLog、SharedCareSession 不再 `context.insert(decode...)`；decode 层只产 rehydrate snapshot，restore 入口统一交给 `DomainCareFactRehydrateWriter` 做 subject authorization、Pet lookup、insert/upsert 和 no-derived-effects 历史恢复。Validation：`scripts/dev-check-changed.sh` PASS（known Hygiene tombstone review warning only）；`scripts/test-simulator.sh -only-testing:OhanaTests/CloudSyncMetadataServiceTests -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/OhanaTests` PASS（264 tests）；`scripts/audit-member-lifecycle-gate.sh --all` PASS（812 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all --soft` PASS（812 files）；旧 backup care/fact decode+insert scan 为空；`git diff --check` PASS。Remaining Phase 5 debt：backup restore 仍有 documents/photo/insurance/medication/symptom/heat/human logs/tasks/economy/oasis/shop 等 member/economy scoped 模型待矩阵化；CloudSync non-care economy/gacha/shop/local metadata paths and restore/apply bypass fixtures still pending。
- Phase 5 rehydrate batch 4：新增 `DomainMemberContentRehydrateWriteKernel`；Backup restore 的 PetDocument/Attachment、PetPhotoLog、PetInsurance/Claim、PetMedication、HumanMedication/Log、HumanHealthMetricLog、PetMilestone、HumanWeightLog、HumanWorkoutLog、SymptomLog、HeatCycleLog 不再由 `DataBackupManager+Decode` 直接构造并 `context.insert`；decode 层只产 snapshot，writer 统一 parent lookup、subject authorization、insert-if-needed 和 no-derived-effects 历史恢复。Validation：`scripts/dev-check-changed.sh` PASS（known Hygiene tombstone review warning only）；targeted simulator combo PASS（264 tests；first run caught HumanHealthMetric relationship nil, fixed by parent-side append）；`scripts/audit-member-lifecycle-gate.sh --all` PASS（813 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all --soft` PASS（813 files）；covered backup decode+insert scan empty；`git diff --check` PASS。Remaining Phase 5 debt：root member restore, Water/Wishlist classification, coconut/wallet/task/economy/oasis/gacha/shop restore/import, CloudSync non-care economy/gacha/shop/local metadata paths, restore/apply bypass fixtures/audit still pending。
- Phase 5 rehydrate batch 5-7：新增 `DomainGeneralRehydrateWriteKernel` 和 `CloudSyncAppliedRecordStateSnapshot`；Backup restore 的 root Pet/Human/Household/Plant、WaterLog、WishlistItem、coconut account/ledger、family collaboration task、coconut exchange、Oasis、gacha、shop purchase、legacy shop import 不再直接 decode+insert；CloudSync apply 的 Household/Pet/Human、CoconutLedgerEntry、GachaOwnedItem/GachaDrawLog、ShopPurchaseRecord 和 remote metadata state 不再在 applier 内直接构造/insert，统一提交 rehydrate snapshot/writer 或 metadata service。R8 审计新增 `member-lifecycle-rehydrate-bypass`，bad/good fixture 已覆盖 restore/apply bypass。Validation：`scripts/dev-check-changed.sh` PASS（known Hygiene tombstone review warning only；initial economy audit caught direct coconut balance projection, fixed by removing restore-side balance mutation）；`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（814 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all --soft` PASS（814 files）；targeted simulator combo PASS（264 tests）；restore/apply direct insert/constructor scan empty；`git diff --check` PASS。Phase 5 local complete；Phase-end CI still required。
- Phase 5/6 post-review disposition hardening：全新纯复审发现 `DomainRehydrateDisposition.quarantined` / `.legacyHistoryOnly` 只是结果标签、未强制影响 active schedule persistence。修复为：unregistered schedule link 返回 `.quarantined` 且不持久化；unresolved owner schedule 返回 `.legacyHistoryOnly` 且写成 completed/non-recurring/non-actionable 历史；缺失 event 的 restored reminder 跳过持久化；Cloud apply 对 skipped schedule 返回 skipped 而非 inserted/updated。R8 新增 `member-lifecycle-rehydrate-disposition-unconsumed` fixture/audit，要求 quarantined deny active persistence、legacyHistoryOnly 被 schedule writer 消费。Validation：`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（815 files, warnings=0）；`scripts/dev-check-changed.sh` PASS；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（44 tests）；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/OhanaTests -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（267 tests）；`git diff --check` PASS。Post-review fix CI pending。
- Phase 5/6 aggregate hardening：复审继续发现 existing schedule aggregate 未被完全封口：backup restore 在 writer 前跳过 existing Event/Reminder；legacyHistoryOnly 只改 Event row、不终止已挂载 pending reminders；cloud quarantined/skipped Event 可被 markSynced 后留下旧 active row。修复为：backup Event/Reminder 全量进入 `DomainScheduleRehydrateWriter.upsert...`；writer 对 existing Event aggregate 执行 history-only/quarantine neutralization 并终止旧 reminders；linked event 已 history-only 时 reminder rehydrate 自动降为 legacy history；existing orphan reminder 也会跳过；cloud live apply 对 `.skippedUnsupported` 不写 sync state，只有已 materialize local update/delete 的结果才 mark synced。R8 扩展 `member-lifecycle-rehydrate-disposition-unconsumed`：禁止 restore writer 前过滤 existing schedule、要求 history-only 消费 `event.reminders`、禁止 skipped/quarantined cloud apply 假同步。Validation：`scripts/tests/run-audit-fixture-tests.sh` PASS；`scripts/audit-member-lifecycle-gate.sh --all` PASS（815 files, warnings=0）；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/CloudSyncMetadataServiceTests` PASS（143 tests）；`scripts/dev-check-changed.sh` PASS；`git diff --check` PASS。Aggregate hardening CI pending。

## 核心判断

成员生命周期只是最先暴露问题的场景；真正病灶是业务写入、subject 解析、policy、effects 没有统一入场券。

真正根因：

- 调用方先拥有写能力，再被要求自觉问 policy。
- `Event/Reminder` 和多种业务事实用 raw string/id 表达 owner、subject、assignee、display target、effect target。
- disposition/resolver 是建议性信号，不是能力令牌。
- restore/sync/import 仍能绕过 user command 直接落 persistence。
- 审计查调用形状，没有证明“非法状态构造不出来”。

目标架构：

```text
Raw Intent / DTO / Restore Record
-> DomainSubjectResolution
-> DomainPolicyAuthorizer
-> AuthorizedMutationPlan / token
-> ScopedPersistenceWriter
-> Typed Effects Dispatcher
-> Revision / Sync / Notification / Ledger / Reward
```

## 最终验收

- command 构造不出未经授权的成员/经济/提醒/ledger 写入。
- restore/sync 构造不出 raw persistence bypass。
- deep link、delete、filter、overdue、task、notification、ledger 全部消费 typed resolution。
- 新 link type 未注册 taxonomy 时无法写入并审计红。
- bad/good fixture 覆盖 direct constructor、raw matcher、assignee-only、indirect owner、restore/apply、effects bypass。
- 结尾只做一次全新纯复审；P0/P1 必须为 0。

## 更新日志

- 2026-06-15 22:56:16 +0200 Phase 0-7 closeout 记账：按本轮默认 CI 全绿口径，GitHub Actions run `27575213112` 作为统一 closeout run 入账；Phase 1/2/4/7 从 🟢* 升为 🏁，Phase 0/3/5/6 保持 🏁。阶段总览现在显示 Domain write-kernel Phase 0-7 全部完成，后续新问题进入独立后续轮/新计划，不再回滚本轮 Phase 状态。

- 2026-06-15 22:09:06 +0200 Phase 0-7 local closeout：阶段总览已收口。Phase 0/3/5/6 保持 🏁；Phase 1/2/4/7 标为 🟢*，因为本地门禁/纯复审已过，但当前收口修复尚未 commit/push/Phase-end CI，不能冒充 CI 闭环。最新 Phase 7 post-review fix：Domain 不再构造 feature live/default dependencies；`FamilyTaskManaging` / `ReminderNotificationScheduleResult` 下沉 Domain；ReminderScheduling/Medication/Walk adapters 和 `NotificationManager` conformance 移到 Feature/App 边界；architecture audit 新增 `domain-feature-live-default-dependency` + fixture。Validation PASS：`scripts/dev-check-changed.sh`；`scripts/build-debug-fast.sh`；`scripts/test-simulator.sh -only-testing:OhanaTests/OhanaNotificationsSchedulingTests`（10 tests）；`scripts/tests/run-audit-fixture-tests.sh`；architecture/member/economy audits all；direct Domain/Models live/default concrete scan empty；`git diff --check`。Known derived-state review warnings remain existing/non-blocking；Phase-end commit/push/CI remains required before 🏁。

- 2026-06-15 21:32:10 +0200 Phase 7F/7G local checkpoint：projection / app runtime / concrete implementation dependency 收口完成。`CoconutProjectionManaging` 下沉到 Domain，backup/wallet/settings projection 消费 protocol；care ledger Oasis energy effect 移到 feature adapter；`StaticCareEventEconomyAwarder` 与 live dependency factory 移到 Economy feature；app runtime adapters 移出 Domain，Domain side 仅保留 walk/medication/reminder service adapters。architecture audit 已更新 `domain-feature-implementation-dependency` + fixture，Domain/Models concrete economy/Oasis scan clean。Validation PASS：fixture tests；architecture boundaries all；targeted simulator combo（295 tests）。Phase 7 仍未结束，等待最后纯复审后再 Phase-end commit/push/CI。

- 2026-06-15 21:35:40 +0200 Phase 7H local checkpoint：Domain presentation framework dependency 继续收口。`CareLedgerStatsService` / `HealthMetricCatalog` 改为返回 `DomainColorToken`，SwiftUI `Color` 映射在 Shared/Feature presentation extensions；architecture audit 新增 `domain-presentation-framework-dependency` 并由 fixture 自测覆盖。Validation PASS：changed-file gate；targeted simulator combo（257 tests）；fixture tests；architecture all；member lifecycle all（832 files, warnings=0）；economy all（832 files）；diff-check。Remaining accepted broader debt：Models 层仍有历史 SwiftUI presentation helpers，另开独立模型/UI 解耦轮处理。

- 2026-06-15 21:39:57 +0200 Phase 7 local complete：结尾纯复审完成，P0/P1=0。Manual scans confirm Domain/Models concrete economy/Oasis refs empty, feature command/result refs only fixture hits, feature-owned taxonomy literals only registry + fixtures, Domain service SwiftUI `Color/View` scan clean. Spot-checked residual direct writes: ledger writer / tombstoned deletes / aggregate root writes only. Validation PASS：fixture tests；member lifecycle all（832 files, warnings=0）；economy all（832 files）；architecture all；changed-file gate（known derived-state warning only）；targeted simulator combo（257 tests）；diff-check。Phase-end commit/push/CI pending；下一次 CI 会复验 run `27568234607` 暴露的本地修复。

- 2026-06-15 21:13:46 +0200 Phase 7D/7E local checkpoint：`CareEventServiceDependencies` 不再暴露 `QuestManager`；`CareLedgerRecording` 协议移入 Domain 并去掉需要 `QuestManager` 的 reward metadata API；calendar generated care reward/cooldown 与 pet medication dose reward/rollback 改为消费 `CareEventEconomyAwarding`，生产 call sites 注入 `StaticCareEventEconomyAwarder`。Validation PASS：`scripts/dev-check-changed.sh`（仅已知 `CoconutEconomyPolicyV2.swift:488 context.delete(event)` derived-state warning）；targeted simulator combo PASS（329 tests：EconomyBackdate、CareCompletion、SharedPetActionRecorder、ManualFeed、WalksLogic、ReminderActionCoordinator、RecurringFindingsRepair、MemberLifecycleGate、HomeCommandExecutor）。Remaining Phase 7 debt：Domain 仍有 `QuestManager` projection/adapter surfaces（`DataBackupManager`、`CoconutWallet*`、`CareEventEconomyAwarder`），`CareLedgerService` 仍直接触发 Oasis energy effects。

- 2026-06-15 21:05:00 +0200 Phase 4 Phase-end CI checkpoint：commit `ea3edbfbb` / GitHub Actions run `27568234607` completed with `audits` PASS、`lint` PASS、`build-test` FAIL。失败点是 `OhanaTests/familyTaskServiceSyncReopenedReminderReopensCompletedTaskOnly()` 的 fixture 未插入真实 pet 导致 reopened subject 不可解析；另有 `CloudSyncRecordApplier.swift` 对 `GachaDrawService.costPerDraw` 的 MainActor warning。当前本地树已修：test fixture 插入真实 pets，gacha cost 下沉到 `DomainGachaDrawDefaults`。下一次 Phase-end CI 将复验，不在 Phase 内重复触发 CI。

- 2026-06-15 21:01:11 +0200 Phase 7B/7C local checkpoint：economy reward type 依赖倒挂继续收口；`DomainGachaDrawDefaults` 替代 Domain restore/sync 对 `GachaDrawService.costPerDraw` 的直接依赖；新增 `DomainCareRewardAction` / `DomainCareRewardQuality` 与 `CoconutWalkRewardPolicy` Domain policy 类型，`QuestManager` 保留 compatibility alias，Domain care/effects API 不再暴露 `QuestManager.OhanaActionType|QualityBonus`。Architecture audit 新增 `domain-feature-reward-type-dependency` fixture/rule。Local validation PASS：changed-file gate；targeted simulator combo（190 tests）；fixture tests；architecture all；member lifecycle all（823 files, warnings=0）；Domain/Models reward type scan clean。CI run `27568234607`：audits/lint PASS，build-test FAIL（`familyTaskServiceSyncReopenedReminderReopensCompletedTaskOnly` fixture 缺真实 active pet owner）；current tree 已修复 fixture，等待下一次 Phase-end commit/push 后 CI 复验。

- 2026-06-15 20:50:50 +0200 Phase 7A local checkpoint：revision publishing 依赖倒挂首批收口完成；`DomainRevisionPublishing` 只暴露通用 mutation 发布，feature command result adapters 移到 `Ohana/Features/RevisionPublishing`，notification action coordinator 移出 Domain；architecture audit 新增 Domain feature command/taxonomy 禁止规则与 fixture 自测。Local validation PASS：fixture tests；architecture boundary audit；member lifecycle audit；changed-file gate；`ReminderActionCoordinatorTests`（9 tests）。复审确认 Domain/Models feature command result/executor/service scan clean，taxonomy literal 只剩 registry；下一批继续收 `QuestManager` / economy feature-type coupling。Phase 7 仍进行中，不触发 Phase-end CI。

- 2026-06-15 19:20:30 +0200 Phase 4A local checkpoint：active care fact writer 首批落地；`CareEventService` 单宠/planned care facts 与 `SharedPetActionRecorder` shared care/potty/hygiene/unknown-potty child facts 已消费 `AuthorizedDomainCareFactWrite` + `DomainCareFactWriter`。Local validation PASS：changed-file gate；`OhanaTests`（129 tests）；`OhanaTests` + `SharedPetActionRecorderTests` + `CareCompletionChokepointCharacterizationTests`（178 tests）；full member lifecycle audit（816 files, warnings=0）；full economy audit（816 files）；diff-check。Phase 4 仍进行中，Phase-end CI pending。

- 2026-06-15 19:45:55 +0200 Phase 4B/4C local checkpoint：planned/generated facts、pet/human expense、shared expense/walk、single walk + poop markers、derived medical/food/document expenses、ledger/reward/Oasis/reminder/revision effects 已消费 authorized writer/dispatcher。Local validation PASS：changed-file gate；targeted simulator combo（323 tests）；audit fixture tests；full member lifecycle audit（816 files, warnings=0）；full economy audit strict（816 files）；architecture boundaries；diff-check。纯复审 4B/4C P0/P1=0；Phase 4 不触发 CI，下一批 4D 继续收剩余 active member facts/effects。

- 2026-06-15 19:58:16 +0200 Phase 4B/4C post-review hardening：复审发现同根 P1：pet document generated expense、pet health generated medical expense、food-stock expense ledger 有路径使用已授权 writer 创建 expense，但 ledger 没消费同一份 expense write plan。修复为 helper 返回 `(expense, write)`，food-stock existing expense update 走 `DomainCareFactWriter.upsertExpenseLog`，所有 generated expense ledger 均包进对应 `DomainCareFactEffectsDispatcher.run(plan:)`。Validation PASS：`scripts/dev-check-changed.sh`；targeted simulator combo（323 tests）；`scripts/tests/run-audit-fixture-tests.sh`；`scripts/audit-member-lifecycle-gate.sh --all`（816 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all`（816 files）；`scripts/audit-architecture-boundaries.sh --all`；`git diff --check`。纯复审：4B/4C active care/expense/walk constructors 只剩 writer / rehydrate writer / no-op preview / shared-session aggregate；effects scan 剩余项均在 dispatcher 内或属 Phase 4D，P0/P1=0。Phase 4 仍进行中，不触发 Phase-end CI。

- 2026-06-15 20:12:44 +0200 Phase 4D member-content local checkpoint：新增 `DomainMemberFactWriteKernel`，dashboard weight、symptom/heat、human health、workout、pet document/receipt、moment/photo/milestone、member-creation 初始 facts 已消费 `DomainMemberFactWriteAuthorizer` + writer/effects dispatcher；receipt builder 删除 detached `PetDocument` 构造，测试改查 draft。Validation PASS：changed-file gate；targeted simulator combo（341 tests）；warning fix 后 `MemberLifecycleGateTests` + `HomeCommandExecutorTests`（230 tests）；receipt/privacy targeted（12 tests）；audit fixture tests；full member lifecycle audit（817 files, warnings=0）；full economy audit（817 files）；architecture boundaries；member-content constructor scan 只剩 active writer / rehydrate writers。Phase 4 仍进行中，不触发 Phase-end CI。

- 2026-06-15 Phase 3 local complete：Schedule writer 首批收口完成，本地 module-exit gate 通过；Phase-end CI pending。

- 2026-06-15 17:58:00 +0200 Phase 3/5/6 CI checkpoint：commit `b89d04fa2` / GitHub Actions run `27558005993` 全绿（`audits` PASS、`lint` PASS、`build-test` PASS）。Phase 3 schedule writer、Phase 5 rehydrate writer、Phase 6 R8/R9 audit upgrade 均已获得 CI 覆盖；仍未标 🏁，因为结尾纯复审 pending。

- 2026-06-15 18:13:00 +0200 Phase 5/6 post-review fix：修复 rehydrate disposition 假消费 P1；quarantined schedule 不落库，legacy history schedule 非活跃化，orphan restored reminder 跳过；新增 disposition-unconsumed audit + fixture。Local validation PASS：audit fixture、R8 全量、dev-check、MemberLifecycleGateTests、Cloud/Backup targeted combo、diff-check。新 CI 待提交后启动。

- 2026-06-15 18:35:00 +0200 Phase 5/6 aggregate hardening：收口 existing schedule aggregate；backup restore 不再跳过已有 Event/Reminder，rehydrate writer 统一终止 history/quarantine Event 的旧 reminders，existing orphan reminder 降级为 skipped，cloud skippedUnsupported 不再 markSynced。Local validation PASS：audit fixture、R8 全量、CloudSyncMetadataServiceTests + MemberLifecycleGateTests（143 tests）、dev-check、diff-check。新 CI 仍待 Phase-end/提交后启动。

- 2026-06-15 18:50:34 +0200 Phase 5/6 cleanup hardening：复审继续发现 two P1：orphan/denied existing Reminder 只降级 reminder row、未关停 owning recurring Event；history/quarantine terminalize reminder 后未取消已注册 local notification。修复为：`DomainScheduleRehydrateEventResult` / `DomainScheduleRehydrateReminderResult` 暴露 `notificationIdsToCancel`，`DomainRehydrateEffectsDispatcher` 统一消费；backup restore 保存成功后批量 cancel；cloud apply 以 `CloudSyncRecordApplyOutcome` 携带 cleanup ids，markSynced + context.save 成功后再 cancel；orphan/denied existing reminder 统一走 owning Event aggregate history-only neutralization。R8 新增 history-only notification cleanup 与 row-only reminder neutralization 检查。Validation PASS：`scripts/tests/run-audit-fixture-tests.sh`；`scripts/audit-member-lifecycle-gate.sh --all`（815 files, warnings=0）；`scripts/audit-economy-boundaries.sh --all --soft`；`scripts/dev-check-changed.sh`；`scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/CloudSyncMetadataServiceTests`（144 tests，rerun after save-before-cleanup timing fix）；`git diff --check`。本地复审 touched aggregate/effects surface P0/P1=0；新 CI 待 Phase-end/提交后启动。

- 2026-06-15 19:10:47 +0200 Phase 3/5/6 complete：commit `3a753798f` / GitHub Actions run `27562081673` 全绿（`audits` PASS、`lint` PASS、`build-test` PASS）。Phase 3/5/6 结尾纯复审证据：raw `Event(`/`Reminder(` constructor scan 只剩 `DomainScheduleWriteKernel` / `DomainRehydrateWriteKernel`；fixture tests PASS；member lifecycle audit PASS（815 files, warnings=0）；architecture boundaries PASS；economy boundaries PASS；targeted simulator tests PASS（144 tests）；P0/P1=0。Phase 3、Phase 5、Phase 6 标记 🏁。

- 2026-06-15 19:10:47 +0200 Phase 4A pre-scan：只读扫描完成，未改代码。可持久化 care fact 直接构造集中在 `CareEventService+Care.swift`、`CareEventService+Feed.swift`、`SharedPetActionRecorder.swift`、`CalendarTaskCompletionSyncService.swift` 和少量 feature commands；现有 `CareDerivationToken/CareWriteOutcome` 只保护 revision/no-op 派生，未封口 persistence writer / reward / ledger effects。下一批建议先引入 `CareFactCreateIntent -> AuthorizedMutationPlan -> DomainCareFactWriter -> CareWriteOutcome`，首批替换 `CareEventService` / `SharedPetActionRecorder` 的持久化写入，保留 no-op preview/unpersisted 构造路径。

- 2026-06-15 14:34:57 +0200 Phase 0 start：Phase 0 开始：只做全 app 写入矩阵和扫描，本阶段内不触发 CI。

- 2026-06-15 14:37:47 +0200 Phase 0 complete：本地完成：Phase 0 写入矩阵已产出：docs/planning/domain-write-kernel-write-matrix.md；本阶段仅本地验证，Phase-end CI 待提交后一次性执行。；Phase-end CI required。

- 2026-06-15 Phase 0 local gate：`bash -n scripts/track-domain-write-kernel-plan.sh` PASS；`scripts/track-domain-write-kernel-plan.sh status` PASS；`scripts/dev-check-changed.sh` PASS（no app build recommended）；`git diff --check` PASS。

- 2026-06-15 Phase 0 CI start：commit `c41e7d2be` pushed to `main`；CI run `27546879625` started. At first non-blocking check: `audits` PASS, `lint` PASS, `build-test` in progress.

- 2026-06-15 Phase 1/2 local checkpoint：generic subject/capability kernel introduced; schedule authorizer/writer now consume `AuthorizedMutationPlan`; changed-file gate PASS; `MemberLifecycleGateTests` PASS（28 tests）.

- 2026-06-15 14:40:51 +0200 Phase 1 start：Phase 1/2 并行启动：先补全 typed subject taxonomy 与全局 capability authorizer 基础，不等待 Phase 0 CI 完成。

- 2026-06-15 14:40:51 +0200 Phase 2 start：Phase 1/2 并行启动：AuthorizedMutationPlan 作为全 app 写入能力雏形；先保持行为 characterization。

- 2026-06-15 Phase 0 CI complete：run `27546879625` conclusion `success`（audits/lint/build-test all PASS）。

- 2026-06-15 Phase 1/2 local checkpoint：ledger/reminder scheduling/reward completion subject resolution now consumes `DomainSubjectResolver`; removed local raw subject matchers; added characterization for indirect insurance, assignee-only, legacy unknown, no-event, and resolved ledger writes. Validation: `scripts/dev-check-changed.sh` PASS; `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（30 tests）; `git diff --check` PASS.

- 2026-06-15 Phase 1 local checkpoint：`DomainSubjectResolver` gained an array/catalog entry point; `MemberLifecycleActiveScheduleResolver` now delegates member ownership and assignee involvement to `DomainSubjectResolution` instead of maintaining a second raw matcher. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（30 tests）; `scripts/dev-check-changed.sh` PASS.

- 2026-06-15 15:05:10 +0200 Phase 1 local checkpoint：`FocusHomeReminderDeepLinkRouter` removed local pet/human/plant raw matchers and now consumes `DomainSubjectResolver` / `DomainEntityLinkRole` for event routing and payload fallback. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（31 tests）; `scripts/dev-check-changed.sh` PASS; `git diff --check` PASS; touched owner/effect raw matcher scan returned no matches. CI status check: latest run `27546879625` remains PASS, no current in-progress run.

- 2026-06-15 15:09:52 +0200 Phase 1 local checkpoint：notification delivery classification and merge subject keys now consume `DomainEntityLinkRole` / `DomainResolvedSubjectKey` instead of raw `relatedEntityType` comparisons or local compound-id normalization. Added characterization that `Pet`/`pet` aliases and food-stock compound ids converge to the same typed owner key. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（33 tests）; `scripts/dev-check-changed.sh` PASS; `git diff --check` PASS; touched notification/deep-link/ledger/resolver raw matcher scan returned no matches.

- 2026-06-15 15:13:19 +0200 Phase 1 local checkpoint：overdue status now consumes `DomainEntityLinkRole` for plant-scoped legacy links, pet medication, human medication, and insurance classification; `plant_...` taxonomy is registered in `DomainEntityLinkRegistry` instead of being interpreted inside overdue. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（34 tests）; `scripts/dev-check-changed.sh` PASS; `git diff --check` PASS.

- 2026-06-15 15:16:55 +0200 Phase 1 local checkpoint：family task reminder assignment, review ledgers, and reward receiver ledgers now derive task subject from `DomainSubjectResolver` instead of treating raw `relatedEntityId` as a pet id. Indirect pet reminders persist the real pet id; human reminders ledger as human subjects without spoofing `relatedPetId`. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（35 tests）; `scripts/dev-check-changed.sh` PASS; `git diff --check` PASS; CI status check: latest run `27546879625` remains PASS, no current in-progress run.

- 2026-06-15 15:20:54 +0200 Phase 1 local checkpoint：member deletion command result event discovery now uses `MemberLifecycleActiveScheduleResolver` / `DomainEntityLinkRegistry` for pet, human, and plant schedule links instead of fetching by raw `relatedEntityId`. This keeps indirect pet medication/insurance and human medication links in the same typed ownership path as reminders, ledgers, and deep links. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（36 tests）; `scripts/dev-check-changed.sh` PASS; `git diff --check` PASS; deletion raw matcher scan returned no matches; CI status check: latest run `27546879625` remains PASS.

- 2026-06-15 15:26:38 +0200 Phase 1 local checkpoint：notification cancellation now accepts a typed `Pet` scope and computes cancellable ids through `MemberLifecycleActiveScheduleResolver`, so indirect pet medication/insurance schedules are not skipped by a raw pet-id matcher. Quest reward auto-complete now resolves event owner through `DomainSubjectResolver` instead of matching `relatedEntityType/id` locally. Validation: `scripts/test-simulator.sh -only-testing:OhanaTests/OhanaNotificationsSchedulingTests` PASS（10 tests）; `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests` PASS（37 tests）; reward raw matcher scan returned no matches.

- 2026-06-15 15:50:00 +0200 Phase 1/2 local checkpoint：expanded typed schedule ownership consumption across water/feed/play/potty/medication/insurance/plant/home/cat-care/oasis/startup auto-feed paths; care-plan sync now returns authorized events for downstream reminder effects; QuickPlay/QuickPotty removed view-scoped imperative fetches; notification cleanup now consumes `.cleanupActiveSchedules` lifecycle disposition. Validation: `scripts/dev-check-changed.sh` PASS; `scripts/audit-member-lifecycle-gate.sh --all` warnings=0; raw matcher scan has no Domain/Feature ownership matcher residuals beyond authorized intent/UI/payload presence cases; `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（349 tests）.

- 2026-06-15 15:55:00 +0200 Phase 1/2 local checkpoint：UI/filter consumers tightened: Human detail assignee display, Calendar plant tinting, Pet health dose history, and QuickFeed plan-reminder revision affected ids now consume typed resolver/registry helpers instead of raw id interpretation. Validation: `scripts/dev-check-changed.sh` PASS; `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（349 tests）; `scripts/audit-member-lifecycle-gate.sh --all` warnings=0; `git diff --check` PASS.

- 2026-06-15 16:14:00 +0200 Phase 1/2 local checkpoint：Event raw field consumption compressed behind `DomainEntityLink(event:)`, `DomainSubjectResolutionRequest(event:)`, and `DomainScheduleCreateIntent(event:...)`; downstream consumers now receive typed subject/link facts instead of rebuilding request/link shapes at each call site. Validation: `scripts/dev-check-changed.sh` PASS（known Hygiene tombstone false positive only）; `scripts/test-simulator.sh -only-testing:OhanaTests/MemberLifecycleGateTests -only-testing:OhanaTests/HomeCommandExecutorTests -only-testing:OhanaTests/OhanaTests` PASS（352 tests）; `scripts/audit-member-lifecycle-gate.sh --all` warnings=0; raw Event field scan leaves only schedule intent boundary; dangerous owner matcher scan returned no matches; `git diff --check` PASS; CI status check latest run `27546879625` remains PASS.

- 2026-06-15 16:18:00 +0200 Phase 1/2 local checkpoint：assignee resolution tightened from syntactic UUID parsing to context/catalog-backed Human verification; `.drop` policy now applies only when an owner remains, preventing missing assignee-only writes from becoming unscoped writes. Validation: first rerun caught `missingMemberTargetsCannotCreateCalendarPlans`; root rule fixed; `MemberLifecycleGateTests` PASS（41 tests）; combo target tests PASS（352 tests）; changed-file gate PASS; full member lifecycle audit warnings=0; raw matcher scans unchanged and dangerous owner matcher scan empty.

- 2026-06-15 16:23:10 +0200 Phase 1/2 local checkpoint：owner resolution tightened from syntactic UUID parsing to context/catalog-backed Pet/Human verification for direct owners and indirect pet/human links; active schedule resolver now forwards pet/human snapshots where it has them, while empty catalogs preserve read-only classification compatibility. Validation: `MemberLifecycleGateTests` PASS（41 tests）; combo target tests PASS（352 tests）; `scripts/dev-check-changed.sh` PASS（known Hygiene false positive only）; full member lifecycle audit warnings=0; raw matcher scans leave only authorized boundary/UI/payload/comment cases; dangerous owner matcher scan empty; latest CI run `27546879625` remains PASS.

- 2026-06-15 16:32:07 +0200 Phase 1/2 local checkpoint：effects and residual raw consumers tightened: FamilyTask reminder assignment now updates schedule assignee only through authorized schedule mutation; CareLedger and CloudSync mutation fallback subject ids consume typed resolver/registry facts; water maintenance, plant dots, feed stock reminders, Oasis plant counts, overdue/deep-link text classification no longer rebuild or depend on raw event taxonomy strings. Validation: QuickWater+Member tests PASS（56 tests）; Member+Cloud tests PASS（135 tests）; combo target tests PASS（461 tests）; changed-file gate PASS（known Hygiene false positive only）; full member lifecycle audit warnings=0; raw-wrapper scan leaves only schedule intent boundary; dangerous owner matcher scan empty; latest CI run `27546879625` remains PASS. Phase 5 debt remains explicit for Backup decode/insert and CloudSync apply.

- 2026-06-15 16:40:39 +0200 Phase 5 start：Rehydrate writer batch in progress：Backup restore 与 Cloud apply 的 Event/Reminder/CareLedgerEvent 已改走集中 rehydrate capability；Phase 内继续本地验证，不触发 CI。

- 2026-06-15 16:41:29 +0200 Phase 5 local checkpoint：rehydrate batch 1 landed for Backup restore + Cloud apply Event/Reminder/CareLedgerEvent via `DomainRehydrateWriteKernel`; old raw constructor allowlists removed from audit. Validation: changed-file gate PASS; audit fixture tests PASS; targeted simulator tests PASS（264 tests）. Phase remains 🟡; no Phase-end CI triggered.

- 2026-06-15 16:47:13 +0200 Phase 5 local checkpoint：rehydrate batch 2 landed for CloudSync pet care/fact apply paths via `DomainCareFactRehydrateWriteKernel`; targeted simulator tests PASS（264 tests）；full member lifecycle audit PASS；full economy boundary audit PASS. Phase remains 🟡; no Phase-end CI triggered.

- 2026-06-15 16:50:47 +0200 Phase 5 local checkpoint：rehydrate batch 3 landed for Backup restore pet care/fact paths via `DomainCareFactRehydrateWriteKernel`; targeted simulator tests PASS（264 tests）；full member lifecycle audit PASS；full economy boundary audit PASS；old backup decode+insert scan empty. Phase remains 🟡; no Phase-end CI triggered.

- 2026-06-15 17:01:36 +0200 Phase 5 local checkpoint：rehydrate batch 4 landed for Backup member-content paths via `DomainMemberContentRehydrateWriteKernel`; targeted simulator tests PASS（264 tests）；full member lifecycle audit PASS；full economy boundary audit PASS；covered backup decode+insert scan empty. Phase remains 🟡; no Phase-end CI triggered.

- 2026-06-15 17:19:44 +0200 Phase 5 local complete：rehydrate batch 5-7 landed for Backup root/remaining/economy/task/Oasis/gacha/shop/import paths and CloudSync non-care apply/metadata paths via `DomainGeneralRehydrateWriteKernel` / `CloudSyncMetadataService`; restore/apply bypass audit and fixtures added. Validation: changed-file gate PASS; fixture tests PASS; full member lifecycle audit PASS（814 files, warnings=0）; full economy boundary audit PASS（814 files）; targeted simulator tests PASS（264 tests）; restore/apply direct insert/constructor scan empty; git diff check PASS. No Phase-end CI triggered yet.

- 2026-06-15 17:41:29 +0200 Phase 6 complete：本地完成：R8/R9 audit upgraded: raw effect subject guessing and feature taxonomy strings now have bad/good fixtures; MedicationEventLink removed in favor of DomainEntityLinkRegistry; release data-safety audit updated for rehydrate writers; fixture tests, member audit, release audit, changed gate, targeted simulator tests PASS；Phase-end CI required。
